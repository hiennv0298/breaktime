---
phase: 02-npc-dong-nghiep
plan: 06
type: execute
wave: 3
depends_on: ["02-03", "02-04"]
files_modified:
  - .planning/STATE.md
  - src/logic/npcSettings.ts
  - tests/unit/npcSettings.test.ts
  - src/main.ts
  - src/game/npc.ts
  - src/game/game.ts
  - tests/e2e/npc.spec.ts
  - tests/e2e/characters.spec.ts
  - tests/e2e/npcNames.spec.ts
  - tests/e2e/npcSettings.spec.ts
autonomous: true
requirements: [NPC-01]
must_haves:
  truths:
    - "The plan starts with the D-12 entry guard; unless it prints GUARD_CONTINUE the plan changes no source or test file and ends with a SUMMARY marked skipped (D-12)"
    - "Up to 15 coworkers can be in the office: `?npcs=` and the settings stepper go to 15, name tags and ragdoll collision groups cover all 15 slots (D-01, NPC-01)"
    - "NPCs 11–15 walk the reused routes from their seeded start points, so every one of them spawns at least 0.5 m away from every other NPC; NPCs 1–10 keep their Phase 1 routes, start points and offsets (D-01, G3r)"
    - "`?bench=1` and `?soak=1` still run exactly 10 NPCs, so Phase 1 device numbers stay comparable (D-11)"
    - "At 15 NPCs the scene stays within 120 draw calls and 206 physics bodies (95 + 7 × 15 + 6 reserved for the player ragdoll of plan 02-11), measured and printed as MEASURE lines (D-01)"
  artifacts:
    - path: "src/logic/npcSettings.ts"
      provides: "MAX_NPCS = 15 and BENCH_NPCS = 10"
      contains: "BENCH_NPCS = 10"
    - path: "src/game/game.ts"
      provides: "Slot 0..14 spawn and start index through spawnPointForNpc / routeStartIndexForNpc"
      contains: "routeStartIndexForNpc("
    - path: "src/game/npc.ts"
      provides: "respawn(spawn, startIndex?)"
      contains: "startIndex"
    - path: "tests/e2e/characters.spec.ts"
      provides: "'cap:' test with MEASURE npcs=15 idle / smash lines"
      contains: "MAX_BODIES_AT_CAP"
  key_links:
    - from: "src/game/game.ts"
      to: "src/game/waypoints.ts"
      via: "spawnPointForNpc / routeStartIndexForNpc for slots 0..14"
      pattern: "routeStartIndexForNpc\\("
    - from: "src/main.ts"
      to: "src/logic/npcSettings.ts"
      via: "bench / soak forcedNpcCount BENCH_NPCS (10)"
      pattern: "BENCH_NPCS"
---

## Phase Goal

ROADMAP Phase 2 goal (verbatim): "Văn phòng có đồng nghiệp mang tên do người chơi đặt, số lượng thêm/bớt tuỳ ý tới 15, và bị tát đủ nhiều thì nổi giận đuổi theo đánh trả kiểu slapstick — đánh qua lại vẫn buồn cười, không máu, không thanh HP, không tụt fps".

## Execution Rule (conditional plan, D-12)

1. Run Task 1 (entry guard) first. It prints exactly one of `GUARD_CONTINUE`, `PLAN_EXIT_GATE_PENDING` or `PLAN_EXIT_STACK_STOP` (each followed by `plan=02-06 reason=…`).
2. On `PLAN_EXIT_GATE_PENDING` or `PLAN_EXIT_STACK_STOP`: **stop executing this plan immediately.** Do not start Task 2. Do not edit any file under src/ or tests/. Write the SUMMARY with `status: skipped`, the guard line and the STATE.md blocker it recorded, then end.
3. Only after `GUARD_CONTINUE` do Tasks 2 and 3 run, in order.

<objective>
Vertical slice "tới 15 đồng nghiệp": `?npcs=` and the existing settings stepper reach 15, NPCs 11–15 spawn apart on the reused routes, the bench stays at 10, and the 15-NPC budget is measured. The roster (names/looks/tempers from `bt.roster`) is plan 02-07.

Purpose: D-01 (cap 15), G3r, D-11, behind the D-12 guard. Split from the original 02-06 (plan-checker W3) so each plan stays small.
Output: cap constants, bench constant in main.ts, slot mapping in game.ts, respawn start index in npc.ts, Phase 1 specs at the new cap with a measured 15-NPC budget.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-npc-dong-nghiep/02-CONTEXT.md
@.planning/phases/02-npc-dong-nghiep/02-RESEARCH.md
@.planning/phases/02-npc-dong-nghiep/02-03-SUMMARY.md
@.planning/phases/02-npc-dong-nghiep/02-04-SUMMARY.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-23-SUMMARY.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-27-SUMMARY.md
@src/game/game.ts
@src/game/npc.ts
@src/main.ts
@src/logic/npcSettings.ts

<local_traps>
1. Write every multi-command verify into a `.sh` file in the session scratchpad and run `bash <file>.sh`; inline Bash with `set -e` has produced false GREEN here. Trust printed markers, and grep Playwright output for " failed".
2. `grep -P` fails silently here: use `grep -E` or Node.
3. PATH can drop mid-session: call `node` / `npx` directly; PowerShell for git if needed.
4. `npm run typecheck` prints nothing on success: chains append `&& echo TYPECHECK_OK`.
5. The Write tool may turn backslash-u escapes into raw invisible characters: never write them; run the control-character scan in Task 3's verify on every file you wrote.
</local_traps>

<interfaces>
Existing (verified in the repo on 16/09):

```typescript
// src/logic/npcSettings.ts: MAX_NPCS = 10, DEFAULT_NPCS = 3, normalizeNpcSettings (count clamp 0..MAX_NPCS, names length MAX_NPCS), npcCountFromQuery clamps to MAX_NPCS, resolveStartNpcSettings(search, storedRaw, forcedCount?)
// src/game/game.ts: re-exports MAX_NPCS / DEFAULT_NPCS; spawnPointFor(i) = route[i % route.length] + sharedIndexForNpc(i) × 0.3 on x;
//   ensureNpc(i): texture 'b' + k, route NPC_ROUTES[routeIndexForNpc(k)], startIndex k % route.length, spawn spawnPointFor(k) (or ?npcAt pin for slot 0), groupIndex k;
//   activateNpc(i): npc.respawn(spawnPointFor(i)); resetForSoak(): despawn + respawn(spawnPointFor(i)); labels = createNpcLabels(uiRoot, MAX_NPCS)
// src/game/npc.ts: respawn(spawn) — walker = createWalker(opts.route, { x, z }, startIndex)
// src/main.ts: createGame(ctx, soakOn || benchOn ? { forcedNpcCount: MAX_NPCS, bench: true } : {})
// src/render/characters.ts: spawnCharacter accepts letters 'a'..'r' (slot 14 → 'p', valid)
// src/physics/ragdoll.ts: ragdollGroups clamps the index to 14 (slots 0..14 get distinct bits)
```

From 02-03 / 02-04 (pure):

```typescript
// src/game/waypoints.ts: NPC_SLOT_COUNT 15, routeIndexForNpc(i) (0..14), routeStartIndexForNpc(i), spawnPointForNpc(i)
// src/logic/roster.ts: NPC_CAP = 15
```

Contracts this plan creates:

```typescript
// src/logic/npcSettings.ts
export const MAX_NPCS = 15;      // D-01 (was 10); equals NPC_CAP
export const BENCH_NPCS = 10;    // D-11: ?bench=1 and ?soak=1 stay at 10
// src/game/game.ts: re-exports BENCH_NPCS next to MAX_NPCS / DEFAULT_NPCS
// src/game/npc.ts
// respawn(spawn: { x: number; z: number }, startIndex?: number): void   // startIndex wrapped to the route length
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Entry guard — continue only when the Phase 1 device gate passed (D-12)</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - This plan's "Execution Rule" section
    - .planning/phases/02-npc-dong-nghiep/02-CONTEXT.md decision D-12
    - .planning/phases/02-npc-dong-nghiep/02-03-SUMMARY.md (guard tool contract)
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-GATE.md (may not exist; the tool handles that)
  </read_first>
  <action>Implements D-12. Run `node scripts/phase-gate-guard.mjs --plan 02-06` from the repo root and copy its single output line into the SUMMARY. The tool itself updates the one Phase 2 gate blocker line in .planning/STATE.md (adds it on pending/stop, removes it on continue); do not edit STATE.md by hand. On PLAN_EXIT_GATE_PENDING or PLAN_EXIT_STACK_STOP follow the Execution Rule: stop, no src/tests changes, SUMMARY `status: skipped`. On GUARD_CONTINUE proceed to Task 2.</action>
  <verify>
    <automated>node scripts/phase-gate-guard.mjs --plan 02-06 --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - The guard printed exactly one line matching `^(GUARD_CONTINUE|PLAN_EXIT_GATE_PENDING|PLAN_EXIT_STACK_STOP) plan=02-06 reason=`
    - GUARD_CONTINUE appears only when 01-GATE.md has `VERDICT=PASS` (or `VERDICT=FAIL` plus `VERDICT_AFTER_OPT=PASS`) and STATE.md has no `STOP: Three.js + Rapier failed`
    - On a PLAN_EXIT_* outcome: `git status --porcelain src tests` prints nothing, STATE.md contains the line starting `- [Phase 2] Tích hợp chờ cổng máy thật Phase 1`, and the SUMMARY says `status: skipped`
  </acceptance_criteria>
  <done>The plan either ended cleanly with an explicit gate-pending/stack-stop record, or is cleared to integrate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cap 15 with distinct spawn points for NPCs 11–15, bench kept at 10 (RED e2e, then GREEN)</name>
  <files>src/logic/npcSettings.ts, tests/unit/npcSettings.test.ts, src/main.ts, src/game/npc.ts, src/game/game.ts, tests/e2e/npc.spec.ts, tests/e2e/characters.spec.ts</files>
  <read_first>
    - .planning/phases/02-npc-dong-nghiep/02-CONTEXT.md decisions D-01, D-11 and G3r
    - .planning/phases/02-npc-dong-nghiep/02-RESEARCH.md "M1" table (15 NPCs: 96 draws, 200 bodies), "Pitfall 12: Label layer capacity", "Pitfall 14", "Conflicts C2"
    - src/logic/npcSettings.ts, tests/unit/npcSettings.test.ts, src/main.ts (bench / soak createGame options), src/game/npc.ts (respawn), src/game/game.ts (spawnPointFor, ensureNpc, activateNpc, resetForSoak, labels capacity)
    - src/game/waypoints.ts (spawnPointForNpc, routeStartIndexForNpc from 02-03)
    - tests/e2e/npc.spec.ts ('npcs=10 spawns ten', 'npcs clamps to 0..10'), tests/e2e/characters.spec.ts (MAX_DRAW_CALLS, MAX_BODIES, measure(), waitBroken)
  </read_first>
  <behavior>
    - npc.spec desktop 'npcs clamps to 0..15': './?autoplay=1&npcs=99' → 15 NPCs; './?autoplay=1&npcs=0' → 0
    - npc.spec desktop 'npcs=15 spawns fifteen apart': './?autoplay=1&npcs=15' → 15 NPCs, ids npc-0..npc-14 distinct, textures 'b'..'p' distinct; positions read within 300 ms after 'playing': every pair that includes an index ≥ 10 is ≥ 0.5 m apart on XZ; all positions finite with |x| < 8 and |z| < 6; zero console errors
    - characters.spec desktop test titled 'cap: 15 NPCs idle and at the smash peak within the draw budget': `const MAX_BODIES_AT_CAP = 206; // 95 + 7 × 15 + 6 player ragdoll (plan 02-11), RESEARCH C2`; './?autoplay=1&npcs=15' idle → peakDrawCalls ≤ 120, peakBodies ≤ MAX_BODIES_AT_CAP, prints `MEASURE npcs=15 idle …`; './?autoplay=1&npcs=15&scenario=smash' with ≥ 10 broken → the same budget, prints `MEASURE npcs=15 smash …`; clean pages; existing 3/10-NPC tests keep MAX_BODIES 200
    - tests/e2e/bench.spec.ts is untouched and still reports npcCount 10
    - tests/unit/npcSettings.test.ts: expectations that encode the old cap move from 10 to 15 (normalize clamps, '?npcs=99' → 15, forcedCount 99 → 15, names length 15) and `BENCH_NPCS === 10`; a new case asserts `MAX_NPCS === NPC_CAP` (import NPC_CAP from src/logic/roster.ts)
  </behavior>
  <action>Implements D-01 (cap 15), G3r (NPCs 11–15 on reused routes with seeded starts) and D-11 (bench and soak stay 10). First change tests/e2e/npc.spec.ts and add the characters.spec 'cap:' test from the behavior list, build and confirm they fail on the old 10 clamp. Then: (a) src/logic/npcSettings.ts: MAX_NPCS = 15, add BENCH_NPCS = 10, update the header (D-01) and tests/unit/npcSettings.test.ts; (b) src/main.ts: bench and soak pass { forcedNpcCount: BENCH_NPCS, bench: true } (import BENCH_NPCS through the game module re-export next to MAX_NPCS) with a comment citing D-11; (c) src/game/npc.ts: respawn(spawn, startIndex?) passes startIndex (wrapped to the route length) to createWalker when given; (d) src/game/game.ts: re-export BENCH_NPCS; replace the local spawnPointFor with spawnPointForNpc(i) from waypoints.ts; ensureNpc uses startIndex routeStartIndexForNpc(k) (texture stays 'b' + k in this plan — plan 02-07 binds looks from the roster); activateNpc and resetForSoak call respawn(spawnPointForNpc(i), routeStartIndexForNpc(i)). Slots 0–9 must give exactly the Phase 1 values (02-03 proved the helpers equal the old formulas). The existing settings section and `bt.npcs` storage keep working unchanged at the new cap (plan 02-07 moves storage to `bt.roster`). Run the two specs until green, then typecheck and unit tests.</action>
  <verify>
    <automated>npm run typecheck && echo TYPECHECK_OK && npx vitest run && echo UNIT_OK && npx vite build && echo BUILD_OK && npx playwright test tests/e2e/npc.spec.ts tests/e2e/characters.spec.ts --project=desktop && echo CAP_OK && npx playwright test tests/e2e/characters.spec.ts -g cap --project=desktop && echo CAP_MEASURED</automated>
  </verify>
  <acceptance_criteria>
    - Output contains TYPECHECK_OK, UNIT_OK, BUILD_OK, CAP_OK and CAP_MEASURED; the new npc/characters expectations were observed failing before the change (SUMMARY TDD section)
    - `grep -c "routeStartIndexForNpc(" src/game/game.ts` ≥ 2, `grep -c "BENCH_NPCS" src/main.ts` ≥ 2, `grep -c "MAX_NPCS = 15" src/logic/npcSettings.ts` = 1
    - The SUMMARY quotes `MEASURE npcs=15 idle` and `MEASURE npcs=15 smash` with peakDrawCalls ≤ 120 and peakBodies ≤ 206
  </acceptance_criteria>
  <done>`?npcs=15` fills the office with 15 coworkers that start apart; the bench still runs 10; the 15-NPC budget is measured.</done>
</task>

<task type="auto">
  <name>Task 3: Remaining Phase 1 specs at the new cap and the full suite</name>
  <files>tests/e2e/npcNames.spec.ts, tests/e2e/npcSettings.spec.ts</files>
  <read_first>
    - tests/e2e/npcNames.spec.ts ('?npcs overrides the stored count' with 42 / 99 → 10), tests/e2e/npcSettings.spec.ts (stepper stops at 10, 10 name fields, + disabled at 10)
    - src/ui/npcSettingsSection.ts (stepper and field count follow MAX_NPCS)
  </read_first>
  <action>Implements the D-01 cap in the remaining Phase 1 regression specs. Change only expectations that encode the old cap, never the behaviour under test: npcNames.spec.ts — tampered count 42 plus ?npcs=99 now clamps to 15; npcSettings.spec.ts — the stepper now stops at 15 (15 name fields, + disabled at 15); 'set ten' keeps 60 NPC ragdoll bodies at 10 and ≤ 200 HUD bodies; every other assertion unchanged. Then run the full local suite and the repo-wide gates.</action>
  <verify>
    <automated>npm run typecheck && echo TYPECHECK_OK && npx vitest run && echo UNIT_OK && npm run build && echo BUILD_OK && npm run size && npx playwright test && echo E2E_OK && node -e "const fs=require('fs'),path=require('path');const walk=d=>fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>e.isDirectory()?walk(path.join(d,e.name)):[path.join(d,e.name)]);let html=0;for(const f of walk('src').filter(f=>/\.(ts|css)$/.test(f)))for(const l of fs.readFileSync(f,'utf8').split(/\r?\n/)){if(/^\s*(\/\/|\*|\/\*)/.test(l))continue;if(/innerHTML|outerHTML|insertAdjacentHTML/.test(l))html++}const mine=['src/logic/npcSettings.ts','tests/unit/npcSettings.test.ts','src/main.ts','src/game/npc.ts','src/game/game.ts','tests/e2e/npc.spec.ts','tests/e2e/characters.spec.ts','tests/e2e/npcNames.spec.ts','tests/e2e/npcSettings.spec.ts'];let bad=0;for(const f of mine){for(const ch of fs.readFileSync(f,'utf8')){const c=ch.codePointAt(0);if((c<32&&c!==9&&c!==10&&c!==13)||(c>=127&&c<=159)||c===0xad||(c>=0x200b&&c<=0x200f)||(c>=0x2028&&c<=0x202e)||(c>=0x2060&&c<=0x206f)||c===0xfeff){bad++;console.log('RAW_CTRL',f,c.toString(16))}}}console.log('INNERHTML_GATE',html,'CTRL_SCAN',bad);process.exit(html||bad?1:0)"</automated>
  </verify>
  <acceptance_criteria>
    - Output contains TYPECHECK_OK, UNIT_OK, BUILD_OK, `SIZE_GATE_OK`, E2E_OK, `INNERHTML_GATE 0`, `CTRL_SCAN 0`, and the Playwright output has no line containing " failed"
    - tests/e2e/bench.spec.ts and tests/e2e/soak-leak.spec.ts are unchanged (`git diff --stat` shows neither) and pass
  </acceptance_criteria>
  <done>All Phase 1 NPC specs pass at the 15 cap and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query `npcs` / `npcAt` and stored `bt.npcs` count → spawned bodies | Visitor-controlled numbers |
| This build's `bt.npcs` writes ← old builds on /b/<sha>/ | Same origin, shared storage |
| Phase 1 device gate → integration | D-12 go/no-go |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06-01 | Elevation (integration on an unmeasured stack) | Task 1 guard | mitigate | `node scripts/phase-gate-guard.mjs --plan 02-06` first; fail-closed; no src/tests change on PLAN_EXIT_* |
| T-02-06-02 | Denial of service | bodies from URL or storage | mitigate | Count clamped to 15 by normalizeNpcSettings / npcCountFromQuery; grow-only pool (7 bodies per slot); 'cap:' test ≤ 206 bodies |
| T-02-06-03 | Denial of service (NPCs spawning inside each other) | slots 10–14 | mitigate | spawnPointForNpc / routeStartIndexForNpc (unit-tested in 02-03), e2e ≥ 0.5 m for every pair with an index ≥ 10 |
| T-02-06-04 | Tampering (measurement integrity) | bench / soak | mitigate | BENCH_NPCS = 10 for both; bench.spec unchanged and green |
| T-02-06-05 | Tampering | 15-entry `bt.npcs` read by an old build | accept | Old builds normalise to 10 names and clamp the count to 10; plan 02-07 stops writing `bt.npcs` altogether |
| T-02-06-SC | Tampering | npm installs | accept | No package installed |
</threat_model>

<verification>
- Guard line recorded; on GUARD_CONTINUE: typecheck, unit, build, size gate and full Playwright green; MEASURE lines at 15 NPCs within 120 draws / 206 bodies
</verification>

<success_criteria>
- The office holds up to 15 coworkers that start apart on the reused routes, the bench stays comparable, and the budget at 15 is measured (NPC-01 cap)
</success_criteria>

## Artifacts this phase produces

- Symbols: `MAX_NPCS = 15`, `BENCH_NPCS = 10` (re-exported from game.ts); `Npc.respawn(spawn, startIndex?)`
- game.ts slot mapping through `spawnPointForNpc` / `routeStartIndexForNpc`
- Tests: `npc.spec.ts` ('npcs clamps to 0..15', 'npcs=15 spawns fifteen apart'), `characters.spec.ts` test 'cap: …' (MEASURE npcs=15), updated `npcNames.spec.ts`, `npcSettings.spec.ts`, `tests/unit/npcSettings.test.ts`

<output>
Create `.planning/phases/02-npc-dong-nghiep/02-06-SUMMARY.md` when done
</output>
