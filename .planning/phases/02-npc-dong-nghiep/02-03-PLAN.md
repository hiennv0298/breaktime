---
phase: 02-npc-dong-nghiep
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lib/phaseGate.mjs
  - scripts/phase-gate-guard.mjs
  - tests/unit/phaseGate.test.mjs
  - src/game/waypoints.ts
  - tests/unit/waypoints.test.ts
autonomous: true
requirements: [NPC-01, NPC-06]
must_haves:
  truths:
    - "One command, `node scripts/phase-gate-guard.mjs --plan 02-NN`, decides whether a Phase 2 integration plan may touch the game: it prints GUARD_CONTINUE only when 01-GATE.md has a `VERDICT=PASS` line, or `VERDICT=FAIL` followed by `VERDICT_AFTER_OPT=PASS`; a missing file, REMEASURE, a FAIL still waiting for the D-07 pass, or a missing verdict prints PLAN_EXIT_GATE_PENDING; a final failure or a STATE.md STOP prints PLAN_EXIT_STACK_STOP (D-12)"
    - "When the guard does not continue it keeps exactly one Phase 2 gate blocker line in STATE.md (inserted, or replaced when the reason changed; the same bytes for every integration plan, so plans in parallel worktrees never conflict) and removes that line once the gate passes; it changes nothing else and never exits non-zero for a pending or stop outcome, so the plan ends cleanly instead of blocking forever (D-12)"
    - "NPC slots 10–14 (NPCs 11–15) reuse the 5 hand-placed routes 0..4 with a seeded start point, so every slot 10–14 spawn point is at least 0.6 m from every other slot spawn point; slots 0–9 keep exactly the routes, start indices and spawn offsets they have today (D-01, G3r, Phase 1 bench comparability D-11)"
    - "The route list itself is unchanged (still 5 routes, same points), so the ?bench=1 waypoint loop and timeline stay identical (D-11)"
  artifacts:
    - path: "scripts/lib/phaseGate.mjs"
      provides: "Pure verdict parser and STATE.md blocker insertion"
      exports: ["GATE_FILE", "STATE_FILE", "BLOCKER_PREFIX", "readGateVerdict", "blockerLine", "upsertBlocker", "removeBlocker"]
    - path: "scripts/phase-gate-guard.mjs"
      provides: "CLI entry guard for Phase 2 integration plans"
      contains: "GUARD_CONTINUE"
    - path: "src/game/waypoints.ts"
      provides: "Slot 0..14 route, start index and spawn point mapping plus farthest route point pick"
      exports: ["NPC_SLOT_COUNT", "routeIndexForNpc", "sharedIndexForNpc", "routeStartIndexForNpc", "spawnPointForNpc", "farthestRouteIndex"]
  key_links:
    - from: "scripts/phase-gate-guard.mjs"
      to: ".planning/phases/01-spike-k-thu-t-ng-deploy/01-GATE.md"
      via: "readGateVerdict(gateText, stateText), then upsertBlocker / removeBlocker"
      pattern: "readGateVerdict\\("
    - from: "src/game/waypoints.ts"
      to: "src/logic/rng.ts"
      via: "mulberry32 seeded start index for slots 10..14"
      pattern: "mulberry32\\("
---

## Phase Goal

ROADMAP Phase 2 goal (verbatim): "Văn phòng có đồng nghiệp mang tên do người chơi đặt, số lượng thêm/bớt tuỳ ý tới 15, và bị tát đủ nhiều thì nổi giận đuổi theo đánh trả kiểu slapstick — đánh qua lại vẫn buồn cười, không máu, không thanh HP, không tụt fps".

Ordering (D-12): this plan is tooling + pure route data. It changes no runtime behaviour today (the game still spawns at most 10 NPCs until plan 02-06), so it needs no entry guard.

<objective>
(1) The D-12 entry guard as a tested Node tool that every integration plan (02-06..02-13) runs first. (2) Route, start-point and spawn mapping for NPC slots 10–14, plus a "farthest route point from the player" helper for quick add.

Purpose: D-12 (integration waits for the Phase 1 device verdict, outcome explicit, never an endless human block) and D-01 / G3r (cap 15 needs 5 more slots without new routes or a navmesh).
Output: scripts/lib/phaseGate.mjs, scripts/phase-gate-guard.mjs, tests/unit/phaseGate.test.mjs; extended src/game/waypoints.ts and tests/unit/waypoints.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-npc-dong-nghiep/02-CONTEXT.md
@.planning/phases/02-npc-dong-nghiep/02-RESEARCH.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-19-PLAN.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-21-PLAN.md
@.planning/STATE.md
@src/game/waypoints.ts
@src/game/game.ts
@scripts/lib/stateCheck.mjs
@tests/unit/stateCheck.test.mjs

<local_traps>
1. Run each multi-command verify by writing it into a `.sh` file in the session scratchpad and running `bash <file>.sh`; inline Bash with `set -e` has produced false GREEN here. Trust printed markers.
2. `grep -P` fails silently here: use `grep -E` or Node.
3. PATH can drop mid-session: call `node` directly; PowerShell for git if needed.
4. `npm run typecheck` prints nothing on success: chains append `&& echo TYPECHECK_OK`.
5. The Write tool may turn backslash-u escapes into raw invisible characters: never write them; run the control-character scan in Task 2's verify.
6. Vietnamese text in STATE.md must stay UTF-8: read and write with `fs.readFileSync(path, 'utf8')` / `fs.writeFileSync(path, text, 'utf8')`, never through shell redirection.
</local_traps>

<interfaces>
Gate file format written by plans 01-19 / 01-21 (not yet present — Phase 1 waits at 01-19):
- `01-GATE.md` lines include `VERDICT=PASS|FAIL|REMEASURE` (01-19) and, after the D-07 pass, `VERDICT_AFTER_OPT=PASS|FAIL|REMEASURE` (01-21).
- On a second failure 01-21 writes the STATE.md blocker text `STOP: Three.js + Rapier failed`.
- STATE.md has a `### Blockers/Concerns` heading followed by `- ` bullet lines.

Existing route mapping (src/game/waypoints.ts, 01-23) — keep these results for slots 0..9 exactly:
- `NPC_ROUTES: WaypointPoint[][]` has 5 routes (lengths 6, 7, 8, 8, 6 — read the file to confirm).
- `routeIndexForNpc(i)`: index truncated and clamped to 0..9; i < 8 → i % 3; 8 → 3; 9 → 4; NaN → 0; +Infinity → 4.
- `sharedIndexForNpc(i)`: i < 8 → floor(i / 3); 8, 9 → 0.
- game.ts today: startIndex = k % route.length; spawn = route[i % route.length] + sharedIndexForNpc(i) × 0.3 m on x.

Contracts this plan creates:

```js
// scripts/lib/phaseGate.mjs (pure ESM, no fs access)
export const GATE_FILE = '.planning/phases/01-spike-k-thu-t-ng-deploy/01-GATE.md';
export const STATE_FILE = '.planning/STATE.md';
/** @returns {{ status: 'pass'|'pending'|'stop', reason: string, verdict: string|null, afterOpt: string|null }} */
export function readGateVerdict(gateText, stateText);
export const BLOCKER_PREFIX = '- [Phase 2] Tích hợp chờ cổng máy thật Phase 1';
export function blockerLine(reason); // BLOCKER_PREFIX + ' (01-GATE.md: <reason>) — các plan tích hợp 02-06..02-13 dừng ở entry guard, chưa sửa code (D-12)'
export function upsertBlocker(stateText, reason); // exactly one line starting with BLOCKER_PREFIX: insert, replace when different, keep when identical → { text, changed }
export function removeBlocker(stateText);         // drop that line when present → { text, changed }
```

```typescript
// src/game/waypoints.ts (extended, pure)
export const NPC_SLOT_COUNT = 15;                               // slots 0..14
export function routeIndexForNpc(i: number): number;            // 0..9 unchanged; 10..14 → (i - 10) % 5; clamp 0..14
export function sharedIndexForNpc(i: number): number;           // 0..9 unchanged; 10..14 → 0
export function routeStartIndexForNpc(i: number): number;       // 0..9 → i % route.length; 10..14 seeded (see Task 2)
export function spawnPointForNpc(i: number): { x: number; z: number }; // route[start] + sharedIndex × 0.3 on x
export function farthestRouteIndex(route: readonly { x: number; z: number }[], px: number, pz: number): number; // -1 for empty
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-12 entry-guard tool with verdict parser and idempotent STATE.md blocker (RED then GREEN)</name>
  <files>scripts/lib/phaseGate.mjs, scripts/phase-gate-guard.mjs, tests/unit/phaseGate.test.mjs</files>
  <read_first>
    - .planning/phases/02-npc-dong-nghiep/02-CONTEXT.md decision D-12 (integration plans read 01-GATE.md; continue only on PASS; stop if Phase 1 stops to change stack)
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-19-PLAN.md Task 3 action (VERDICT line format and REMEASURE) and 01-21-PLAN.md (VERDICT_AFTER_OPT and the `STOP: Three.js + Rapier failed` blocker)
    - .planning/STATE.md section `### Blockers/Concerns` (bullet format)
    - scripts/lib/stateCheck.mjs and tests/unit/stateCheck.test.mjs (pure-lib + Vitest .mjs pattern in this repo)
  </read_first>
  <behavior>
    - readGateVerdict(null, '') → status 'pending', reason 'gate-missing'
    - gate 'SHA=abc\nVERDICT=PASS\n' → 'pass', reason 'passed'; with CRLF line endings also 'pass'
    - 'VERDICT=FAIL\nFAILED=GATE_ANDROID_FPS\n' → 'pending', 'awaiting-d07'; plus 'VERDICT_AFTER_OPT=PASS' → 'pass', 'passed-after-d07'; plus 'VERDICT_AFTER_OPT=FAIL' → 'stop', 'failed-after-d07'; plus 'VERDICT_AFTER_OPT=REMEASURE' → 'pending', 'remeasure-after-d07'
    - 'VERDICT=REMEASURE' → 'pending', 'remeasure'; a file without any `VERDICT=` line → 'pending', 'verdict-missing'
    - A line `  VERDICT=PASS` indented, `XVERDICT=PASS`, or `VERDICT=PASSED` does not count as PASS (anchored `^VERDICT=(PASS|FAIL|REMEASURE)$` per line after trimming the trailing CR only)
    - `VERDICT_AFTER_OPT=PASS` alone (no VERDICT line) → 'pending', 'verdict-missing'
    - stateText containing 'STOP: Three.js + Rapier failed' → 'stop', 'stack-stop' even when the gate says PASS
    - upsertBlocker on a STATE text with `### Blockers/Concerns` inserts blockerLine(reason) as the first bullet after that heading and returns changed true; the same reason again returns identical text and changed false; a different reason replaces the existing line in place (still exactly one line starting with BLOCKER_PREFIX); text without the heading gets a new `### Blockers/Concerns` section with the line appended at the end; every other byte of the input is preserved; the real STATE.md uses CRLF line endings, so when the input contains CRLF the inserted or replaced line ends with CRLF too (test both LF and CRLF fixtures)
    - removeBlocker drops that line (only that line) and returns changed true; text without it comes back identical with changed false
    - blockerLine('gate-missing') is exactly `- [Phase 2] Tích hợp chờ cổng máy thật Phase 1 (01-GATE.md: gate-missing) — các plan tích hợp 02-06..02-13 dừng ở entry guard, chưa sửa code (D-12)`
    - CLI (spawned with node in the test, using temp copies passed through --gate / --state): pass → stdout exactly one line `GUARD_CONTINUE plan=<id> reason=<r>` and a pre-existing Phase 2 gate blocker line is removed from the state copy (nothing else changes); pending → `PLAN_EXIT_GATE_PENDING plan=<id> reason=gate-missing` and the state copy gains the blocker; stop → `PLAN_EXIT_STACK_STOP plan=<id> reason=stack-stop`; running plan ids 02-08 and 02-09 against copies of the same pending state yields byte-identical files; exit code 0 in all outcomes; `--dry-run` never writes; a missing or malformed --plan (not matching ^02-\d{2}$) exits 2 with `GUARD_USAGE` on stderr
  </behavior>
  <action>Implements D-12's entry guard as a deterministic tool so integration plans never depend on a human-action checkpoint that blocks forever. Write tests/unit/phaseGate.test.mjs first (Vitest, import from '../../scripts/lib/phaseGate.mjs', CLI cases via child_process.spawnSync(process.execPath, ['scripts/phase-gate-guard.mjs', ...]) with temp files under os.tmpdir()) and confirm it fails. Implement scripts/lib/phaseGate.mjs as pure functions over strings (split on CR/LF, per-line anchored regexes, fail-closed: anything that is not a clean PASS is pending or stop). Implement scripts/phase-gate-guard.mjs: parse `--plan`, optional `--gate` (default GATE_FILE), `--state` (default STATE_FILE), `--dry-run`; read files with fs.readFileSync(..., 'utf8') treating ENOENT as null; print exactly one marker line (`GUARD_CONTINUE plan=<id> reason=<r>`, `PLAN_EXIT_GATE_PENDING plan=<id> reason=<r>` or `PLAN_EXIT_STACK_STOP plan=<id> reason=<r>`); unless --dry-run, write back with 'utf8' only when changed is true (upsertBlocker on pending/stop, removeBlocker on continue); exit 0. The blocker text deliberately contains no plan id, so plans in parallel worktrees write identical bytes; the plan id appears in stdout and in each plan SUMMARY. Interpretation recorded in the lib header: D-12 says "VERDICT=PASS mới làm"; a gate that failed and then passed after the single D-07 pass (VERDICT_AFTER_OPT=PASS, 01-21) also means Phase 1 did not stop, so it continues; this reading is flagged in the Phase 2 planning return for the operator. Finally run `node scripts/phase-gate-guard.mjs --plan 02-03 --dry-run` against the real repo (today 01-GATE.md does not exist, so it must print PLAN_EXIT_GATE_PENDING plan=02-03 reason=gate-missing and write nothing).</action>
  <verify>
    <automated>npx vitest run tests/unit/phaseGate.test.mjs && echo UNIT_GREEN && node -e "const fs=require('fs');fs.writeFileSync(require('path').join(require('os').tmpdir(),'bt-state-before.tmp'),fs.readFileSync('.planning/STATE.md','utf8'),'utf8')" && node scripts/phase-gate-guard.mjs --plan 02-03 --dry-run && node -e "const fs=require('fs');const p=require('path').join(require('os').tmpdir(),'bt-state-before.tmp');const same=fs.readFileSync(p,'utf8')===fs.readFileSync('.planning/STATE.md','utf8');fs.unlinkSync(p);console.log(same?'STATE_UNTOUCHED':'STATE_WRITTEN_BAD');process.exit(same?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - Output contains UNIT_GREEN, a line matching `^(GUARD_CONTINUE|PLAN_EXIT_GATE_PENDING|PLAN_EXIT_STACK_STOP) plan=02-03 reason=` (today `PLAN_EXIT_GATE_PENDING plan=02-03 reason=gate-missing` unless 01-GATE.md exists), and STATE_UNTOUCHED
    - scripts/lib/phaseGate.mjs contains 'STOP: Three.js + Rapier failed', 'VERDICT_AFTER_OPT' and 'dừng ở entry guard'
    - `git diff --stat -- .planning/STATE.md` shows no change caused by the dry run
  </acceptance_criteria>
  <done>Every integration plan can run one command that continues, exits pending with a STATE blocker, or exits on a stack stop — tested and fail-closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Slots 10–14 on the existing routes with seeded start points (RED then GREEN)</name>
  <files>src/game/waypoints.ts, tests/unit/waypoints.test.ts</files>
  <read_first>
    - .planning/phases/02-npc-dong-nghiep/02-CONTEXT.md decision D-01 (cap 15) and Claude's Discretion G3r (NPCs 11–15 reuse the 5 hand-placed routes with seeded phase/offset, still pass furniture clearance)
    - .planning/phases/02-npc-dong-nghiep/02-RESEARCH.md "Pitfall 14: Route and texture index clamps above 10", "Pitfall 15: Spawning onto the player", "Pattern 6"
    - src/game/waypoints.ts (whole file), tests/unit/waypoints.test.ts (whole file), src/game/game.ts functions spawnPointFor and ensureNpc (current slot mapping to preserve)
    - src/logic/rng.ts (mulberry32, BENCH_SEED)
  </read_first>
  <behavior>
    - Regression: for i in 0..9, routeIndexForNpc, sharedIndexForNpc, routeStartIndexForNpc and spawnPointForNpc return exactly what today's code gives (routeIndexForNpc values [0,1,2,0,1,2,0,1,3,4]; start = i % route.length; spawn = route[i % route.length] + sharedIndex × 0.3 on x) — write these expectations from the current code before changing it
    - routeIndexForNpc(10..14) === [0,1,2,3,4]; routeIndexForNpc(99) === 4; routeIndexForNpc(NaN) === 0; routeIndexForNpc(+Infinity) === 4; sharedIndexForNpc(10..14) all 0
    - routeStartIndexForNpc(10..14) are integers within their route length, identical across repeated calls, and computed from mulberry32(BENCH_SEED + i)
    - For every slot b in 10..14 and every other slot a in 0..14 (a ≠ b) the spawn points are ≥ 0.6 − 1e-9 m apart; pairs where both slots are in 0..9 keep their Phase 1 positions and are NOT required to be 0.6 m apart (today slot 2 and slot 4 start 0.3 m apart on the west column — existing behaviour, unchanged)
    - Every slot 10..14 spawn point is one of its route's points (offset 0), so the existing ≥ 0.35 m furniture clearance and wall tests already cover it; the test asserts membership
    - farthestRouteIndex(route, px, pz) returns the index with the largest distance (ties → lower index), -1 for an empty route, ignores non-finite points
    - NPC_ROUTES still has exactly 5 routes and every existing route test still passes unchanged
  </behavior>
  <action>Implements G3r for the D-01 cap 15 as pure data. First add regression cases for slots 0..9 to tests/unit/waypoints.test.ts using the current functions, then the new slot 10..14 cases, and confirm the new ones fail. In src/game/waypoints.ts: export NPC_SLOT_COUNT = 15; raise the clamp (MAX_NPC_INDEX) from 9 to 14; routeIndexForNpc maps 10..14 to (i − 10) % 5 while 0..9 keep today's rule; sharedIndexForNpc gives 0 for 8..14; add routeStartIndexForNpc (0..9: i % route.length; 10..14: take mulberry32(BENCH_SEED + i), produce a Fisher–Yates order of that route's point indices, pick the first index whose point is ≥ 0.6 m from every spawn point of slots 0..i−1, falling back to the index farthest from all of them); memoise the 10..14 results in a module-level array computed lazily in slot order; add spawnPointForNpc (moved logic of game.ts spawnPointFor, which plan 02-06 will switch to) and farthestRouteIndex. Do not add or move any route point and do not touch game.ts in this plan. Update the header comment: slots 10–14 reuse routes (G3r), navmesh replaces all of this in Phase 3.</action>
  <verify>
    <automated>npx vitest run tests/unit/waypoints.test.ts && echo UNIT_GREEN && npm run typecheck && echo TYPECHECK_OK && npx vitest run && echo ALL_UNIT_OK && node -e "const fs=require('fs');const files=['src/game/waypoints.ts','tests/unit/waypoints.test.ts','scripts/lib/phaseGate.mjs','scripts/phase-gate-guard.mjs','tests/unit/phaseGate.test.mjs'];let bad=0;for(const f of files){const s=fs.readFileSync(f,'utf8');for(const ch of s){const c=ch.codePointAt(0);if((c<32&&c!==9&&c!==10&&c!==13)||(c>=127&&c<=159)||c===0xad||(c>=0x200b&&c<=0x200f)||(c>=0x2028&&c<=0x202e)||(c>=0x2060&&c<=0x206f)||c===0xfeff){bad++;console.log('RAW_CTRL',f,c.toString(16))}}}const w=fs.readFileSync('src/game/waypoints.ts','utf8').split(/\r?\n/).filter(l=>!/^\s*(\/\/|\*|\/\*)/.test(l)&&/from 'three'|@dimforge|Math\.random/.test(l)).length;console.log('CTRL_SCAN',bad,'PURE_GATE',w);process.exit(bad||w?1:0)" && git diff --quiet -- src/game/game.ts && echo GAME_UNTOUCHED</automated>
  </verify>
  <acceptance_criteria>
    - Output contains UNIT_GREEN, TYPECHECK_OK, ALL_UNIT_OK, `CTRL_SCAN 0`, `PURE_GATE 0` and GAME_UNTOUCHED
    - tests/unit/waypoints.test.ts asserts `NPC_ROUTES.length` is 5 and the ≥ 0.6 m spawn distance for every pair that includes a slot in 10..14
    - src/game/waypoints.ts contains `NPC_SLOT_COUNT = 15` and `mulberry32(`
  </acceptance_criteria>
  <done>15 slots have distinct, clearance-safe spawn points on the existing routes; slots 0..9 are byte-for-byte the Phase 1 mapping.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 01-GATE.md / STATE.md (human-edited markdown) → integration go/no-go | Text parsed to decide whether game code may change |
| guard CLI → STATE.md | Tool writes into a shared planning document |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering / Elevation (integration on an unmeasured stack) | `readGateVerdict` | mitigate | Fail-closed: only an anchored `VERDICT=PASS` line (or FAIL + `VERDICT_AFTER_OPT=PASS`) continues; indented / prefixed / misspelled lines do not; STATE STOP overrides PASS; unit tests per case |
| T-02-03-02 | Tampering (planning doc corruption) | `upsertBlocker` / `removeBlocker` / CLI write | mitigate | One plan-independent line upserted or removed, utf8 read/write, `--dry-run`, byte-preservation and parallel-identical unit tests |
| T-02-03-03 | Denial of service (plan blocks forever) | guard outcome | mitigate | Exit 0 with an explicit PLAN_EXIT_* marker and a STATE blocker instead of a human-action checkpoint |
| T-02-03-04 | Denial of service (NPCs spawning inside each other) | `routeStartIndexForNpc` | mitigate | ≥ 0.6 m spawn distance test for every pair including a slot 10..14; spawn points are route points already covered by the furniture clearance test |
| T-02-03-SC | Tampering | npm installs | accept | No package installed |
</threat_model>

<verification>
- `npx vitest run && npm run typecheck` green; the guard dry run prints a marker and leaves STATE.md untouched; game.ts untouched
</verification>

<success_criteria>
- D-12 guard exists as a tested command with explicit outcomes; D-01/G3r slot mapping for 15 NPCs is pure, seeded and clearance-safe
</success_criteria>

## Artifacts this phase produces

- Tool: `node scripts/phase-gate-guard.mjs --plan 02-NN [--gate <path>] [--state <path>] [--dry-run]` → `GUARD_CONTINUE plan=<id> reason=…` | `PLAN_EXIT_GATE_PENDING plan=<id> reason=…` | `PLAN_EXIT_STACK_STOP plan=<id> reason=…`
- Symbols: `GATE_FILE`, `STATE_FILE`, `BLOCKER_PREFIX`, `readGateVerdict`, `blockerLine`, `upsertBlocker`, `removeBlocker`; `NPC_SLOT_COUNT`, `routeIndexForNpc` (0..14), `sharedIndexForNpc`, `routeStartIndexForNpc`, `spawnPointForNpc`, `farthestRouteIndex`
- STATE.md blocker line format (one line, same for every plan): `- [Phase 2] Tích hợp chờ cổng máy thật Phase 1 (01-GATE.md: <reason>) — các plan tích hợp 02-06..02-13 dừng ở entry guard, chưa sửa code (D-12)`
- Tests: `tests/unit/phaseGate.test.mjs`, extended `tests/unit/waypoints.test.ts`

<output>
Create `.planning/phases/02-npc-dong-nghiep/02-03-SUMMARY.md` when done
</output>
