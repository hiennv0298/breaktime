---
phase: 02-npc-dong-nghiep
plan: 08
type: execute
wave: 5
depends_on: ["02-04", "02-07"]
files_modified:
  - .planning/STATE.md
  - src/input/inputState.ts
  - src/input/keyboard.ts
  - src/logic/keyMap.ts
  - tests/unit/keyMap.test.ts
  - tests/e2e/keyHints.spec.ts
  - src/ui/quickNpcPill.ts
  - src/ui/quickNpcPill.css
  - src/game/game.ts
  - tests/e2e/quickNpc.spec.ts
autonomous: true
requirements: [NPC-01]
must_haves:
  truths:
    - "The plan starts with the D-12 entry guard; unless it prints GUARD_CONTINUE nothing under src/ or tests/ changes and the SUMMARY is marked skipped (D-12)"
    - "While playing, pressing + (Equal or NumpadAdd) adds the next present roster member to the office and − (Minus or NumpadSubtract) removes the last one added, between 0 and min(15, present members), without opening settings (D-02, D-01, NPC-01)"
    - "A translucent '− N +' pill on the HUD (desktop and mobile) does the same with taps or clicks, shows the live count, never swings the arm and never starts the joystick (D-02 data-hud-panel)"
    - "Ctrl/Cmd/Alt with + or − stay browser zoom and are never default-prevented; key auto-repeat, keys typed into text fields and presses while the pause menu is open change nothing (D-02, Phase 1 D-27)"
    - "A newly added coworker appears at the point of its own route farthest from the player (never on top of the player), NPCs already in the office keep their state, and the physics pool never grows past 15 NPC slots (D-02, RESEARCH Pitfall 15, T-01-27-02)"
    - "The new count is saved to `bt.roster` about 0.5 s after the last change and survives reload; `?bench=1` hides the pill and ignores the keys (D-11)"
    - "The desktop key hint panel lists the new row '+/− thêm/bớt NPC' (D-02, Phase 1 D-28)"
  artifacts:
    - path: "src/ui/quickNpcPill.ts"
      provides: "HUD '− N +' control (createElement/textContent only, data-hud-panel)"
      exports: ["QuickNpcPill", "createQuickNpcPill"]
    - path: "src/ui/quickNpcPill.css"
      provides: "Translucent pill placement under the build badge, safe-area aware"
      contains: "#npc-pill"
    - path: "src/input/inputState.ts"
      provides: "npcDelta queue and consumeNpcDelta"
      exports: ["consumeNpcDelta"]
    - path: "tests/e2e/quickNpc.spec.ts"
      provides: "Keys, numpad, pill clicks and taps, bounds, pool size, repeat/typing/paused/Ctrl guards, spawn distance, save + reload, bench"
  key_links:
    - from: "src/input/keyboard.ts"
      to: "src/logic/keyMap.ts"
      via: "classifyKey → 'npc-add' / 'npc-remove' → s.npcDelta"
      pattern: "npc-add"
    - from: "src/game/game.ts"
      to: "src/logic/quickNpc.ts"
      via: "consumeNpcDelta → quickAdd / quickRemove → applyRoster(…, 'quick') → scheduleWriteRoster"
      pattern: "quickAdd\\("
    - from: "src/game/game.ts"
      to: "src/game/waypoints.ts"
      via: "farthestRouteIndex for the spawn point of a newly added slot"
      pattern: "farthestRouteIndex\\("
---

## Phase Goal

ROADMAP Phase 2 goal (verbatim): "Văn phòng có đồng nghiệp mang tên do người chơi đặt, số lượng thêm/bớt tuỳ ý tới 15, và bị tát đủ nhiều thì nổi giận đuổi theo đánh trả kiểu slapstick — đánh qua lại vẫn buồn cười, không máu, không thanh HP, không tụt fps".

## Execution Rule (conditional plan, D-12)

1. Run Task 1 (entry guard) first. It prints exactly one of `GUARD_CONTINUE`, `PLAN_EXIT_GATE_PENDING` or `PLAN_EXIT_STACK_STOP` (each followed by `plan=02-08 reason=…`).
2. On `PLAN_EXIT_GATE_PENDING` or `PLAN_EXIT_STACK_STOP`: **stop immediately.** Do not start Task 2, do not edit src/ or tests/. SUMMARY `status: skipped` with the guard line, then end.
3. Only after `GUARD_CONTINUE` do Tasks 2 and 3 run, in order.

<objective>
Vertical slice "thêm/bớt đồng nghiệp ngay trong lúc chơi": +/− keys and a HUD "− N +" pill change how many present coworkers are in the office (0–15) without opening settings, spawn newcomers away from the player, and save the count.

Purpose: NPC-01 / D-02 / D-01, behind the D-12 guard.
Output: npcDelta input plumbing, keyboard handling, KEY_HINTS row, quickNpcPill UI, game wiring, quickNpc e2e.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-npc-dong-nghiep/02-CONTEXT.md
@.planning/phases/02-npc-dong-nghiep/02-RESEARCH.md
@.planning/phases/02-npc-dong-nghiep/02-04-SUMMARY.md
@.planning/phases/02-npc-dong-nghiep/02-07-SUMMARY.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-22-SUMMARY.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-24-SUMMARY.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-25-SUMMARY.md
@src/input/inputState.ts
@src/input/keyboard.ts
@src/input/pointerPick.ts
@src/input/touchButtons.ts
@src/game/game.ts
@src/ui/hud.css

<local_traps>
1. Write every multi-command verify into a `.sh` file in the session scratchpad and run `bash <file>.sh`; inline Bash with `set -e` has produced false GREEN here. Trust printed markers and grep Playwright output for " failed".
2. `grep -P` fails silently here: use `grep -E` or Node.
3. PATH can drop mid-session: call `node` / `npx` directly; PowerShell for git if needed.
4. `npm run typecheck` prints nothing on success: chains append `&& echo TYPECHECK_OK`.
5. The Write tool may turn backslash-u escapes into raw invisible characters: never write them. The minus sign '−' (U+2212) is written as the literal character, exactly as src/ui/npcSettingsSection.ts does. Run the control-character scan in Task 3's verify.
6. CSS must live in a .css file imported by the module (CSP `style-src 'self'`).
</local_traps>

<interfaces>
Existing:

```typescript
// src/input/inputState.ts: InputState { moveX; moveY; interactQueued; pauseToggleQueued }, createInputState(), consumeInteract(s), consumePauseToggle(s)
// src/input/keyboard.ts: onKeyDown returns early for typing targets (except Escape); switch(classifyKey(code, e)) with a no-op default
// src/logic/keyMap.ts (02-04): 'npc-add' (Equal, NumpadAdd), 'npc-remove' (Minus, NumpadSubtract); KEY_HINTS 4 rows (D-28)
// src/input/pointerPick.ts: clicks inside [data-hud-button], [data-hud-panel], #pause-menu, button, input, textarea, select never swing
// src/ui/hud.css: #build-badge top safe+6 px left safe+8 px z 1000; #bench-label top safe+28 px; [data-hud-button] opacity 0.6 (0.95 :active); z stack labels 90 < touch-zone 100 < key hints 150 < HUD 200 < pause menu 400
// src/game/game.ts (02-07): roster state, applyRosterInternal(r, source), Game.roster(), Game.applyRoster(r, source?), bench flag, getPauseState(), attachTouchButtons(uiRoot, input)
// src/logic/quickNpc.ts: quickAdd(r), quickRemove(r) → { roster, changed, member }; src/logic/roster.ts: maxOnFloor(r), NPC_CAP
// src/game/waypoints.ts: NPC_ROUTES, routeIndexForNpc(i), farthestRouteIndex(route, px, pz)
// src/game/rosterStore.ts: scheduleWriteRoster(r), rosterSaveState()
// src/game/npc.ts: respawn(spawn, startIndex?)
```

Contracts this plan creates:

```typescript
// src/input/inputState.ts
// InputState gains npcDelta: number (clamped to -15..15), createInputState() sets 0
export function consumeNpcDelta(s: InputState): number;   // returns and clears
// src/ui/quickNpcPill.ts
export interface QuickNpcPill { update(count: number, max: number): void; setHidden(hidden: boolean): void }
export function createQuickNpcPill(root: HTMLElement, opts: { onDelta(delta: 1 | -1): void }): QuickNpcPill;
// DOM: div#npc-pill[data-hud-panel][role=group][aria-label='Số đồng nghiệp'] > button#npc-pill-dec ('−', aria-label 'Bớt một đồng nghiệp'), output#npc-pill-value, button#npc-pill-inc ('+', aria-label 'Thêm một đồng nghiệp')
// src/logic/keyMap.ts: KEY_HINTS gains a 5th row { keys: '+/−', label: 'thêm/bớt NPC' }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Entry guard — continue only when the Phase 1 device gate passed (D-12)</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - This plan's "Execution Rule" section; .planning/phases/02-npc-dong-nghiep/02-CONTEXT.md decision D-12; scripts/lib/phaseGate.mjs header comment and scripts/phase-gate-guard.mjs (guard contract built by plan 02-03)
  </read_first>
  <action>Implements D-12. Run `node scripts/phase-gate-guard.mjs --plan 02-08` from the repo root and copy its single output line into the SUMMARY. The tool maintains the single Phase 2 gate blocker line in .planning/STATE.md; do not edit STATE.md by hand. PLAN_EXIT_* → stop per the Execution Rule; GUARD_CONTINUE → Task 2.</action>
  <verify>
    <automated>node scripts/phase-gate-guard.mjs --plan 02-08 --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one line matching `^(GUARD_CONTINUE|PLAN_EXIT_GATE_PENDING|PLAN_EXIT_STACK_STOP) plan=02-08 reason=`
    - On PLAN_EXIT_*: `git status --porcelain src tests` prints nothing and the SUMMARY says `status: skipped`
  </acceptance_criteria>
  <done>Either a clean, recorded exit or clearance to integrate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Failing quick add/remove e2e plus key and input plumbing (RED)</name>
  <files>tests/e2e/quickNpc.spec.ts, src/input/inputState.ts, src/input/keyboard.ts, src/logic/keyMap.ts, tests/unit/keyMap.test.ts, tests/e2e/keyHints.spec.ts</files>
  <read_first>
    - .planning/phases/02-npc-dong-nghiep/02-CONTEXT.md decisions D-01, D-02; .planning/phases/01-spike-k-thu-t-ng-deploy/01-CONTEXT.md D-27, D-28
    - .planning/phases/02-npc-dong-nghiep/02-RESEARCH.md "Pattern 6: Quick add/remove", "Security Domain" (key-repeat spam row), "Pitfall 15"
    - src/input/inputState.ts, src/input/keyboard.ts, src/logic/keyMap.ts, tests/unit/keyMap.test.ts ('KEY_HINTS' describe block), tests/e2e/keyHints.spec.ts (row count 4)
    - tests/e2e/npcSettings.spec.ts (menu helpers, waitNpcCount, storage seeding), tests/e2e/swing.spec.ts ([data-hud-panel] click test), tests/e2e/helpers.ts (touchTap)
  </read_first>
  <behavior>
    - quickNpc.spec desktop 'keys add and remove coworkers in play': './?autoplay=1' → 3 NPCs; press Equal → 4 within 1000 ms; NumpadAdd → 5; Minus → 4; NumpadSubtract → 3; __bt.roster.source 'quick' after a change
    - 'bounds and pool': press Equal 20 times 40 ms apart → exactly 15 NPCs and __bt.roster.max 15; __bt.ragdolls.bodies === 90; press Minus 20 times → 0 NPCs, __bt.ragdolls.bodies still 90; no page error
    - 'saved and restored': Equal ×2 (5 NPCs) → wait 800 ms → localStorage 'bt.roster' parses with count 5 and __bt.roster.savePending false; localStorage 'bt.npcs' is still null; reload → 5 NPCs, source 'stored'
    - 'newcomer spawns away from the player': after Equal from 3 → 4, npc 3 XZ distance to __bt.player.pos ≥ 1.0 m in the first sample after the change
    - 'guards': (a) a capture listener records `defaultPrevented` for keydown; press 'Control+Equal' and 'Control+Minus' → NPC count unchanged and both recorded defaultPrevented false; (b) window.dispatchEvent(new KeyboardEvent('keydown', { code: 'Equal', repeat: true })) → count unchanged; (c) an injected, focused `<input>` in the page receives Equal / Minus → count unchanged and the input value contains '=' and '-'; (d) Escape opens the menu, Equal ×2, Escape closes → count unchanged
    - 'pill clicks never swing': #npc-pill visible with data-hud-panel; #npc-pill-value text '3'; click #npc-pill-inc → 4 and value '4'; click #npc-pill-dec → 3; __bt.swing.count stays 0; at 15 #npc-pill-inc is disabled, at 0 #npc-pill-dec is disabled
    - 'bench hides the pill': './?bench=1&dur=5&autoplay=1' → after 'playing' #npc-pill is absent or hidden; press Equal → __bt.npcs length stays 10
    - quickNpc.spec mobile-emu 'pill taps work next to the joystick': touchTap the centre of #npc-pill-inc twice → 5 NPCs; __bt.player.pos moved < 0.05 m during the taps
    - tests/unit/keyMap.test.ts: KEY_HINTS equals the four D-28 rows followed by { keys: '+/−', label: 'thêm/bớt NPC' }; tests/e2e/keyHints.spec.ts expects 5 rows
    - Every e2e test: zero console errors, page errors and off-origin requests
  </behavior>
  <action>Implements the D-02 input path (keys → npcDelta) without the game reaction yet, so the new e2e is RED for the right reason. Write tests/e2e/quickNpc.spec.ts from the behavior list (desktop tests skip on mobile-emu and vice versa, like slap.spec.ts). Update tests/unit/keyMap.test.ts and tests/e2e/keyHints.spec.ts for the 5th hint row, then add the row `{ keys: '+/−', label: 'thêm/bớt NPC' }` to KEY_HINTS in src/logic/keyMap.ts (D-28 panel shows it). In src/input/inputState.ts add `npcDelta: number` (0 in createInputState) and consumeNpcDelta. In src/input/keyboard.ts add cases 'npc-add' / 'npc-remove': when !e.repeat, s.npcDelta = clamp(s.npcDelta ± 1, −15, 15); do not call preventDefault (Equal/Minus have no page default without modifiers; Ctrl combos already classify as null and are never touched). The typing guard at the top of onKeyDown already covers text fields. Build and run the unit suite (must be green) and the new spec (must fail on the missing reaction / #npc-pill, not on syntax).</action>
  <verify>
    <automated>npm run typecheck && echo TYPECHECK_OK && npx vitest run && echo UNIT_OK && npx vite build && echo BUILD_OK && npx playwright test tests/e2e/keyHints.spec.ts && echo HINTS_OK && node -e "const {spawnSync}=require('child_process');const r=spawnSync('npx',['playwright','test','tests/e2e/quickNpc.spec.ts','--project=desktop'],{encoding:'utf8',shell:true});const out=(r.stdout||'')+(r.stderr||'');const noTests=/No tests found/.test(out);console.log(r.status!==0&&!noTests?'E2E_RED':'E2E_NOT_RED');process.exit(r.status!==0&&!noTests?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - Output contains TYPECHECK_OK, UNIT_OK, BUILD_OK, HINTS_OK and E2E_RED
    - The failing quickNpc tests fail on NPC count / #npc-pill assertions (quote one failure in the SUMMARY), not "No tests found"
    - `grep -c "npcDelta" src/input/keyboard.ts` prints at least 2
  </acceptance_criteria>
  <done>+/− reach the input state, the hint panel shows the row, and the quick add e2e fails only because the game does not react yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: HUD pill and game reaction — add/remove in place, spawn away from the player, debounced save (GREEN)</name>
  <files>src/ui/quickNpcPill.ts, src/ui/quickNpcPill.css, src/game/game.ts</files>
  <read_first>
    - tests/e2e/quickNpc.spec.ts (Task 2), src/game/game.ts (applyRosterInternal, ensureNpc, activateNpc, bench flag, frameUpdate)
    - src/ui/keyHints.ts and src/ui/npcLabels.ts (createElement/textContent module + css import style), src/input/touchButtons.ts (pointerdown handler with preventDefault, tabIndex -1)
    - src/logic/quickNpc.ts, src/game/waypoints.ts (farthestRouteIndex), src/game/rosterStore.ts (scheduleWriteRoster)
  </read_first>
  <behavior>
    - All quickNpc.spec tests pass in both projects; the full Playwright suite stays green
  </behavior>
  <action>Implements D-02's HUD control and in-play reaction (D-01 cap, grow-only pool). src/ui/quickNpcPill.ts (imports './quickNpcPill.css'): build div#npc-pill with data-hud-panel, role group and aria-label 'Số đồng nghiệp', children button#npc-pill-dec (textContent '−'), output#npc-pill-value (aria-live polite), button#npc-pill-inc ('+'); buttons type button, tabIndex −1; each button's pointerdown (mouse left button or any touch/pen) calls preventDefault and opts.onDelta(−1 / +1); update(count, max) writes textContent and disabled states only when values changed; setHidden toggles hidden. createElement/textContent only. src/ui/quickNpcPill.css: #npc-pill position fixed, top calc(env(safe-area-inset-top) + 52px), left calc(env(safe-area-inset-left) + 8px), z-index 200, display flex, gap 4px, align-items center, padding 4px, border-radius 14px, background rgba(0, 0, 0, 0.45), opacity 0.6 with 0.95 on :active and :hover, touch-action none, user-select none; buttons 44 × 44 px, border 0, border-radius 10px, font 700 24px system-ui, color #fff, background rgba(255, 255, 255, 0.15); disabled buttons opacity 0.35; output min-width 2ch, text-align center, font 700 18px system-ui; [hidden] display none. src/game/game.ts: create the pill after attachTouchButtons with onDelta adding ±1 to input.npcDelta (same clamp); in bench mode call setHidden(true) and never create listeners that change the roster. In fixedUpdate (before player.fixedUpdate): d = consumeNpcDelta(input); ignore when bench; apply |d| times quickAdd (d > 0) or quickRemove (d < 0) on a working copy; if anything changed call applyRosterInternal(next, 'quick') with a flag so that every slot that becomes active in this call (created or respawned) spawns at NPC_ROUTES[routeIndexForNpc(i)][farthestRouteIndex(route, player.x, player.z)] with that index as startIndex; then scheduleWriteRoster(next). In frameUpdate: when paused set input.npcDelta = 0 (presses while the menu is open are dropped), and call pill.update(roster.count, maxOnFloor(roster)). NPCs that stay in the office are not respawned. Run the quickNpc spec in both projects, then the full suite and gates.</action>
  <verify>
    <automated>npm run typecheck && echo TYPECHECK_OK && npx vitest run && echo UNIT_OK && npm run build && echo BUILD_OK && npm run size && npx playwright test tests/e2e/quickNpc.spec.ts && echo QUICK_OK && npx playwright test && echo E2E_OK && node -e "const fs=require('fs'),path=require('path');const walk=d=>fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>e.isDirectory()?walk(path.join(d,e.name)):[path.join(d,e.name)]);let html=0;for(const f of walk('src').filter(f=>/\.(ts|css)$/.test(f)))for(const l of fs.readFileSync(f,'utf8').split(/\r?\n/)){if(/^\s*(\/\/|\*|\/\*)/.test(l))continue;if(/innerHTML|outerHTML|insertAdjacentHTML/.test(l))html++}const mine=['src/input/inputState.ts','src/input/keyboard.ts','src/logic/keyMap.ts','tests/unit/keyMap.test.ts','tests/e2e/keyHints.spec.ts','src/ui/quickNpcPill.ts','src/ui/quickNpcPill.css','src/game/game.ts','tests/e2e/quickNpc.spec.ts'];let bad=0;for(const f of mine){for(const ch of fs.readFileSync(f,'utf8')){const c=ch.codePointAt(0);if((c<32&&c!==9&&c!==10&&c!==13)||(c>=127&&c<=159)||c===0xad||(c>=0x200b&&c<=0x200f)||(c>=0x2028&&c<=0x202e)||(c>=0x2060&&c<=0x206f)||c===0xfeff){bad++;console.log('RAW_CTRL',f,c.toString(16))}}}const pill=fs.readFileSync('src/ui/quickNpcPill.ts','utf8');console.log('INNERHTML_GATE',html,'CTRL_SCAN',bad,'PILL_PANEL',pill.includes('data-hud-panel'));process.exit(html||bad||!pill.includes('data-hud-panel')?1:0)"</automated>
  </verify>
  <acceptance_criteria>
    - Output contains TYPECHECK_OK, UNIT_OK, BUILD_OK, `SIZE_GATE_OK`, QUICK_OK, E2E_OK, `INNERHTML_GATE 0`, `CTRL_SCAN 0`, `PILL_PANEL true`; no Playwright line contains " failed"
    - `grep -c "farthestRouteIndex(" src/game/game.ts` and `grep -c "scheduleWriteRoster(" src/game/game.ts` each print at least 1
    - The SUMMARY records the bodies value at 15 and after removing to 0 (both 90 NPC ragdoll bodies)
  </acceptance_criteria>
  <done>A player adds or removes coworkers with +/− or the pill in play, newcomers appear away from them, and the choice is saved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Keyboard / pointer input → physics population | Rapid or scripted presses can try to thrash spawn/despawn |
| Browser shortcuts ↔ game keys | Ctrl/Cmd +/− must stay browser zoom |
| game → localStorage | Frequent changes must not hammer storage |
| Phase 1 gate → integration | D-12 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08-01 | Elevation (integration on an unmeasured stack) | Task 1 guard | mitigate | Guard tool first; PLAN_EXIT_* leaves src/tests untouched |
| T-02-08-02 | Denial of service (spawn thrash / unbounded bodies) | keyboard.ts, game quick path | mitigate | e.repeat ignored, npcDelta clamped ±15, bounds no-op at 0 and max, grow-only pool (e2e: 90 ragdoll bodies at 15 and after removing to 0) |
| T-02-08-03 | Denial of service (browser shortcuts swallowed) | keyboard.ts | mitigate | No preventDefault on npc keys; Ctrl/Meta/Alt combos classify as null; e2e records defaultPrevented false |
| T-02-08-04 | Tampering (typing drives the game) | keyboard.ts typing guard | mitigate | Existing isTypingTarget early return; e2e with a focused input |
| T-02-08-05 | Denial of service (storage churn) | scheduleWriteRoster | mitigate | 500 ms debounce, latest roster only, pagehide flush; e2e checks the saved count after 800 ms |
| T-02-08-06 | Tampering (bench integrity) | bench mode | mitigate | Pill hidden and deltas ignored with opts.bench; e2e checks 10 NPCs stay |
| T-02-08-07 | Elevation (XSS) | pill DOM | mitigate | createElement/textContent only; repo INNERHTML_GATE 0 |
| T-02-08-SC | Tampering | npm installs | accept | No package installed |
</threat_model>

<verification>
- Guard line recorded; on GUARD_CONTINUE: typecheck, unit, build, size and full Playwright green; INNERHTML_GATE 0; quickNpc.spec green in desktop and mobile-emu
</verification>

<success_criteria>
- ROADMAP Phase 2 success criterion 1 holds in the browser: +/− on desktop and the "− N +" pill on mobile change the office between 0 and 15 without opening settings (NPC-01)
</success_criteria>

## Artifacts this phase produces

- DOM: `#npc-pill[data-hud-panel]` with `#npc-pill-dec`, `#npc-pill-value`, `#npc-pill-inc`
- Symbols: `InputState.npcDelta`, `consumeNpcDelta`, `QuickNpcPill`, `createQuickNpcPill`; KEY_HINTS row `{ keys: '+/−', label: 'thêm/bớt NPC' }`
- Keys: Equal / NumpadAdd add, Minus / NumpadSubtract remove (no repeat, no modifiers, not while typing or paused)
- Roster source value `'quick'` in `__bt.roster.source`
- Tests: `tests/e2e/quickNpc.spec.ts`; updated `tests/unit/keyMap.test.ts`, `tests/e2e/keyHints.spec.ts`

<output>
Create `.planning/phases/02-npc-dong-nghiep/02-08-SUMMARY.md` when done
</output>
