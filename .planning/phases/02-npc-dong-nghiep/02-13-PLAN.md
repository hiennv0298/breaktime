---
phase: 02-npc-dong-nghiep
plan: 13
type: execute
wave: 10
depends_on: ["02-06", "02-07", "02-08", "02-09", "02-10", "02-11", "02-12"]
files_modified:
  - .planning/STATE.md
  - .planning/phases/02-npc-dong-nghiep/02-DEVICE-CHECK.md
autonomous: true
requirements: [NPC-01, NPC-02, NPC-03, NPC-04, NPC-05, NPC-06]
must_haves:
  truths:
    - "The plan starts with the D-12 entry guard; unless it prints GUARD_CONTINUE nothing is deployed and the SUMMARY is marked skipped (D-12)"
    - "The Phase 2 build is live at https://game.doibung.com through the existing one-command deploy only: DEPLOY_OK for HEAD, poller non200=0, doibung.com answering 200 before and after, and no infra:apply or other server action (Phase 1 D-03, PROJECT hạ tầng deploy)"
    - "The operator has one checklist file with the exact per-sha URLs for `?bench=1` (10 NPCs, comparable with 01-19), `?bench=1&brawl=1` (15 NPCs fighting) and normal play, tables for both reference phones, and a clearly labelled planner proposal (not a locked decision): D-01 only requires the bench on both phones to confirm ≥ 30 fps at 15 NPCs; the fallback 'if the brawl run on the Android reference phone averages below 30 fps, lower the cap to 10' comes from RESEARCH G1 and waits for the operator's decision recorded as CAP_DECISION (D-01, D-11, Phase 1 D-24)"
    - "The checklist covers every manual-only behaviour of Phase 2: +/− and the pill, roster editing on a phone with random names and the warning line, name-tag switch, anger by temper, 0.6 s telegraph readability, dodge by walking on touch, counter-slap, knockdown feel with no blood look, flash not red, ≤ ~3 s lock and 1.5 s protection (NPC-01..NPC-06 manual rows of 02-VALIDATION.md)"
  artifacts:
    - path: ".planning/phases/02-npc-dong-nghiep/02-DEVICE-CHECK.md"
      provides: "Live sha, gate URLs, device result tables, manual checklist, cap proposal pending operator decision, verdict key"
      contains: "NPC_DEVICE_VERDICT="
  key_links:
    - from: "npm run deploy"
      to: "https://game.doibung.com/b/<sha>/?bench=1&brawl=1"
      via: "DEPLOY_OK <sha> + independent GET checks"
      pattern: "DEPLOY_OK"
---

## Phase Goal

ROADMAP Phase 2 goal (verbatim): "Văn phòng có đồng nghiệp mang tên do người chơi đặt, số lượng thêm/bớt tuỳ ý tới 15, và bị tát đủ nhiều thì nổi giận đuổi theo đánh trả kiểu slapstick — đánh qua lại vẫn buồn cười, không máu, không thanh HP, không tụt fps".

## Execution Rule (conditional plan, D-12)

1. Run Task 1 (entry guard + full local suite) first. The guard prints exactly one of `GUARD_CONTINUE`, `PLAN_EXIT_GATE_PENDING` or `PLAN_EXIT_STACK_STOP` (each followed by `plan=02-13 reason=…`).
2. On `PLAN_EXIT_GATE_PENDING` or `PLAN_EXIT_STACK_STOP`: **stop immediately.** Do not deploy. SUMMARY `status: skipped` with the guard line, then end.
3. Only after `GUARD_CONTINUE` and a green local suite does Task 2 deploy.

<objective>
Put the finished Phase 2 build on game.doibung.com with the existing safe deploy and hand the operator an exact device checklist for the parts headless tests cannot judge (fps at 15 NPCs on phones, fight feel, touch dodge, roster typing on phones).

Purpose: ROADMAP Phase 2 success criteria 1–5 need real-device evidence (Phase 1 D-24: fps is never measured headless); D-01 cap confirmation; D-11 comparable bench.
Output: live build (DEPLOY_OK), 02-DEVICE-CHECK.md, STATE.md todo for the device run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-npc-dong-nghiep/02-CONTEXT.md
@.planning/phases/02-npc-dong-nghiep/02-VALIDATION.md
@.planning/phases/02-npc-dong-nghiep/02-12-SUMMARY.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-18-SUMMARY.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-GO-LIVE.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-DEVICE-LOG.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-GATE.md
@.planning/STATE.md

<local_traps>
1. Write every multi-command verify into a `.sh` file in the session scratchpad and run `bash <file>.sh`; inline Bash with `set -e` has produced false GREEN here. Trust printed markers (DEPLOY_OK, non200=0), not only the exit code.
2. `grep -P` fails silently here: use `grep -E` or Node.
3. PATH can drop mid-session (git, ssh may vanish): if `git` or `ssh` disappears, stop and report instead of improvising another transport.
4. `npm run deploy` refuses a dirty tree: commit the previous plans' SUMMARY files first; do not stash or delete untracked files you did not create.
5. Never run `npm run infra:apply`, `infra:rollback`, or any ssh command by hand; never edit d:/whattoeat. A drift report means stop and hand over (deploy/HANDOFF.md).
</local_traps>

<interfaces>
Deploy contract (01-07 / 01-12 / 01-18, unchanged):
- `npm run deploy` runs build → size gate → test gates → upload → activation → smoke on game.doibung.com and doibung.com with a background poller; prints `DEPLOY_OK <sha12>` on success, a poller line with `count=<n> non200=<n> maxConsecutiveNon200Ms=<n>`, `SITE_FILE_UNCHANGED` when the Caddy site file did not change (no reload), and `__ABORTED__ <sha>` when it stopped before activation.
- Per-commit builds are served at `https://game.doibung.com/b/<sha12>/`; `https://game.doibung.com/version.json` holds the live sha.
- 01-18 needed three attempts (SSH reset at upload, then a test flake that the operator approved fixing). Transport resets before activation are safe to retry; test-gate failures are not a retry case.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Entry guard and full local suite on HEAD (D-12)</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - This plan's "Execution Rule" section; .planning/phases/02-npc-dong-nghiep/02-CONTEXT.md decision D-12; scripts/lib/phaseGate.mjs header comment and scripts/phase-gate-guard.mjs (guard contract built by plan 02-03)
    - .planning/phases/02-npc-dong-nghiep/02-06..02-12 SUMMARY files (every one must say complete, not skipped; if any is skipped, stop with PLAN_EXIT_GATE_PENDING semantics and report)
  </read_first>
  <action>Implements D-12 before anything goes live. Run `node scripts/phase-gate-guard.mjs --plan 02-13` and copy its line into the SUMMARY; PLAN_EXIT_* → stop per the Execution Rule. On GUARD_CONTINUE confirm that the SUMMARY files 02-06 through 02-12 exist and none has `status: skipped`, then run the full local suite on a clean HEAD: `npm run typecheck`, `npx vitest run`, `npm run build`, `npm run size`, `npx playwright test`. Any failure: stop and report the failing test names (do not edit tests or weaken assertions in this plan).</action>
  <verify>
    <automated>node scripts/phase-gate-guard.mjs --plan 02-13 --dry-run && node -e "const fs=require('fs');const d='.planning/phases/02-npc-dong-nghiep/';const ids=['06','07','08','09','10','11','12'];const miss=ids.filter(i=>!fs.existsSync(d+'02-'+i+'-SUMMARY.md'));const skipped=ids.filter(i=>fs.existsSync(d+'02-'+i+'-SUMMARY.md')&&/status:\s*skipped/.test(fs.readFileSync(d+'02-'+i+'-SUMMARY.md','utf8')));console.log('SUMMARIES_MISSING',miss.join(',')||'none','SKIPPED',skipped.join(',')||'none');process.exit(miss.length||skipped.length?1:0)" && npm run typecheck && echo TYPECHECK_OK && npx vitest run && echo UNIT_OK && npm run build && echo BUILD_OK && npm run size && npx playwright test && echo E2E_OK</automated>
  </verify>
  <acceptance_criteria>
    - Guard line recorded; on GUARD_CONTINUE the output contains `SUMMARIES_MISSING none SKIPPED none`, TYPECHECK_OK, UNIT_OK, BUILD_OK, `SIZE_GATE_OK` and E2E_OK with no Playwright line containing " failed"
    - On PLAN_EXIT_*: nothing was deployed and the SUMMARY says `status: skipped`
  </acceptance_criteria>
  <done>Either a clean recorded exit, or a guarded HEAD whose whole suite is green and ready to deploy.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy through npm run deploy, verify live independently, write the device checklist</name>
  <files>.planning/phases/02-npc-dong-nghiep/02-DEVICE-CHECK.md, .planning/STATE.md</files>
  <read_first>
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-18-SUMMARY.md "Live measurement build" and "Deviations" (retry rules), .planning/phases/01-spike-k-thu-t-ng-deploy/01-GO-LIVE.md (gate URL format)
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-DEVICE-LOG.md (reference device names and table style) and 01-GATE.md (Phase 1 bench row to compare with)
    - .planning/phases/02-npc-dong-nghiep/02-VALIDATION.md "Manual-Only Verifications", .planning/phases/02-npc-dong-nghiep/02-CONTEXT.md D-01..D-11
    - .planning/phases/02-npc-dong-nghiep/02-12-SUMMARY.md (headless MEASURE brawl line)
  </read_first>
  <action>Implements the D-01 / D-11 device confirmation hand-off with the Phase 1 deploy rules. (1) Before deploying, GET https://doibung.com/ with Node fetch and record the status (must be 200; otherwise stop). (2) Run `npm run deploy` once. Success requires `DEPLOY_OK <sha12>` with sha12 equal to `git rev-parse --short=12 HEAD` and a poller line with `non200=0`. If it prints `__ABORTED__` with an SSH/transport error before activation, rerun it, at most two more times, and record each attempt. If it fails in the build, size or test gate, or reports Caddy/state drift, stop, record the output and end the task without retrying (no infra:apply, no manual ssh). (3) Independent checks with Node fetch after DEPLOY_OK: https://game.doibung.com/version.json sha equals HEAD; https://doibung.com/ 200; https://game.doibung.com/b/<sha12>/ 200; https://game.doibung.com/b/<sha12>/?bench=1 200; https://game.doibung.com/b/<sha12>/?bench=1&brawl=1 200. (4) Write .planning/phases/02-npc-dong-nghiep/02-DEVICE-CHECK.md in Vietnamese with: key lines `DEPLOY_SHA=<sha12>`, `DEPLOY_POLLER=count=<n> non200=0`, `HEADLESS_BRAWL=<MEASURE brawl line from 02-12>`, `NPC_DEVICE_VERDICT=PENDING`, `CAP_DECISION=PENDING`; a URL section with the four exact /b/<sha12>/ links (play, `?bench=1`, `?bench=1&brawl=1`, `?fight=always&npcs=3`); a results table per reference phone (model/OS/browser from 01-DEVICE-LOG.md) with columns avg fps, 1% low, draw call peak, body peak, NPC, max pursuers, strikes, knockdowns, sim ms p99, throttled, screenshot file — one row for `?bench=1` (compare with the 01-19 row) and one for `?bench=1&brawl=1`; a section titled exactly `## Đề xuất của planner về trần NPC (chờ operator quyết)` that says in plain words this is NOT a locked decision — D-01 only requires ≥ 30 fps confirmed at 15 NPCs on both phones, the fallback comes from RESEARCH G1 — and states the proposal ("nếu brawl trung bình < 30 fps trên Android chuẩn → đề xuất CAP_DECISION=LOWER_TO_10, hạ NPC_CAP và MAX_NPCS về 10 bằng một plan sửa riêng; nếu ≥ 30 → đề xuất CAP_DECISION=KEEP_15; operator có thể chọn khác và ghi CAP_DECISION=OTHER:<ghi chú>"), with no code change implied by this plan; and a checklist with ok / not ok / ghi chú columns for: phím + / − và Numpad (desktop), cụm nút "− N +" trên cả hai điện thoại, thêm 3 đồng nghiệp trên mỗi điện thoại có dùng nút tên ngẫu nhiên và reload vẫn còn, dòng cảnh báo tên đọc được, công tắc hiện tên, NPC Nóng giận sau 1 cú / Thường 2 / Hiền 3, dấu "!" và giơ tay 0,6 s đọc kịp, né bằng joystick được, tát lúc NPC giơ tay thì cắt đòn, ngã ragdoll buồn cười không trông như đau hay máu, viền loé không đỏ, khoá điều khiển ≤ ~3 s và bất tử 1,5 s cảm nhận được, NPC 11–15 không kẹt tường sau 2 phút, tối đa 3 NPC đuổi khi tát nhiều người. (5) Add to STATE.md "### Pending Todos" the line `- [Phase 2] Operator đo 02-DEVICE-CHECK.md trên 2 máy chuẩn (bench 10 NPC + brawl 15 NPC, checklist cảm giác) rồi ghi NPC_DEVICE_VERDICT và CAP_DECISION`. Never write any secret or player data into these files.</action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/02-npc-dong-nghiep/02-DEVICE-CHECK.md';if(!fs.existsSync(p)){console.log('DEVICE_CHECK_MISSING');process.exit(1)}const s=fs.readFileSync(p,'utf8');const sha=(s.match(/^DEPLOY_SHA=([0-9a-f]{12})$/m)||[])[1];const keys=['DEPLOY_POLLER=','non200=0','HEADLESS_BRAWL=','NPC_DEVICE_VERDICT=PENDING','CAP_DECISION=PENDING','?bench=1&brawl=1','LOWER_TO_10','KEEP_15','## Đề xuất của planner về trần NPC (chờ operator quyết)'];const miss=keys.filter(k=>!s.includes(k));const urls=sha?['/b/'+sha+'/?bench=1','/b/'+sha+'/?bench=1&brawl=1','/b/'+sha+'/?fight=always&npcs=3'].filter(u=>!s.includes(u)):['sha'];console.log('DEVICE_CHECK sha',sha||'none','missing',miss.join('|')||'none','urls_missing',urls.join('|')||'none');process.exit(sha&&!miss.length&&!urls.length?0:1)" && node -e "(async()=>{const {execSync}=require('child_process');const head=execSync('git rev-parse --short=12 HEAD').toString().trim();const v=await (await fetch('https://game.doibung.com/version.json',{cache:'no-store'})).json();const d=(await fetch('https://doibung.com/')).status;const b=(await fetch('https://game.doibung.com/b/'+head+'/?bench=1&brawl=1')).status;const ok=String(v.sha).startsWith(head)&&d===200&&b===200;console.log('LIVE_CHECK head',head,'live',v.sha,'doibung',d,'brawl',b,ok?'LIVE_OK':'LIVE_BAD');process.exit(ok?0:1)})().catch(e=>{console.log('LIVE_CHECK_ERROR',e.message);process.exit(1)})"</automated>
    <human-check>End of phase (human_verify_mode end-of-phase): on the Android and iPhone reference devices open the three URLs in 02-DEVICE-CHECK.md, run `?bench=1` and `?bench=1&brawl=1` to the results screen and screenshot them, walk through the checklist, then fill the tables and NPC_DEVICE_VERDICT (PASS / FAIL), and decide CAP_DECISION (KEEP_15 / LOWER_TO_10 / OTHER:<note>) — the planner's proposal is only a suggestion — before /gsd-verify-work.</human-check>
  </verify>
  <acceptance_criteria>
    - The deploy output contains `DEPLOY_OK <sha12>` equal to HEAD and a poller line with `non200=0` (quote both in the SUMMARY, with every attempt if retries happened)
    - The verify prints `DEVICE_CHECK sha <sha12> missing none urls_missing none` and `LIVE_OK`
    - STATE.md contains the Pending Todos line starting `- [Phase 2] Operator đo 02-DEVICE-CHECK.md`
    - No command other than `npm run deploy` touched the server (SUMMARY lists every command run)
  </acceptance_criteria>
  <done>The Phase 2 build is live without disturbing doibung.com, and the operator has exact URLs, tables and a checklist to confirm the 15-NPC cap and the fight-back feel on both phones.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local machine → shared VPS (ssh doibung) | Deploy crosses into a server that also serves doibung.com |
| Public internet → game.doibung.com | Anyone can open the build, including debug flags |
| Planning docs → git | Checklist must not carry secrets or personal data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13-01 | Elevation (shipping an unmeasured stack) | Task 1 guard + suite | mitigate | Guard tool first; all integration SUMMARY files complete; full suite green before deploy |
| T-02-13-02 | Denial of service (doibung.com outage during deploy) | npm run deploy | mitigate | Existing pipeline with poller (non200 must be 0), smoke on both sites, SITE_FILE_UNCHANGED avoids Caddy reload; independent doibung.com 200 check before and after |
| T-02-13-03 | Tampering (server config drift / improvised server actions) | deploy | mitigate | Only `npm run deploy`; drift → stop and hand over; no infra:apply, no manual ssh, no edits to d:/whattoeat |
| T-02-13-04 | Tampering | public test flags (`?fight=always`, `?bench=1&brawl=1`) | accept | Single-player, literal-only flags; no data or other players affected |
| T-02-13-06 | Tampering (scope / unapproved decision) | cap proposal in 02-DEVICE-CHECK.md | mitigate | Labelled as a planner proposal pending operator decision (RESEARCH G1, not CONTEXT); CAP_DECISION starts PENDING; this plan changes no code and any cap change needs the operator's decision plus a separate plan |
| T-02-13-05 | Information disclosure | coworker names | mitigate | Names live only in each visitor's localStorage; the build sends none (e2e from 02-07/02-09); checklist contains no names or secrets |
| T-02-13-SC | Tampering | npm installs | accept | No package installed |
</threat_model>

<verification>
- Guard line recorded; on GUARD_CONTINUE: full local suite green, DEPLOY_OK for HEAD with non200=0, LIVE_OK, 02-DEVICE-CHECK.md keys and URLs present, STATE todo added
</verification>

<success_criteria>
- Every ROADMAP Phase 2 success criterion has either automated evidence (plans 02-01..02-12) or a ready device checklist on the live build (this plan), with the cap fallback written down as a planner proposal for the operator to decide
</success_criteria>

## Artifacts this phase produces

- Live build `https://game.doibung.com/b/<sha12>/` with `?bench=1`, `?bench=1&brawl=1`, `?fight=always&npcs=3`
- File: `.planning/phases/02-npc-dong-nghiep/02-DEVICE-CHECK.md` (keys `DEPLOY_SHA`, `DEPLOY_POLLER`, `HEADLESS_BRAWL`, `NPC_DEVICE_VERDICT`, `CAP_DECISION`; per-phone tables; checklist)
- STATE.md Pending Todo for the operator device run

<output>
Create `.planning/phases/02-npc-dong-nghiep/02-13-SUMMARY.md` when done
</output>
