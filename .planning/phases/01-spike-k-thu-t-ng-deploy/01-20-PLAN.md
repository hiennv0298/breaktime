---
phase: 01-spike-k-thu-t-ng-deploy
plan: 20
type: execute
wave: 16
depends_on: ["01-19", "01-23"]
files_modified:
  - src/logic/quality.ts
  - src/physics/shards.ts
  - src/physics/props.ts
  - src/render/room.ts
  - src/render/renderer.ts
  - tests/unit/quality.test.ts
  - .planning/phases/01-spike-k-thu-t-ng-deploy/01-OPTIMIZATION.md
  - .planning/phases/01-spike-k-thu-t-ng-deploy/01-DEVICE-LOG.md
autonomous: false
requirements: [TECH-03, TECH-04]
must_haves:
  truths:
    - "This plan is conditional: Task 1 is an entry guard, and unless 01-GATE.md says VERDICT=FAIL the executor ends the plan after Task 1, so the Task 3 checkpoint is never reached on the NOT_NEEDED or REMEASURE paths (D-07)"
    - "On VERDICT=PASS the plan writes 01-OPTIMIZATION.md with OPTIMIZATION=NOT_NEEDED and changes no source file (D-07)"
    - "On VERDICT=FAIL exactly one optimisation pass applies the five D-07 levers in order and nothing else: (1) lower render DPR caps, (2) remove any remaining dynamic shadows, (3) instancing / merging of static and repeated meshes (the per-character mesh merge is already done by plan 01-23 before the gate and is not redone), (4) lower debris caps and faster shard cleanup, (5) earlier body sleep (D-07)"
    - "The optimised build is deployed and re-measured on the same two reference devices with the same ?bench=1 scenario and seed (D-06, D-08)"
  artifacts:
    - path: ".planning/phases/01-spike-k-thu-t-ng-deploy/01-OPTIMIZATION.md"
      provides: "Guard outcome and, on FAIL, the levers applied with before numbers and new URLs"
      contains: "OPTIMIZATION="
  key_links:
    - from: "01-GATE.md VERDICT"
      to: "Task 1 entry guard"
      via: "only VERDICT=FAIL prints GUARD_CONTINUE"
      pattern: "GUARD_CONTINUE"
---

## Phase Goal

**As a** solo operator play-testing on my two reference phones, **I want to** open https://game.doibung.com, walk a low-poly office, slap NPCs into ragdolls and smash 20+ objects in a build that any commit can deploy with one command, **so that** I have measured proof that Three.js + Rapier meets the size, fps and iOS-stability budget before any gameplay phase builds on it.

## Execution Rule (conditional plan)

1. Run Task 1 (entry guard) first. It prints exactly one of `PLAN_EXIT_NOT_NEEDED`, `PLAN_EXIT_REMEASURE` or `GUARD_CONTINUE`.
2. If it printed `PLAN_EXIT_NOT_NEEDED` or `PLAN_EXIT_REMEASURE`: **stop executing this plan immediately.** Do not start Task 2, do not present the Task 3 checkpoint. Write the SUMMARY with `status: skipped` and the guard output, then end.
3. Only after `GUARD_CONTINUE` do Tasks 2 and 3 run, in order. Task 3 (human-action) is presented only when Task 2 finished with `OPTIMIZATION=APPLIED` and `DEPLOY_OK`.

<objective>
Conditional recovery pass, bounded by D-07: if and only if the real-device gate failed, apply the five pre-agreed levers once, redeploy, and have the operator re-measure on the same devices. The verdict and the stop/PlayCanvas decision are plan 01-21.

Purpose: the ROADMAP forbids continuing on a failing stack, and D-07 forbids open-ended tuning; a guard keeps the pass path free of human halts.
Output: 01-OPTIMIZATION.md; on FAIL also the lever changes, a redeploy and new measurement evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-CONTEXT.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-RESEARCH.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-GATE.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-DEVICE-LOG.md

<interfaces>
Knobs created by earlier plans (change values only; do not restructure modules):

```typescript
// src/logic/quality.ts (01-11)       TIERS high {dprCap 2, debrisCap 60, far 40} med {1.5, 40, 30} low {1, 20, 22}
// src/physics/shards.ts (01-16)      SHARD_LIFETIME_S = 2.5, SHARD_SHRINK_S = 0.3
// src/physics/props.ts (01-10)       SLEEP_LINEAR = 0.05, SLEEP_ANGULAR = 0.08, SLEEP_FRAMES = 60
// src/render/room.ts (01-10)         static furniture merged per material via mergeGeometries
// src/render/characters.ts (01-23)   ALREADY MERGED: one rigid-skinned mesh per character (1 draw); not a lever here
// src/render/renderer.ts (01-02/09/11) shadowMap.enabled = false, DPR from TIERS
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Entry guard — read the gate verdict and exit the plan unless VERDICT=FAIL</name>
  <files>.planning/phases/01-spike-k-thu-t-ng-deploy/01-OPTIMIZATION.md</files>
  <read_first>
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-GATE.md
    - The "Execution Rule" section of this plan
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-CONTEXT.md decision D-07
  </read_first>
  <action>Implements the D-07 entry condition. Read 01-GATE.md. If it contains `VERDICT=PASS`: write 01-OPTIMIZATION.md with `OPTIMIZATION=NOT_NEEDED`, `GATE_SHA=<SHA from 01-GATE.md>` and "Gate passed; no optimisation pass (D-07)", print `PLAN_EXIT_NOT_NEEDED`, and end the plan per the Execution Rule (no source change, no deploy, no checkpoint). If the file is missing or contains `VERDICT=REMEASURE`: write 01-OPTIMIZATION.md with `OPTIMIZATION=BLOCKED_REMEASURE` and the reason, print `PLAN_EXIT_REMEASURE`, and end the plan. Only when it contains `VERDICT=FAIL`: write 01-OPTIMIZATION.md with `OPTIMIZATION=IN_PROGRESS`, the FAILED list and the before numbers (Android avg / 1% low / knocked-or-broken, iOS soak result, first-load raw), then print `GUARD_CONTINUE`.</action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/01-spike-k-thu-t-ng-deploy/01-OPTIMIZATION.md','utf8');const m=s.match(/OPTIMIZATION=(NOT_NEEDED|BLOCKED_REMEASURE|IN_PROGRESS)/);console.log(m?(m[1]==='IN_PROGRESS'?'GUARD_CONTINUE':m[1]==='NOT_NEEDED'?'PLAN_EXIT_NOT_NEEDED':'PLAN_EXIT_REMEASURE'):'GUARD_MISSING');process.exit(m?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints exactly one of PLAN_EXIT_NOT_NEEDED, PLAN_EXIT_REMEASURE or GUARD_CONTINUE and exits 0
    - PLAN_EXIT_NOT_NEEDED appears only when 01-GATE.md contains VERDICT=PASS; GUARD_CONTINUE only when it contains VERDICT=FAIL
    - On either PLAN_EXIT path, `git status --porcelain src tests` is empty and the SUMMARY is written with status skipped (Tasks 2–3 not started)
  </acceptance_criteria>
  <done>The plan either ended cleanly without code changes or confirmed that the single D-07 pass must run.</done>
</task>

<task type="auto">
  <name>Task 2: Apply exactly the five D-07 levers, keep tests green, redeploy (runs only after GUARD_CONTINUE)</name>
  <files>src/logic/quality.ts, src/physics/shards.ts, src/physics/props.ts, src/render/room.ts, src/render/renderer.ts, tests/unit/quality.test.ts, .planning/phases/01-spike-k-thu-t-ng-deploy/01-OPTIMIZATION.md</files>
  <read_first>
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-OPTIMIZATION.md (must say OPTIMIZATION=IN_PROGRESS; otherwise do not run this task)
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-CONTEXT.md decision D-07 (the exact lever list)
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-RESEARCH.md "Performance budget", "Pattern 14" (sleep earlier), "Pattern 15"; .planning/phases/01-spike-k-thu-t-ng-deploy/01-23-SUMMARY.md (character merge already applied, measured draw calls)
    - src/logic/quality.ts, src/physics/shards.ts, src/physics/props.ts, src/render/room.ts, src/render/renderer.ts
  </read_first>
  <action>Implements D-07 levers, in order, nothing else; record each change and its commit in 01-OPTIMIZATION.md.
(1) DPR — TIERS dprCap high 2 → 1.5, med 1.5 → 1.0, low 1 → 0.75; update tests/unit/quality.test.ts expectations.
(2) Dynamic shadows — confirm renderer.shadowMap.enabled is false and remove any `castShadow = true` / `receiveShadow = true` or shadow-casting light in src; blob shadows stay.
(3) Instancing / static merge — verify all static furniture is merged per material (fix any mesh that escaped); convert repeated dynamic props with identical geometry (computerKeyboard, computerMouse, computerScreen, mug, chairDesk) to one InstancedMesh per role with instance matrices written from their bodies each frame. The Blocky character merge (one rigid SkinnedMesh per character, RESEARCH A6) was already applied by plan 01-23 before the gate: record it in 01-OPTIMIZATION.md as "already satisfied (01-23)" and do not touch src/render/characters.ts.
(4) Debris — TIERS debrisCap high 60 → 40, med 40 → 24, low 20 → 12; SHARD_LIFETIME_S 2.5 → 1.5; SHARD_SHRINK_S 0.3 → 0.2.
(5) Sleep earlier — SLEEP_LINEAR 0.05 → 0.12, SLEEP_ANGULAR 0.08 → 0.2, SLEEP_FRAMES 60 → 15.
Run `npm run typecheck && npx vitest run && npx vite build && npx playwright test`; fix only regressions caused by these levers. Commit, run `npm run deploy` (require DEPLOY_OK; on drift stop and report — never run infra:apply), then set `OPTIMIZATION=APPLIED` in 01-OPTIMIZATION.md with the new sha and the exact URLs `https://game.doibung.com/b/<sha>/?bench=1` and `…/?soak=1`.</action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/01-spike-k-thu-t-ng-deploy/01-OPTIMIZATION.md','utf8');const ok=s.includes('OPTIMIZATION=APPLIED')&&/b\/[0-9a-f]{12}\/\?bench=1/.test(s);console.log(ok?'LEVERS_APPLIED':'LEVERS_MISSING');process.exit(ok?0:1)" && npm run typecheck && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints LEVERS_APPLIED and type-check and unit tests pass
    - src/logic/quality.ts contains "dprCap: 1.5", "dprCap: 0.75", "debrisCap: 24", "debrisCap: 12"; src/physics/shards.ts contains "1.5" and "0.2"; src/physics/props.ts contains "SLEEP_FRAMES = 15"
    - `grep -rn "castShadow = true\|receiveShadow = true" src | wc -l` prints 0
    - The deploy output contains DEPLOY_OK and the full Playwright suite passed before it
    - 01-OPTIMIZATION.md lists the five levers in order, each "applied" or "already satisfied", lever (3) names the character merge as "already satisfied (01-23)", and no other change
    - `git diff --name-only <gate sha>..HEAD -- src/render/characters.ts | wc -l` prints 0
  </acceptance_criteria>
  <done>Exactly one D-07 pass is implemented, tested and deployed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Operator re-measures the optimised build on the same two devices (reached only after Task 2 APPLIED)</name>
  <files>.planning/phases/01-spike-k-thu-t-ng-deploy/01-DEVICE-LOG.md</files>
  <read_first>
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-OPTIMIZATION.md (must say OPTIMIZATION=APPLIED; new sha and URLs)
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-19-PLAN.md Task 2 protocol
  </read_first>
  <action>Implements the D-06/D-08 re-measurement. Precondition (checked before presenting the checkpoint): 01-OPTIMIZATION.md contains OPTIMIZATION=APPLIED; if not, this task must not be presented (the Execution Rule already ended the plan). Print the new bench and soak URLs and the protocol below. After the operator resumes, list the new -opt- screenshots with sizes and copy the operator's notes into 01-DEVICE-LOG.md "## Measurement sessions" as unfilled rows marked "after D-07" (plan 01-21 transcribes the numbers).</action>
  <instructions>
    The optimised build is deployed (URLs above). Same two reference devices, Low Power Mode / Battery Saver OFF, same Wi-Fi.
    1. Android Chrome: `…/b/&lt;new sha&gt;/?bench=1`, tap Chơi, wait 60 s → save evidence/android-bench-opt-&lt;sha&gt;.png.
    2. iPhone Safari: same → evidence/iphone-bench-opt-&lt;sha&gt;.png.
    3. If the iOS soak failed before: `…/b/&lt;new sha&gt;/?soak=1` for 15 min, then 15 min of play → evidence/iphone-soak-opt-&lt;sha&gt;.png; reload and report any crash banner or self-reload.
    Copy the screenshots into `.planning/phases/01-spike-k-thu-t-ng-deploy/evidence/`.
  </instructions>
  <verification>The -opt- screenshots exist in evidence/ and are larger than 10 KB</verification>
  <resume-signal>Type "re-measured" plus crash notes</resume-signal>
  <verify>
    <automated>node -e "const fs=require('fs');const f=fs.readdirSync('.planning/phases/01-spike-k-thu-t-ng-deploy/evidence');const ok=['android-bench-opt-','iphone-bench-opt-'].every(p=>f.some(x=>x.startsWith(p)&&fs.statSync('.planning/phases/01-spike-k-thu-t-ng-deploy/evidence/'+x).size>10000));console.log(ok?'REMEASURE_EVIDENCE_OK':'REMEASURE_EVIDENCE_MISSING');process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints REMEASURE_EVIDENCE_OK
    - 01-DEVICE-LOG.md contains new rows marked "after D-07" referencing the -opt- screenshots
  </acceptance_criteria>
  <done>Post-optimisation evidence from the same devices is in the repository.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local machine → VPS deploy | Redeploy of the optimised build |
| operator devices → evidence | Second measurement round |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-20-01 | Denial of service | doibung.com during redeploy | mitigate | Standard `npm run deploy` only (drift check, poller non200 = 0, dual-domain smoke); no infra:apply allowed |
| T-01-20-02 | Tampering | scope creep beyond D-07 | mitigate | Five enumerated levers with fixed values; 01-OPTIMIZATION.md must list only those |
| T-01-20-03 | Repudiation | unnecessary human halts or code changes on a passing gate | mitigate | Entry guard with explicit Execution Rule; acceptance requires no src/tests changes on exit paths |
</threat_model>

<verification>
- 01-OPTIMIZATION.md exists with NOT_NEEDED, BLOCKED_REMEASURE or APPLIED; on APPLIED, re-measure evidence exists
</verification>

<success_criteria>
- D-07 honoured: no halt and no change when the gate passed; exactly one bounded pass when it failed
</success_criteria>

## Artifacts this phase produces

- File: `01-OPTIMIZATION.md` (key `OPTIMIZATION=NOT_NEEDED|BLOCKED_REMEASURE|IN_PROGRESS|APPLIED`, `GATE_SHA`)
- Guard markers: `PLAN_EXIT_NOT_NEEDED`, `PLAN_EXIT_REMEASURE`, `GUARD_CONTINUE`, `LEVERS_APPLIED`, `REMEASURE_EVIDENCE_OK`
- Tuned constants on the APPLIED path: `TIERS` dprCap 1.5/1.0/0.75 and debrisCap 40/24/12, `SHARD_LIFETIME_S` 1.5, `SHARD_SHRINK_S` 0.2, `SLEEP_LINEAR` 0.12, `SLEEP_ANGULAR` 0.2, `SLEEP_FRAMES` 15; per-role InstancedMesh for repeated props (character mesh merge is not part of this plan: done in 01-23)
- Evidence files: `evidence/*-opt-<sha>.png`

<output>
Create `.planning/phases/01-spike-k-thu-t-ng-deploy/01-20-SUMMARY.md` when done (status skipped on either PLAN_EXIT path)
</output>
