---
phase: 01-spike-k-thu-t-ng-deploy
plan: 19
type: execute
wave: 15
depends_on: ["01-18"]
files_modified:
  - .planning/phases/01-spike-k-thu-t-ng-deploy/01-DEVICE-LOG.md
  - .planning/phases/01-spike-k-thu-t-ng-deploy/01-GATE.md
  - .planning/phases/01-spike-k-thu-t-ng-deploy/evidence/README.md
  - .planning/STATE.md
autonomous: false
requirements: [TECH-01, TECH-03, TECH-04, CTRL-02]
must_haves:
  truths:
    - "Before any measurement, 01-DEVICE-LOG.md records exactly one mid-range Android and one iPhone (model, OS version, browser and version, Low Power Mode / Battery Saver state, display refresh rate), and these two devices are declared the reference devices for every later phase (D-06)"
    - "The operator ran ?bench=1 (10 NPCs ragdolling at once, D-11 revised) on both phones and on one desktop browser against the deployed measurement sha with Low Power Mode / Battery Saver off, and saved the result screenshots in evidence/ (D-08)"
    - "The operator ran ?soak=1 for 15 minutes then played 15 minutes on the iPhone in Safari (Auto-Lock Never, Low Power Mode off), and reopening the page showed no crash banner (TECH-04)"
    - "The operator confirmed the game opens and is playable in Chrome, Edge and Firefox on desktop, Chrome on Android and Safari on iOS, and noted joystick/context-button feel on both phones (TECH-01, CTRL-02)"
    - "On desktop and on both phones the operator checked the 15/09/2026 controls and settings — key map (arrows/WASD move, Space swings and hits, E still works, lone Ctrl and Esc open settings, Space never pauses, Z/C rotate, arrows never rotate), the key hint panel (bottom-left, fades to ~30%, full on hover, toggle survives reload; on phones translucent buttons and a one-time hint), NPC count 0–10 with names above heads that survive reload, and the swing animation on every press with or without a target — and the answers are in 01-DEVICE-LOG.md (D-27, D-28, D-29, D-30)"
    - "01-GATE.md states VERDICT=PASS only when the Android bench row shows avg fps ≥ 30 (not throttled) with NPC 10 AND at least 20 objects knocked or broken in that same run (ROADMAP success criterion 2), iOS soak + play had no crash or reload, and first load ≤ 20 MB (with a reason if > 8 MB); otherwise VERDICT=FAIL naming each failed criterion (D-07, ROADMAP gate). Controls/settings findings go to gaps and never trigger D-07 by themselves"
  artifacts:
    - path: ".planning/phases/01-spike-k-thu-t-ng-deploy/01-DEVICE-LOG.md"
      provides: "Reference device records, measurement sessions and the controls/settings checklist"
      contains: "## Controls & settings checklist"
    - path: ".planning/phases/01-spike-k-thu-t-ng-deploy/01-GATE.md"
      provides: "Gate verdict with per-criterion evidence"
      contains: "VERDICT="
  key_links:
    - from: "01-GATE.md"
      to: "evidence/*.png"
      via: "numbers transcribed from bench screenshots"
      pattern: "evidence/"
---

## Phase Goal

**As a** solo operator play-testing on my two reference phones, **I want to** open https://game.doibung.com, walk a low-poly office, slap NPCs into ragdolls and smash 20+ objects in a build that any commit can deploy with one command, **so that** I have measured proof that Three.js + Rapier meets the size, fps and iOS-stability budget before any gameplay phase builds on it.

<objective>
Real-device gate: record the two reference devices, have the operator measure the deployed build on them, check the new controls and settings by hand, and write a verdict against the ROADMAP gate. This is the evidence the phase is accepted on; "chạy mượt" in words is not accepted.

Purpose: TECH-03/TECH-04 can only be proven on real hardware (D-24); D-06 fixes the devices for all later phases; D-07 decides what happens on failure; the 15/09/2026 decisions D-27..D-30 need a feel check on real screens.
Output: 01-DEVICE-LOG.md, screenshots in evidence/, 01-GATE.md with VERDICT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-CONTEXT.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-RESEARCH.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-VALIDATION.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-GO-LIVE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator records the two reference devices before any measurement</name>
  <files>.planning/phases/01-spike-k-thu-t-ng-deploy/01-DEVICE-LOG.md</files>
  <read_first>
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-CONTEXT.md decision D-06
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-RESEARCH.md "Open Questions" 3, "Pitfall 3"
  </read_first>
  <action>Implements D-06. Before halting create 01-DEVICE-LOG.md with: a "## Reference devices" table (columns: Role, Model, OS version, Browser + version, Low Power Mode / Battery Saver default, Display refresh rate, RAM if known, Recorded at) with two empty rows "Android" and "iPhone"; a "## Measurement sessions" table (Date, Sha, Device, URL, Tier (source), Low Power, Throttled flag, NPC, Avg fps, 1% low, Peak draws, Peak bodies, Knocked/broken, Ragdolls at once, Flavor, Screenshot); a "## Browser checklist" table (Chrome desktop, Edge desktop, Firefox desktop, Chrome Android, Safari iOS × Loads / Playable / Notes); and a "## Controls & settings checklist" table with columns Check, Desktop Chrome, Android Chrome, iPhone Safari, Notes and these rows (desktop-only rows get "n/a" in the phone columns, phone-only rows "n/a" on desktop): "Arrows and WASD move (D-27)", "Space swings and hits, E also works (D-27, D-30)", "Lone Ctrl opens/closes settings, Ctrl+R/Ctrl+W still work (D-27)", "Esc opens/closes settings (D-27)", "Space never pauses (D-27)", "Z/C rotate, arrows never rotate (D-19)", "Key hint panel bottom-left, fades to ~30%, full on hover (D-28)", "Key hint toggle in settings survives reload (D-28)", "Virtual buttons translucent, one-time hint not over the joystick or buttons (D-28)", "Swing plays on every press/tap with or without a target (D-30)", "NPC count 0–10 applies at once from settings (D-29)", "NPC names ≤ 16 chars show above heads and survive reload (D-29)", "Typing a name does not move/rotate/pause the game; phone keyboard does not zoom (D-29)". After the operator replies, fill the Reference devices rows exactly as given. Then check baselines: iOS must be ≥ 16.4 and Chrome ≥ 111 (Vite 8 target). If either is lower, record "BELOW_BASELINE" in the log, add a STATE.md blocker, and stop the plan instead of measuring.</action>
  <what-built>Empty device log template in the phase directory.</what-built>
  <how-to-verify>
    Send, for each phone (one mid-range Android and one iPhone you will keep using for all phases):
    1. Model name (Android: Settings → About phone; iPhone: Settings → General → About → Model Name).
    2. OS version (Android version; iOS version).
    3. Browser and version (Android: chrome://version; iPhone: Safari follows the iOS version).
    4. Whether Low Power Mode (iPhone) / Battery Saver (Android) is normally on.
    5. Screen refresh rate if known (60 / 90 / 120 Hz).
  </how-to-verify>
  <resume-signal>Paste the five items for the Android phone and for the iPhone</resume-signal>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/01-spike-k-thu-t-ng-deploy/01-DEVICE-LOG.md','utf8');const sec=s.split('## Reference devices')[1]||'';const rows=sec.split('\n').filter(l=>/^\|\s*(Android|iPhone)\s*\|/.test(l));const filled=rows.filter(l=>l.split('|').slice(2,5).every(c=>c.trim().length>0));const ck=s.includes('## Controls & settings checklist');console.log('DEVICE_ROWS',rows.length,'FILLED',filled.length,'CHECKLIST',ck);process.exit(rows.length===2&&filled.length===2&&ck?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `DEVICE_ROWS 2 FILLED 2 CHECKLIST true` and exits 0
    - 01-DEVICE-LOG.md contains "## Measurement sessions", "## Browser checklist" and "## Controls & settings checklist" (13 check rows) and no measurement row has been filled yet
  </acceptance_criteria>
  <done>The two reference devices are on record before any measurement, and the checklist template is ready.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator measures on real devices (bench on Android, iPhone, desktop; 15-min iOS soak + play; browser and controls checklists)</name>
  <files>.planning/phases/01-spike-k-thu-t-ng-deploy/01-DEVICE-LOG.md, .planning/phases/01-spike-k-thu-t-ng-deploy/evidence/README.md</files>
  <read_first>
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-GO-LIVE.md section "## Measurement build" (exact URLs and sha)
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-VALIDATION.md "Manual-Only Verifications"
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-CONTEXT.md decisions D-07, D-08, D-27, D-28, D-29, D-30
  </read_first>
  <action>Implements the D-08 measurement protocol and the D-27..D-30 feel check. Before halting, create evidence/README.md listing the expected file names (android-bench-<sha>.png, iphone-bench-<sha>.png, desktop-bench-<sha>.png, iphone-soak-<sha>.png, optional iphone-after-reload-<sha>.png) and print the exact URLs from 01-GO-LIVE.md "## Measurement build" plus the checklist below. After the operator resumes, confirm the screenshot files exist in evidence/ (list them with sizes) and copy the operator's notes (feel, browser checklist answers, controls & settings checklist answers, any crash or reload) into 01-DEVICE-LOG.md. Do not interpret numbers yet (Task 3 does).</action>
  <what-built>The measurement build (bench + soak, 10-NPC bench, new key map, key hint panel, NPC settings, swing) is live; exact URLs are in 01-GO-LIVE.md "## Measurement build".</what-built>
  <how-to-verify>
    Before every run: Low Power Mode / Battery Saver OFF, phone charged above 50%, no other heavy apps, same Wi-Fi.
    1. Android (Chrome): open `…/b/<sha>/?bench=1`, tap Chơi, wait 60 s, screenshot the results (the "NPC" row must say 10) → save as evidence/android-bench-<sha>.png.
    2. iPhone (Safari): same URL, same steps → evidence/iphone-bench-<sha>.png.
    3. Desktop (Chrome): same URL → evidence/desktop-bench-<sha>.png (note the monitor refresh rate).
    4. iPhone (Safari), Settings → Display → Auto-Lock → Never: open `…/b/<sha>/?soak=1`, tap Chơi, leave 15 minutes, screenshot the soak panel → evidence/iphone-soak-<sha>.png; then open `…/b/<sha>/` and play normally for 15 more minutes (slap, smash, rotate, open settings).
    5. Reload `…/b/<sha>/` on the iPhone: report whether the red "Lần chơi trước bị dừng đột ngột" banner appears, and whether Safari ever reloaded the tab by itself during steps 4–5.
    6. Open `…/b/<sha>/` in Chrome, Edge and Firefox on desktop, Chrome on Android, Safari on iPhone: for each say "loads / playable / notes".
    7. On both phones play 2 minutes and describe joystick and context-button feel (anything awkward, mis-taps, zoom or scroll glitches).
    8. Controls & settings (answer ok / not ok + note for each row of the checklist in 01-DEVICE-LOG.md):
       a. Desktop `…/b/<sha>/`: walk with arrows, then WASD; press Space next to nothing (arm swings), next to a coworker (slap) and next to a box (push); press E once; tap Ctrl alone (settings open), tap again (close); press Esc (open/close); press Space and confirm it never pauses; press Z / C (rotate) and arrows (no rotation); watch the bottom-left key panel fade after a few seconds and come back under the mouse; turn the panel off in settings, reload, confirm it stays off, turn it back on.
       b. Desktop settings: set NPC to 10, name two coworkers (one with Vietnamese accents), Áp dụng, confirm ten coworkers and the names above heads; reload and confirm they are kept; set 0 then 3.
       c. Each phone `…/b/<sha>/` in a fresh tab: the short hint line appears once and does not cover the joystick side or buttons; the buttons look semi-transparent; tap the round button with nothing nearby (arm swings); open ⏸ → settings, change the NPC count, tap a name field and type a name (the page must not zoom, the field must not be hidden by the keyboard), Áp dụng, Tiếp tục, confirm the name tag; reload and confirm the hint does not show again.
    Copy the screenshots into `.planning/phases/01-spike-k-thu-t-ng-deploy/evidence/` on this machine.
  </how-to-verify>
  <resume-signal>Type "measured" plus your notes for steps 5–8 (and "no crash" / "crash at mm:ss")</resume-signal>
  <verify>
    <automated>node -e "const fs=require('fs');const d='.planning/phases/01-spike-k-thu-t-ng-deploy/evidence';const f=fs.readdirSync(d);const need=['android-bench-','iphone-bench-','desktop-bench-','iphone-soak-'];const miss=need.filter(p=>!f.some(x=>x.startsWith(p)&&/\.(png|jpe?g)$/i.test(x)&&fs.statSync(d+'/'+x).size>10000));const log=fs.readFileSync('.planning/phases/01-spike-k-thu-t-ng-deploy/01-DEVICE-LOG.md','utf8');const sec=(log.split('## Controls & settings checklist')[1]||'').split('\n## ')[0];const rows=sec.split('\n').filter(l=>/^\|/.test(l)&&!/^\|\s*(Check|-)/.test(l));const empty=rows.filter(l=>l.split('|').slice(2,5).some(c=>c.trim().length===0));console.log('EVIDENCE_MISSING',miss.join(',')||'none','CHECK_ROWS',rows.length,'CHECK_EMPTY',empty.length);process.exit(miss.length||rows.length<13||empty.length?1:0)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `EVIDENCE_MISSING none`, `CHECK_ROWS` ≥ 13 and `CHECK_EMPTY 0`, and exits 0
    - 01-DEVICE-LOG.md "## Browser checklist" has an answer in every row and contains the operator's soak/crash note and feel notes
  </acceptance_criteria>
  <done>Screenshots, browser/feel notes and the controls & settings answers from the real devices are in the repository.</done>
</task>

<task type="auto">
  <name>Task 3: Transcribe measurements and write the gate verdict</name>
  <files>.planning/phases/01-spike-k-thu-t-ng-deploy/01-DEVICE-LOG.md, .planning/phases/01-spike-k-thu-t-ng-deploy/01-GATE.md, .planning/STATE.md</files>
  <read_first>
    - every image in .planning/phases/01-spike-k-thu-t-ng-deploy/evidence/ (open each with the Read tool)
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-DEVICE-LOG.md, 01-GO-LIVE.md (first-load total, sha)
    - .planning/ROADMAP.md Phase 1 success criteria and gate paragraph; .planning/phases/01-spike-k-thu-t-ng-deploy/01-CONTEXT.md decisions D-06, D-07, D-08, D-11 (revised), D-21, D-27..D-30
  </read_first>
  <action>Implements the D-07 gate decision. Read every screenshot and transcribe into 01-DEVICE-LOG.md "## Measurement sessions" one row per bench/soak screenshot: sha shown on screen (must equal the measurement sha, otherwise mark the row INVALID_SHA), tier and source, throttled flag, NPC count, avg fps, 1% low, peak draw calls, peak bodies, knocked-or-broken count ("Đồ văng/vỡ"), ragdolls at once, Rapier flavor, file name. A bench row with throttled "Có" or Low Power reported on is INVALID_THROTTLED; a bench row whose NPC value is not 10 is INVALID_SCENARIO; both must be re-measured (report them; they are neither pass nor fail).
Write 01-GATE.md with lines: `SHA=<sha>`, `GATE_ANDROID_FPS=PASS|FAIL (avg=<n>, low1=<n>, tier=<t>, npc=10)` (PASS when avg ≥ 30 on a valid row), `GATE_ANDROID_OBJECTS=PASS|FAIL (knocked_or_broken=<n>)` (PASS when the same valid Android bench row shows ≥ 20 objects knocked or broken, ROADMAP success criterion 2), `GATE_IOS_SOAK=PASS|FAIL` (PASS when the soak panel shows 15 minutes completed, the operator reported no crash and no self-reload, and no crash banner appeared on reload), `GATE_FIRST_LOAD=PASS|FAIL (raw=<bytes>)` (PASS when ≤ 20,000,000 bytes; when > 8,000,000 SIZE-REASON.md must exist), `TECH03_DESKTOP=<avg> on <refresh> Hz` with `TECH03_DESKTOP_OK=yes|no` (yes when avg ≥ 57 on a 60 Hz display or ≥ 95% of the display rate), `TECH01_BROWSERS=<n>/5 playable`, `CTRL02_FEEL=<summary>`, `CTRL_UI_CHECK=<ok cells>/<answered cells> ok` (from the controls & settings checklist, "n/a" cells excluded), then `VERDICT=PASS` only if GATE_ANDROID_FPS, GATE_ANDROID_OBJECTS, GATE_IOS_SOAK and GATE_FIRST_LOAD are all PASS, else `VERDICT=FAIL` and `FAILED=<comma list>`. Add a short evidence section linking each screenshot and quoting the operator notes. If TECH03_DESKTOP_OK=no, TECH01_BROWSERS < 5 or CTRL_UI_CHECK has any "not ok", list them under "## Gaps for verify-work" with the check names (they do not trigger D-07 by themselves). If VERDICT=FAIL, add a STATE.md blocker: "Phase 1 gate FAIL (<FAILED>) — run plans 01-20 and 01-21 (single D-07 optimisation pass); do not start Phase 2". If any bench row is INVALID, write VERDICT=REMEASURE with the reason and stop so the operator can redo Task 2 for that device.</action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/phases/01-spike-k-thu-t-ng-deploy/01-GATE.md','utf8');const keys=['SHA=','GATE_ANDROID_FPS=','GATE_ANDROID_OBJECTS=','GATE_IOS_SOAK=','GATE_FIRST_LOAD=','TECH03_DESKTOP_OK=','TECH01_BROWSERS=','CTRL_UI_CHECK=','VERDICT='];const miss=keys.filter(k=>!s.includes(k));const v=(s.match(/VERDICT=(PASS|FAIL|REMEASURE)/)||[])[1];console.log('GATE_KEYS_MISSING',miss.join(',')||'none','VERDICT',v||'none');process.exit(miss.length||!v?1:0)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `GATE_KEYS_MISSING none` and a VERDICT of PASS, FAIL or REMEASURE, exit 0
    - 01-DEVICE-LOG.md "## Measurement sessions" has at least 4 rows whose Screenshot column names files that exist in evidence/
    - When VERDICT=FAIL, `grep -c "Phase 1 gate FAIL" .planning/STATE.md` prints 1
    - The SHA in 01-GATE.md equals the sha in 01-GO-LIVE.md "## Measurement build"
    - `grep -E "^GATE_ANDROID_OBJECTS=(PASS|FAIL) \(knocked_or_broken=[0-9]+\)" 01-GATE.md` matches, and VERDICT=PASS never appears together with GATE_ANDROID_OBJECTS=FAIL
    - `grep -E "^CTRL_UI_CHECK=[0-9]+/[0-9]+ ok" 01-GATE.md` matches
  </acceptance_criteria>
  <done>The phase has a written, evidence-backed verdict against the ROADMAP gate, with the new controls and settings checked on real devices.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator devices → repository evidence | Screenshots and notes become acceptance records |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-19-01 | Repudiation | measurement authenticity | mitigate | Screenshots must show the deployed sha, tier source, NPC count and throttled flag; rows with a different sha, throttling or NPC ≠ 10 are marked INVALID and excluded |
| T-01-19-02 | Information disclosure | screenshots and device details in git | mitigate | Only model/OS/browser are recorded (no serials, accounts or personal data); the operator uses made-up NPC names for the checklist and crops notifications before saving if needed |
| T-01-19-03 | Tampering | gate outcome drift | mitigate | Verdict rules written as explicit thresholds tied to ROADMAP criteria 2–4 and D-07; controls/settings findings are gaps, never silent verdict changes |
</threat_model>

<verification>
- 01-DEVICE-LOG.md complete before measurements; evidence screenshots present; controls & settings checklist answered; 01-GATE.md has VERDICT
</verification>

<success_criteria>
- ROADMAP Phase 1 gate evaluated on real devices with evidence (10-NPC bench); D-27..D-30 checked on desktop and both phones; next step (Phase 2 or plan 01-20) is unambiguous
</success_criteria>

## Artifacts this phase produces

- Files: `01-DEVICE-LOG.md` (sections `## Reference devices`, `## Measurement sessions`, `## Browser checklist`, `## Controls & settings checklist`), `01-GATE.md` (keys `SHA`, `GATE_ANDROID_FPS`, `GATE_ANDROID_OBJECTS`, `GATE_IOS_SOAK`, `GATE_FIRST_LOAD`, `TECH03_DESKTOP`, `TECH03_DESKTOP_OK`, `TECH01_BROWSERS`, `CTRL02_FEEL`, `CTRL_UI_CHECK`, `VERDICT`, `FAILED`; section `## Gaps for verify-work`), `evidence/README.md`, `evidence/{android,iphone,desktop}-bench-<sha>.png`, `evidence/iphone-soak-<sha>.png`
- Row markers: `INVALID_SHA`, `INVALID_THROTTLED`, `INVALID_SCENARIO`, `BELOW_BASELINE`; verdict `REMEASURE`
- STATE.md blocker text on failure: "Phase 1 gate FAIL"

<output>
Create `.planning/phases/01-spike-k-thu-t-ng-deploy/01-19-SUMMARY.md` when done
</output>
