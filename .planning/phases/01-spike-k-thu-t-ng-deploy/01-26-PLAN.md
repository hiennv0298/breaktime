---
phase: 01-spike-k-thu-t-ng-deploy
plan: 26
type: execute
wave: 11
depends_on: ["01-23", "01-24"]
files_modified:
  - src/logic/npcSettings.ts
  - tests/unit/npcSettings.test.ts
  - tests/e2e/npcNames.spec.ts
  - src/game/npcSettingsStore.ts
  - src/ui/npcLabels.ts
  - src/ui/npcLabels.css
  - src/game/game.ts
autonomous: true
requirements: [CTRL-07, TECH-06]
must_haves:
  truths:
    - "The NPC count (0–10, default 3) and up to 10 NPC names saved on this device are applied when the game starts; each named NPC shows its name above its head, following it while walking and while flying as a ragdoll (D-29, CTRL-07, D-11 revised)"
    - "Names are cleaned before use: trimmed, whitespace collapsed, control / bidi / zero-width characters removed, NFC-normalised and cut to 16 characters (code points); an empty name shows no label (D-29)"
    - "Names are rendered with textContent only — a stored name like <img src=x onerror=alert(1)> shows as literal text, creates no element and runs nothing (D-29 'không innerHTML')"
    - "Corrupt, oversized or tampered storage and a throwing localStorage (incognito / disabled) never crash the game: it falls back to 3 unnamed NPCs (D-29 'bọc try/catch')"
    - "?npcs= still overrides the saved count (clamped 0–10) for tests, createGame(ctx, { forcedNpcCount }) overrides both (the 01-17 bench and 01-18 soak pass MAX_NPCS), and saved names still apply (D-29)"
    - "Names never leave the device: no request carries them and the new modules contain no network call (D-29, D-31 accepted risk for the play-test build, to be reviewed before the Phase 8 CrazyGames submission)"
    - "Settings parsing and name sanitising are pure and unit-tested (TECH-06)"
  artifacts:
    - path: "src/logic/npcSettings.ts"
      provides: "Pure NPC settings model: limits, sanitize, parse/serialize, start resolution with ?npcs precedence"
      exports: ["MAX_NPCS", "DEFAULT_NPCS", "NPC_NAME_MAX", "NPC_SETTINGS_KEY", "NpcSettings", "NpcSettingsSource", "sanitizeNpcName", "normalizeNpcSettings", "parseNpcSettings", "serializeNpcSettings", "npcCountFromQuery", "resolveStartNpcSettings"]
    - path: "src/game/npcSettingsStore.ts"
      provides: "try/catch localStorage read/write for 'bt.npcs'"
      exports: ["readNpcSettingsRaw", "writeNpcSettings"]
    - path: "src/ui/npcLabels.ts"
      provides: "DOM name labels over NPC heads (textContent, 0 draw calls)"
      exports: ["createNpcLabels", "NpcLabels"]
    - path: "tests/e2e/npcNames.spec.ts"
      provides: "Stored names, XSS payload, corrupt/throwing storage, ?npcs precedence, ragdoll-following label, no network"
    - path: "src/game/game.ts"
      provides: "createGame(ctx, opts: CreateGameOptions); opts.forcedNpcCount is passed to resolveStartNpcSettings as the count override"
      contains: "forcedNpcCount"
  key_links:
    - from: "src/game/game.ts"
      to: "src/logic/npcSettings.ts"
      via: "resolveStartNpcSettings(location.search, readNpcSettingsRaw().raw, opts.forcedNpcCount)"
      pattern: "resolveStartNpcSettings\\("
    - from: "src/game/game.ts"
      to: "src/ui/npcLabels.ts"
      via: "label positions projected every rendered frame"
      pattern: "labels\\.place\\("
    - from: "src/ui/npcLabels.ts"
      to: "DOM"
      via: "textContent only"
      pattern: "textContent"
---

## Phase Goal

**As a** solo operator play-testing on my two reference phones, **I want to** open https://game.doibung.com, walk a low-poly office, slap NPCs into ragdolls and smash 20+ objects in a build that any commit can deploy with one command, **so that** I have measured proof that Three.js + Rapier meets the size, fps and iOS-stability budget before any gameplay phase builds on it.

<objective>
Named coworkers slice (operator play-test 15/09/2026): after this plan the office starts with the NPC count and names saved on this device, and every named coworker carries a readable name tag — safely, even with hostile or broken saved data.

Purpose: D-29 / CTRL-07 (count 0–10, names ≤ 16 characters, local only). The settings screen that writes these values is plan 01-27; this plan makes saved values real at game start and proves the sanitising, rendering and storage rules first.
Output: pure settings model with tests, storage wrapper, DOM label layer, game start wiring, e2e.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-23-SUMMARY.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-24-SUMMARY.md
@src/game/game.ts
@src/game/npc.ts
@src/game/qualityManager.ts

<interfaces>
Existing after 01-23/01-24: game.ts `DEFAULT_NPCS 3`, `MAX_NPCS 10`, `npcCountFromQuery(search)` (clamp 0..10), NPC spawn loop using `routeIndexForNpc` / `sharedIndexForNpc`, `Npc { id, character, body, ragdoll, mode, slappable(), pos(), foot() }` (pos = capsule centre, torso while ragdoll; foot = floor point under the NPC), `createCameraView`, `frameUpdate(dt, nowMs)` (camera updated before `room.update` / `shadows.update`), `__bt.npcs[] { id, pos, mode, texture }`, `__bt.ragdolls.active`, `__bt.swing`, `QUALITY_STORAGE_KEY` try/catch pattern, CSP `style-src 'self' 'unsafe-inline'` (element.style writes are fine), z-order #touch-zone 100 < #key-hints 150 < HUD buttons 200 < #pause-menu 400.

Contracts this plan creates:

```typescript
// src/logic/npcSettings.ts (pure: no three.js, no DOM, no storage)
export const MAX_NPCS = 10;
export const DEFAULT_NPCS = 3;
export const NPC_NAME_MAX = 16;                 // code points after NFC
export const NPC_SETTINGS_KEY = 'bt.npcs';
export const NPC_SETTINGS_MAX_RAW = 4096;       // longer stored strings are ignored
export interface NpcSettings { count: number; names: string[] }   // names.length === MAX_NPCS, '' = no label
export type NpcSettingsSource = 'default' | 'stored' | 'query' | 'manual';   // 'manual' is set by plan 01-27
export function sanitizeNpcName(raw: unknown): string;
export function normalizeNpcSettings(input: unknown): NpcSettings;           // clamps count, sanitizes and pads names
export function parseNpcSettings(raw: string | null): { settings: NpcSettings; valid: boolean };
export function serializeNpcSettings(s: NpcSettings): string;                // {"v":1,"count":n,"names":[...]}
export function npcCountFromQuery(search: string): number | null;            // null when absent or unparsable
export function resolveStartNpcSettings(search: string, storedRaw: string | null, forcedCount?: number): { settings: NpcSettings; source: NpcSettingsSource };   // a finite forcedCount replaces the ?npcs= value (source 'query')
// src/game/npcSettingsStore.ts
export function readNpcSettingsRaw(): { raw: string | null; storageOk: boolean };
export function writeNpcSettings(s: NpcSettings): boolean;                    // false when storage throws
// src/ui/npcLabels.ts
export interface NpcLabels { setText(i: number, text: string): void; place(i: number, x: number, y: number): void; hide(i: number): void; snapshot(): Array<{ index: number; text: string; visible: boolean; x: number; y: number }> }
export function createNpcLabels(root: HTMLElement, capacity: number): NpcLabels;
// src/game/game.ts
export interface CreateGameOptions { forcedNpcCount?: number }   // bench / soak (plans 01-17, 01-18) pass MAX_NPCS
export function createGame(ctx: GameCtx, opts?: CreateGameOptions): Promise<Game>;
// Game.npcSettings(): { settings: NpcSettings; source: NpcSettingsSource; storageOk: boolean }
// window.__bt.npcSettings: { count, names, source, storageOk }; __bt.npcLabels: snapshot(); __bt.npcs[] gains name and screen { x, y } (lazy projection)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NPC settings model and name sanitising via TDD, plus a failing names e2e (RED)</name>
  <files>src/logic/npcSettings.ts, tests/unit/npcSettings.test.ts, tests/e2e/npcNames.spec.ts</files>
  <read_first>
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-CONTEXT.md decisions D-11 (revised), D-29, D-31
    - .planning/REQUIREMENTS.md CTRL-07 and the Out of Scope exception (15/09/2026); .planning/PROJECT.md Out of Scope (tên người thật) and Constraints (localStorage try/catch, no personal data collection)
    - src/game/game.ts (npcCountFromQuery, DEFAULT_NPCS, MAX_NPCS), src/game/qualityManager.ts (storage pattern)
    - tests/e2e/npc.spec.ts, tests/e2e/slap.spec.ts, tests/e2e/helpers.ts
  </read_first>
  <behavior>
    - sanitizeNpcName: '  Sếp   Tùng  ' → 'Sếp Tùng'; the string "Te" + U+0302 + U+0301 + "t" → 'Tết' (NFC); 'ABCDEFGHIJKLMNOPQRSTUV' → 'ABCDEFGHIJKLMNOP'; twenty '😀' → sixteen '😀' (no split surrogate); U+202E followed by "gnp.exe" → 'gnp.exe'; "a" U+0000 "b" U+200B "c" U+FEFF → 'abc'; 'line\nbreak' → 'line break'; '<img src=x onerror=alert(1)>' → '<img src=x onerr' (kept literally, 16 code points); '   ' → ''; 123, null, {} → ''; a 10 000-character string is handled and returns 16 code points
    - normalizeNpcSettings: { count: 42, names: [] } → count 10; count -3 → 0; count 2.7 → 2; count '5', NaN or missing → 3; names not an array → ten ''; more than 10 names → first 10; non-string entries → ''; result.names.length is always 10
    - parseNpcSettings: null → default (count 3, ten '') with valid false; '{not json' → default, valid false; 'null' and '[]' → default, valid false; {"v":2,...} → default, valid false; a string longer than NPC_SETTINGS_MAX_RAW → default, valid false; {"v":1,"count":5,"names":["A","B"]} → count 5, names A, B, then '' ×8, valid true
    - serializeNpcSettings then parseNpcSettings round-trips to a deep-equal NpcSettings; ten 16-emoji names serialize to ≤ NPC_SETTINGS_MAX_RAW characters
    - npcCountFromQuery: '' → null; '?npcs=abc' → null; '?npcs=7' → 7; '?npcs=99' → 10; '?npcs=-1' → 0; '?npcs=2.9' → 2
    - resolveStartNpcSettings: ('', null) → count 3, source 'default'; ('', stored count 5 names A,B) → count 5, names A,B…, source 'stored'; ('?npcs=7', stored count 2 names A,B) → count 7, names A,B…, source 'query'; ('?npcs=abc', stored count 5) → count 5, source 'stored'; ('?npcs=99', '{bad') → count 10, names all '', source 'query'; with forcedCount: ('?npcs=3', stored count 2 names A,B, 10) → count 10, names A,B…, source 'query'; ('', stored count 2, 99) → count 10; ('', stored count 2, -4) → count 0; ('?npcs=7', null, NaN) → count 7 (a non-finite forcedCount is ignored)
    - npcNames.spec desktop 'stored names appear above their NPCs as plain text': addInitScript stores 'bt.npcs' = {"v":1,"count":5,"names":["  Sếp   Tùng  ","<img src=x onerror=alert(1)>","Ánh","","ABCDEFGHIJKLMNOPQRSTUV"]} → './?autoplay=1' → __bt.npcs length 5; __bt.npcSettings.source 'stored'; __bt.npcLabels texts for indices 0,1,2,4 are 'Sếp Tùng', '<img src=x onerr', 'Ánh', 'ABCDEFGHIJKLMNOP' and index 3 is not visible; document.querySelector('#npc-labels img') is null; no 'dialog' event fired; for npc-0, once its __bt.npcs[0].screen is inside the viewport, the label element's box centre x is within 30 px of screen.x and its bottom edge is above screen.y
    - npcNames.spec desktop 'corrupt storage falls back to three unnamed NPCs': stored '{not json' → 3 NPCs, source 'default', no visible label
    - npcNames.spec desktop 'throwing storage does not crash': addInitScript makes Storage.prototype.getItem/setItem throw → state 'playing', 3 NPCs, __bt.npcSettings.storageOk false, zero page errors
    - npcNames.spec desktop '?npcs overrides the stored count, names still apply': stored count 2 names ['Minh','Lan'] + './?autoplay=1&npcs=7' → 7 NPCs, source 'query', labels 'Minh' and 'Lan' on indices 0 and 1; './?autoplay=1&npcs=99' with stored count 42 → 10 NPCs
    - npcNames.spec desktop 'label follows a slapped NPC': stored count 1 name 'Bốp' + './?autoplay=1&npcs=1&npcAt=0.9,1.0' → label index 0 visible → press Space → wait __bt.ragdolls.active 1 → within 1000 ms the label's x or y has changed by more than 20 px while visible, or it is hidden because the ragdoll left the view
    - npcNames.spec desktop 'names never leave the device': with stored names ['Sếp Tùng'] every request URL seen by the page is same-origin, no request method other than GET/HEAD happens after 'playing', and no request URL contains 'S%E1%BA%BFp' or 'Sếp'
    - every test: zero console errors, pageerrors and off-origin requests
  </behavior>
  <action>Implements the pure part of D-29 (and records D-31). Write tests/unit/npcSettings.test.ts first, confirm failure, implement src/logic/npcSettings.ts until green. sanitizeNpcName order: non-string → ''; slice to 256 UTF-16 units (bounds the regex work); normalize('NFC'); remove characters in the ranges U+0000–U+001F, U+007F–U+009F, U+00AD, U+061C, U+180E, U+200B–U+200F, U+2028–U+202E, U+2060–U+206F, U+FEFF except that \n, \r and \t first become spaces; collapse whitespace runs to one space; trim; Array.from(...).slice(0, NPC_NAME_MAX).join(''); trimEnd. Count rules for normalizeNpcSettings: accept only typeof number and finite, Math.trunc, clamp [0, MAX_NPCS], else DEFAULT_NPCS. parseNpcSettings rejects raw.length > NPC_SETTINGS_MAX_RAW before JSON.parse, wraps JSON.parse in try/catch, requires a plain object with v === 1, then normalizes. serializeNpcSettings normalizes again and writes version 1. resolveStartNpcSettings(search, storedRaw, forcedCount): parse stored; q = forcedCount when it is a finite number (Math.trunc, clamped 0..MAX_NPCS), otherwise npcCountFromQuery(search); q !== null → { count q, names from stored (normalized), source 'query' }; else stored valid → 'stored'; else 'default'. Add a header comment: names stay on this device (D-29), content risk accepted for the play-test only and must be reviewed before Phase 8 (D-31). Then write tests/e2e/npcNames.spec.ts per the behavior list (storage seeded with page.addInitScript before goto; request log via page.on('request')) and confirm it fails against the current build.</action>
  <verify>
    <automated>npx vitest run tests/unit/npcSettings.test.ts && echo UNIT_GREEN && npx vite build && echo BUILD_OK && { npx playwright test tests/e2e/npcNames.spec.ts --project=desktop; test $? -ne 0 && echo E2E_RED; }</automated>
  </verify>
  <acceptance_criteria>
    - Output contains UNIT_GREEN, BUILD_OK and E2E_RED with failures on __bt.npcSettings / labels (not "No tests found")
    - `grep -vE "^\s*(//|\*)" src/logic/npcSettings.ts | grep -cE "localStorage|document\.|window\.|fetch\(|from 'three'"` prints 0
    - src/logic/npcSettings.ts contains "NFC", "4096", "'bt.npcs'" and "D-31"
  </acceptance_criteria>
  <done>Name cleaning, count limits, storage parsing and ?npcs precedence are proven in Node; stored names in the browser are a failing e2e.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply saved settings at game start and draw name labels (GREEN)</name>
  <files>src/game/npcSettingsStore.ts, src/ui/npcLabels.ts, src/ui/npcLabels.css, src/game/game.ts</files>
  <read_first>
    - tests/e2e/npcNames.spec.ts, src/logic/npcSettings.ts
    - src/game/game.ts (NPC loop, frameUpdate order, __bt npcs/highlight projection code), src/game/npc.ts (pos, foot, mode), src/game/qualityManager.ts (try/catch storage), src/ui/debugHud.ts (css import pattern)
    - tests/e2e/npc.spec.ts, tests/e2e/characters.spec.ts, tests/e2e/slap.spec.ts (must stay green)
  </read_first>
  <behavior>
    - All npcNames.spec tests pass; npc.spec, characters.spec, slap.spec and the full `npx playwright test` stay green
  </behavior>
  <action>Implements D-29 at game start (count + names + labels), with ?npcs precedence and D-31's local-only rule.
npcSettingsStore.ts: readNpcSettingsRaw reads localStorage NPC_SETTINGS_KEY inside try/catch (throw → { raw: null, storageOk: false }); writeNpcSettings(s) stores serializeNpcSettings(s) inside try/catch and returns false on any throw. No other storage access, no network.
npcLabels.ts (imports './npcLabels.css'): one div#npc-labels (aria-hidden true) appended to the given root; capacity div.npc-label elements created lazily; setText uses textContent only (never innerHTML / insertAdjacentHTML / outerHTML) and sets data-npc index; place(i, x, y) rounds to whole pixels and writes style.transform = 'translate(Xpx, Ypx) translate(-50%, -100%)' only when the rounded values changed, and unhides; hide(i) sets hidden; snapshot() reports index, text, visible, x, y.
npcLabels.css: #npc-labels position fixed, inset 0, pointer-events none, z-index 90, overflow hidden; .npc-label position absolute, left 0, top 0, max-width 12em, white-space nowrap, overflow hidden, text-overflow ellipsis, font 600 13px/1.25 system-ui, color #fff, background rgba(0,0,0,0.55), padding 1px 6px, border-radius 6px; [hidden] display none.
game.ts: import MAX_NPCS / DEFAULT_NPCS / npcCountFromQuery from src/logic/npcSettings.ts instead of the local copies (keep `export { MAX_NPCS, DEFAULT_NPCS }` re-exports for existing importers); export interface CreateGameOptions { forcedNpcCount?: number } and change the signature to createGame(ctx: GameCtx, opts: CreateGameOptions = {}) (main.ts keeps calling createGame(ctx) here; plan 01-17 passes { forcedNpcCount: MAX_NPCS } for the bench); at creation const stored = readNpcSettingsRaw(); const start = resolveStartNpcSettings(location.search, stored.raw, opts.forcedNpcCount); spawn start.settings.count NPCs (the existing loop and route mapping); create labels = createNpcLabels(uiRoot, MAX_NPCS) and setText(i, names[i]) for every spawned NPC. In frameUpdate, after cameraView.update: for each NPC with a non-empty name compute the anchor (mode 'ragdoll': pos() plus 0.9 m up; otherwise foot() plus 1.85 m up), project it with the camera into a reused Vector3, hide when z > 1 or |x| > 1.1 or |y| > 1.1, else labels.place(i, (x+1)/2 × canvas.clientWidth, (1−y)/2 × canvas.clientHeight); unnamed NPCs stay hidden. Add Game.npcSettings() returning settings, source and storageOk; registerDebug('npcSettings', …) and registerDebug('npcLabels', () => labels.snapshot()); extend the 'npcs' getter with name and a lazy screen projection of pos() (same maths as the highlight getter). No network code anywhere in these files.</action>
  <verify>
    <automated>npm run typecheck && echo TYPECHECK_OK && npx vitest run && echo UNIT_OK && npx vite build && echo BUILD_OK && npx playwright test && echo E2E_OK</automated>
  </verify>
  <acceptance_criteria>
    - Output contains TYPECHECK_OK, UNIT_OK, BUILD_OK and E2E_OK, and the Playwright output has no line containing " failed"
    - `grep -rnE "innerHTML|outerHTML|insertAdjacentHTML" src | grep -vE ":[0-9]+:\s*(//|\*)" | wc -l` prints 0
    - `grep -vE "^\s*(//|\*)" src/game/npcSettingsStore.ts src/ui/npcLabels.ts src/logic/npcSettings.ts | grep -cE "fetch\(|XMLHttpRequest|sendBeacon|WebSocket|EventSource"` prints 0
    - `grep -n "localStorage" src/game/npcSettingsStore.ts` lines all sit inside try blocks and `grep -c "try" src/game/npcSettingsStore.ts` prints at least 2
    - `grep -c "resolveStartNpcSettings(" src/game/game.ts` prints 1, `grep -c "forcedNpcCount" src/game/game.ts` prints at least 2, and `grep -c "textContent" src/ui/npcLabels.ts` prints at least 1
  </acceptance_criteria>
  <done>Saved count and names shape the office at start; names render as safe text tags that follow their NPCs; broken or blocked storage cannot break the game.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage `bt.npcs` → game state and DOM | Visitor-typed (via 01-27) or tampered text rendered on screen |
| URL query `npcs` → NPC count | Visitor-controlled number |
| game → network | Names must never cross this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-26-01 | Elevation of privilege (XSS) | NPC name labels | mitigate | textContent only; repo-wide innerHTML/outerHTML/insertAdjacentHTML grep gate = 0; e2e with an `<img onerror>` payload asserts no element and no dialog |
| T-01-26-02 | Tampering | corrupt / edited `bt.npcs` JSON | mitigate | length cap 4096 before JSON.parse, try/catch, version check, normalizeNpcSettings clamps count 0..10 and sanitizes every name; unit + e2e fallbacks |
| T-01-26-03 | Spoofing | names with bidi overrides, zero-width or control characters imitating other text | mitigate | sanitizeNpcName strips C0/C1 controls, bidi embeddings/overrides/isolates, zero-width and BOM characters, collapses whitespace, NFC, 16 code points |
| T-01-26-04 | Denial of service | huge stored strings / huge ?npcs | mitigate | 4096-char raw cap, 256-unit pre-slice per name, count clamp 0..10 |
| T-01-26-05 | Denial of service | localStorage throwing (incognito, disabled) | mitigate | npcSettingsStore wraps every access; game starts with defaults; e2e with throwing Storage |
| T-01-26-06 | Information disclosure | names sent off the device | mitigate | no network API in the new modules (grep gate); e2e request log has only same-origin GET/HEAD and never the name |
| T-01-26-07 | Repudiation / content risk | player types real people's names (PROJECT Out of Scope exception) | accept | D-31: operator accepted for the play-test build; names stay local; must be replaced by a preset list or a filter before the Phase 8 CrazyGames submission (recorded in the module header and the SUMMARY) |
</threat_model>

<verification>
- `npm run typecheck && npx vitest run && npx vite build && npx playwright test` exits 0 with the four markers
</verification>

<success_criteria>
- CTRL-07's storage, naming and label rules hold at game start: 0–10 NPCs, names ≤ 16 characters above heads, local only, safe against tampered or unavailable storage
</success_criteria>

## Artifacts this phase produces

- Symbols: `MAX_NPCS`, `DEFAULT_NPCS`, `NPC_NAME_MAX`, `NPC_SETTINGS_KEY`, `NPC_SETTINGS_MAX_RAW`, `NpcSettings`, `NpcSettingsSource`, `sanitizeNpcName`, `normalizeNpcSettings`, `parseNpcSettings`, `serializeNpcSettings`, `npcCountFromQuery`, `resolveStartNpcSettings`, `readNpcSettingsRaw`, `writeNpcSettings`, `NpcLabels`, `createNpcLabels`, `Game.npcSettings`, `CreateGameOptions { forcedNpcCount }` (option of `createGame(ctx, opts)`; replaces the ?npcs= value in `resolveStartNpcSettings(search, storedRaw, forcedCount)`; used by the 01-17 bench and the 01-18 soak)
- Storage key: `localStorage['bt.npcs']` = `{"v":1,"count":n,"names":[…10]}`
- DOM: `#npc-labels` with `.npc-label[data-npc]`
- `window.__bt` keys: `npcSettings { count, names, source, storageOk }`, `npcLabels[]`; `__bt.npcs[]` gains `name`, `screen`
- Tests: `tests/unit/npcSettings.test.ts`, `tests/e2e/npcNames.spec.ts`

<output>
Create `.planning/phases/01-spike-k-thu-t-ng-deploy/01-26-SUMMARY.md` when done
</output>
