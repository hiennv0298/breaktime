---
phase: 01-spike-k-thu-t-ng-deploy
plan: 27
type: execute
wave: 12
depends_on: ["01-26", "01-25", "01-22", "01-15"]
files_modified:
  - tests/e2e/npcSettings.spec.ts
  - src/game/npc.ts
  - src/game/game.ts
  - src/ui/npcSettingsSection.ts
  - src/game/loop.ts
  - src/ui/settings.css
  - src/boot/pageHardening.ts
autonomous: true
requirements: [CTRL-07, CTRL-04, TECH-06]
must_haves:
  truths:
    - "In the settings menu (opened with Ctrl, Esc or the ⏸ button) the player picks 0–10 NPCs with − / + buttons (default 3) and types a name per NPC; 'Áp dụng' applies the choice immediately and saves it on this device (D-29, CTRL-07, CTRL-04, D-27)"
    - "Applying respawns safely in place: NPCs above the new count despawn (a flying ragdoll is put back together first), missing NPCs spawn on their hand-placed routes, NPCs that stay keep their current state and only get the new name; props, broken objects, shards and the remaining blob shadows are untouched (D-29)"
    - "Growing the count reuses NPCs created earlier in the session (grow-only pool, max 10), so repeated Apply never allocates more than 10 NPCs' bodies (D-29, D-11 revised)"
    - "Typed names are cleaned with the 01-26 rules on Apply (≤ 16 characters, trimmed) and shown back in the fields; while typing, keys never move the player, swing, rotate the camera, toggle the debug HUD or close the menu (except Escape) (D-29, D-27)"
    - "On iPhone Safari the name fields accept typing and do not zoom the page (user-select text, 16 px font), and a long-press can paste into them; the menu scrolls to every field in landscape and portrait (D-16, D-29)"
    - "When storage fails, Apply still applies for this session and the menu says the choice was not saved (D-29 'bọc try/catch')"
    - "Clicking the key hint panel never swings (D-28 panel contract from 01-24/01-25)"
  artifacts:
    - path: "src/ui/npcSettingsSection.ts"
      provides: "Pause-menu NPC section: count stepper, 10 name fields, Áp dụng, save status"
      exports: ["createNpcSettingsSection"]
    - path: "src/game/npc.ts"
      provides: "Npc.active / despawn / respawn"
      contains: "despawn("
    - path: "src/game/game.ts"
      provides: "Grow-only NPC pool and Game.applyNpcSettings"
      contains: "applyNpcSettings"
    - path: "tests/e2e/npcSettings.spec.ts"
      provides: "Menu flow, persistence, safe respawn during ragdoll, typing guard, storage failure, mobile fields"
  key_links:
    - from: "src/ui/npcSettingsSection.ts"
      to: "src/game/game.ts"
      via: "onApply → writeNpcSettings + game.applyNpcSettings (wired in loop.ts)"
      pattern: "applyNpcSettings\\("
    - from: "src/game/game.ts"
      to: "src/game/npc.ts"
      via: "despawn / respawn instead of destroying Rapier bodies"
      pattern: "\\.despawn\\("
---

## Phase Goal

**As a** solo operator play-testing on my two reference phones, **I want to** open https://game.doibung.com, walk a low-poly office, slap NPCs into ragdolls and smash 20+ objects in a build that any commit can deploy with one command, **so that** I have measured proof that Three.js + Rapier meets the size, fps and iOS-stability budget before any gameplay phase builds on it.

<objective>
Settings slice (operator play-test 15/09/2026): after this plan the player opens settings, chooses how many coworkers are in the office (0–10), names them, presses Áp dụng, and sees the office change on the spot — on desktop and on both phones.

Purpose: completes D-29 / CTRL-07 on top of 01-26 (saved settings + labels), 01-25 (settings menu sections) and 01-22 (typing guard, Ctrl/Esc).
Output: failing-first e2e, NPC despawn/respawn, grow-only pool with applyNpcSettings, settings section UI, iOS-friendly input styling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-CONTEXT.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-RESEARCH.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-15-SUMMARY.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-25-SUMMARY.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-26-SUMMARY.md
@src/game/npc.ts
@src/game/game.ts
@src/game/loop.ts
@src/ui/pauseMenu.ts
@src/boot/pageHardening.ts

<interfaces>
Existing after 01-22..01-26: `normalizeNpcSettings`, `sanitizeNpcName`, `NpcSettings`, `NpcSettingsSource`, `MAX_NPCS 10` (src/logic/npcSettings.ts); `writeNpcSettings(s): boolean` (src/game/npcSettingsStore.ts); `createNpcLabels` / `labels.setText|place|hide`; `Game.npcSettings()`; `routeIndexForNpc`, `sharedIndexForNpc`; `createNpc(ctx, opts)` (kinematic capsule + pooled 6-body ragdoll created disabled; `beginRecover` re-attaches parts in depth order with `attach` and blends to `idlePose`); `shadows.addCaster(getPos, r) → handle` / `shadows.remove(handle)` (capacity 64: player 1 + 33 props + 10 NPCs fits); targeting `allCandidates` / `entryOf` built at creation, `refreshTarget()`; `createPauseMenu().addSection` (loop.ts adds quality, then key hints); `isTypingTarget` guard in keyboard.ts / cameraKeys.ts / debugHud.ts; `#pause-menu .panel` has max-height 90dvh and overflow-y auto, and pageHardening lets a one-finger touchmove scroll inside #pause-menu; hud.css sets `-webkit-user-select: none` on html/body (inherited by inputs); `__bt.swing.count`, `__bt.keyHints`, `__bt.npcs`, `__bt.npcSettings`, `__bt.ragdolls { active, bodies }`, `__bt.shadows.count`, `__bt.debris.broken`, `__bt.props { dynamicCount, brokenCount, movedCount }`, `__bt.hud.bodies`, `__bt.camera.targetYawDeg`, `__bt.hud.visible`.

Contracts this plan creates:

```typescript
// src/game/npc.ts (additions)
// Npc.active(): boolean; Npc.despawn(): void; Npc.respawn(spawn: { x: number; z: number }): void
// src/game/game.ts (additions)
// Game.applyNpcSettings(s: NpcSettings): void   — normalizes again, sets source 'manual'
// src/ui/npcSettingsSection.ts
export function createNpcSettingsSection(opts: {
  initial: NpcSettings;
  onApply(s: NpcSettings): { saved: boolean };
}): HTMLElement;   // section.npc-section: #npc-count-dec, output#npc-count-value, #npc-count-inc, input.npc-name[data-index] ×10, #npc-apply, #npc-apply-status
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Failing end-to-end test for the settings flow (RED)</name>
  <files>tests/e2e/npcSettings.spec.ts</files>
  <read_first>
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-CONTEXT.md decisions D-16, D-27, D-28, D-29, D-30
    - tests/e2e/npcNames.spec.ts (storage seeding + label helpers from 01-26), tests/e2e/keyHints.spec.ts (menu + toggle helpers from 01-25), tests/e2e/slap.spec.ts (npcAt pin + slap wait), tests/e2e/helpers.ts
    - src/ui/pauseMenu.ts, src/game/loop.ts
  </read_first>
  <action>Implements the D-29 acceptance as a browser test before any code. Write tests/e2e/npcSettings.spec.ts with these tests; each asserts zero console errors, pageerrors and off-origin requests.
Desktop 'set ten named NPCs from the menu, applied at once and kept after reload': './?autoplay=1' → 3 NPCs → press Escape → #npc-count-value shows '3' → click #npc-count-inc 7 times → '10', one more click stays '10'; exactly 10 input.npc-name are visible; fill index 0 with '  Sếp   Tùng  ' and index 1 with 'ABCDEFGHIJKLMNOPQRSTUV'; click #npc-apply → while still paused __bt.npcs length is 10 within 1000 ms, the fields now read 'Sếp Tùng' and 'ABCDEFGHIJKLMNOP', __bt.npcSettings.source 'manual', #npc-apply-status text contains 'Đã lưu'; press Escape → labels for 0 and 1 show those names; localStorage 'bt.npcs' parses to count 10 with names[0] 'Sếp Tùng'; reload './?autoplay=1' → 10 NPCs, source 'stored'; __bt.hud.bodies ≤ 200.
Desktop 'typing a name never drives the game': Escape → focus input.npc-name index 0 → type 'Zz Cc` WASD' and press ArrowLeft, Space and ControlLeft (press) → input value contains 'Zz Cc` WASD' with the spaces; __bt.camera.targetYawDeg unchanged; __bt.hud.visible unchanged; __bt.swing.count unchanged; __bt.paused still true; then press Escape → menu closes (Escape still works while typing).
Desktop 'shrinking and growing while a ragdoll flies is safe': './?autoplay=1&npcs=3&npcAt=0.9,1.0' → press Space → wait __bt.ragdolls.active 1 → press Escape at once; record props.dynamicCount, props.brokenCount, debris.broken, shadows.count; set count 0 and Apply → __bt.npcs length 0, __bt.ragdolls.active 0, shadows.count equals the recorded value minus 3, props and debris numbers unchanged; set count 10 and Apply → __bt.npcs length 10, __bt.ragdolls.bodies 60, shadows.count equals recorded minus 3 plus 10; Apply the same 10 again → ragdolls.bodies still 60 (grow-only pool); press Escape → after 1500 ms all 10 NPC positions are finite and inside the room (|x| < 8, |z| < 6) and every mode is walk or dwell.
Desktop 'renaming keeps a flying ragdoll flying': './?autoplay=1&npcs=1&npcAt=0.9,1.0' → press Space → wait ragdolls.active 1 → Escape → type 'Bốp' into index 0 → Apply (count unchanged 1) → __bt.npcs[0].mode is still 'ragdoll' and ragdolls.active 1 → Escape → the NPC later returns to 'walk' or 'dwell' within 7000 ms and label 0 shows 'Bốp'.
Desktop '?npcs is overridden by an explicit Apply': './?autoplay=1&npcs=7' → menu shows '7' → click − twice → Apply → 5 NPCs, source 'manual', storage count 5.
Desktop 'storage failure: applies but says not saved': addInitScript makes Storage.prototype.getItem/setItem throw → set count 4 → Apply → 4 NPCs, #npc-apply-status contains 'Không lưu được'.
Desktop 'clicking the key hint panel does not swing': './?autoplay=1&npcs=0' → click the centre of #key-hints → __bt.swing.count stays 0 after 300 ms.
Mobile-emu 'phone: count, name and apply from the ⏸ menu': './?autoplay=1' → tap #btn-pause → tap #npc-count-inc → '4' → tap input.npc-name index 0 and type 'Minh' with page.keyboard → computed user-select of that input is 'text' (or -webkit-user-select 'text') and font-size ≥ 16px → scroll #npc-apply into view and tap it → 4 NPCs → tap 'Tiếp tục' → label 0 'Minh'; setViewportSize 390x844 → open the menu again → the last visible name field and #npc-apply can be scrolled into the viewport (boundingBox inside 0..844 after scrollIntoViewIfNeeded).
Run it and confirm it fails (no #npc-count-inc yet).</action>
  <verify>
    <automated>npx vite build && echo BUILD_OK && { npx playwright test tests/e2e/npcSettings.spec.ts; test $? -ne 0 && echo E2E_RED; }</automated>
  </verify>
  <acceptance_criteria>
    - Output contains BUILD_OK and E2E_RED, with failures on missing #npc-count-inc / #npc-apply (not "No tests found")
    - tests/e2e/npcSettings.spec.ts contains "ragdolls", "bodies", "Không lưu được", "#key-hints" and "mobile-emu"
  </acceptance_criteria>
  <done>The full settings behaviour, including the unsafe cases, is written down as a failing browser test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NPC despawn/respawn, grow-only pool, applyNpcSettings and the count stepper (GREEN for the count flow)</name>
  <files>src/game/npc.ts, src/game/game.ts, src/ui/npcSettingsSection.ts, src/game/loop.ts</files>
  <read_first>
    - tests/e2e/npcSettings.spec.ts
    - src/game/npc.ts (beginRecover re-attach order, resume, idlePose), src/physics/ragdoll.ts (activate/deactivate/sync), src/game/game.ts (NPC loop, shadows, candidates, labels from 01-26), src/game/loop.ts (menu sections), src/ui/pauseMenu.ts, src/ui/keyHints.ts (section builder pattern from 01-25)
    - src/logic/npcSettings.ts, src/game/npcSettingsStore.ts
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-RESEARCH.md "Pattern 14" (pooled ragdoll, world-preserving attach)
  </read_first>
  <behavior>
    - The desktop tests 'shrinking and growing while a ragdoll flies is safe', 'renaming keeps a flying ragdoll flying' (with names typed via fill), '?npcs is overridden by an explicit Apply', 'storage failure' and the count part of 'set ten named NPCs' pass; npc.spec, slap.spec, characters.spec, npcNames.spec stay green
  </behavior>
  <action>Implements D-29's safe respawn and the settings entry point (D-27 menu).
npc.ts: add a private active flag (true after createNpc). active() returns it. despawn(): if already inactive return; if the NPC is a ragdoll, call ragdoll.sync() then ragdoll.deactivate() and re-attach the parts to their original parents in the existing reattachOrder (the same world-preserving attach used by beginRecover); in every mode copy idlePose back onto each part (position, quaternion, scale), reset getUp to createGetUp() and recoverT to 0, freeze the walker, body.setEnabled(false), character.root.visible = false, set active false. respawn(spawn): walker = createWalker(opts.route, spawn, startIndex) (not frozen), yaw and targetYaw 0, body.setTranslation({ x, CENTRE_Y, z }, true), body.setEnabled(true), body.setNextKinematicTranslation(same point), character.root.position.set(x, 0, z), rotation 0, visible true, character.setMotion('idle', 0), active true. slappable() is false while inactive; fixedUpdate and frameUpdate return immediately while inactive; pos()/foot() keep working.
game.ts: turn the startup loop into a grow-only pool: ensureNpc(i) creates NPC i exactly as today (route via routeIndexForNpc, startIndex i % route.length, spawn offset via sharedIndexForNpc, texture letter b+i, groupIndex i, the ?npcAt pin only for NPC 0 at page start) and registers its targeting candidate once; spawnPointFor(i) gives the same route spawn used at creation; activate(i) respawns an inactive NPC at spawnPointFor(i) and adds its blob shadow caster (store the handle); deactivate(i) calls npc.despawn(), removes the shadow handle and hides label i. applyNpcSettings(input): s = normalizeNpcSettings(input); for i in 0..MAX_NPCS−1: i < s.count → ensureNpc(i) (new NPCs are already active with a shadow) or activate(i) when inactive, NPCs already active are left untouched; i ≥ s.count → deactivate(i) when it exists and is active; update every label text from s.names; store s with source 'manual'; call refreshTarget() so a despawned glowing NPC stops glowing even while paused. Startup calls the same pool path with the resolved start settings (source kept from 01-26, including createGame's forcedNpcCount option). __bt.npcs lists active NPCs only; __bt.ragdolls stays the pool total. Add applyNpcSettings to the Game interface.
npcSettingsSection.ts (createElement/textContent only): section.npc-section with div.label 'NPC trong văn phòng'; a row with button#npc-count-dec '−', output#npc-count-value, button#npc-count-inc '+' (stepper clamps 0..MAX_NPCS, disables at the ends); ten label.npc-name-row each with input.npc-name (type text, data-index i, maxLength 32 so Vietnamese IME composition is never cut mid-letter, autocomplete off, spellcheck false, enterKeyHint 'done', placeholder 'Tên NPC ' + (i+1), value from initial.names[i]); rows with index ≥ current count are hidden; button#npc-apply 'Áp dụng'; div#npc-apply-status (aria-live polite). Apply builds { count, names: the ten raw input values }, runs normalizeNpcSettings, writes the normalized names back into the fields, calls onApply and sets the status text to 'Đã lưu trên máy này' when saved, otherwise 'Không lưu được (trình duyệt chặn lưu) — chỉ áp dụng lần này'. Enter inside a name field triggers the same Apply.
loop.ts: after the key hints section, menu.addSection(createNpcSettingsSection({ initial: game.npcSettings().settings, onApply: (s) => { const saved = writeNpcSettings(s); game.applyNpcSettings(s); return { saved }; } })).</action>
  <verify>
    <automated>npm run typecheck && echo TYPECHECK_OK && npx vitest run && echo UNIT_OK && npx vite build && echo BUILD_OK && npx playwright test tests/e2e/npcSettings.spec.ts --project=desktop -g "shrinking|renaming|overridden|storage failure|key hint" && npx playwright test tests/e2e/npc.spec.ts tests/e2e/slap.spec.ts tests/e2e/characters.spec.ts tests/e2e/npcNames.spec.ts && echo E2E_SLICE_OK</automated>
  </verify>
  <acceptance_criteria>
    - Output contains TYPECHECK_OK, UNIT_OK, BUILD_OK and E2E_SLICE_OK with no line containing " failed"
    - `grep -c "despawn(" src/game/npc.ts` prints at least 1 and `grep -c "\.despawn(" src/game/game.ts` prints at least 1
    - `grep -vE "^\s*(//|\*)" src/game/game.ts | grep -cE "removeRigidBody|removeCollider|removeImpulseJoint"` prints 0 (no Rapier destruction on Apply)
    - `grep -c "applyNpcSettings(" src/game/loop.ts` prints 1 and `grep -c "textContent" src/ui/npcSettingsSection.ts` prints at least 1
  </acceptance_criteria>
  <done>Players change the NPC count and names at runtime from the menu; removal and addition are safe during ragdolls and never grow the physics world past 10 NPCs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Name fields that work on phones — iOS input styling, paste, menu scrolling (GREEN)</name>
  <files>src/ui/settings.css, src/ui/npcSettingsSection.ts, src/boot/pageHardening.ts</files>
  <read_first>
    - tests/e2e/npcSettings.spec.ts (typing guard and mobile tests), tests/e2e/orientation.spec.ts (page-hardening test must stay green)
    - src/ui/hud.css (html/body user-select none, #pause-menu .panel), src/ui/debugHud.css (#pause-menu section styles), src/ui/keyHints.css
    - src/boot/pageHardening.ts
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-RESEARCH.md "Pattern 5" (page hardening), "Pitfall 4"
  </read_first>
  <behavior>
    - Every npcSettings.spec test passes in both projects (including 'typing a name never drives the game' and the mobile-emu test); orientation.spec page-hardening test still sees contextmenu, dblclick, gesturestart and touchmove prevented on non-input targets; the full `npx playwright test` is green
  </behavior>
  <action>Implements D-29 on phones (D-16 portrait kept).
settings.css (imported by npcSettingsSection.ts): #pause-menu .npc-section column layout with 8 px gaps; stepper buttons and #npc-apply min-height 48 px, touch-action manipulation; #npc-count-value min-width 3ch, centred, font-size 20 px; #pause-menu input.npc-name width 100%, box-sizing border-box, min-height 44 px, font-size 16 px (iOS zooms inputs below 16 px), padding 0 10px, border-radius 8px, border 2px solid #4a4f5a, background #1d1f24, color #f2f2f2, -webkit-user-select text, user-select text, -webkit-touch-callout default; focus border #ffc83d; the name rows use a two-column grid when the panel is at least 520 px wide, one column otherwise; label.npc-name-row[hidden] display none; #npc-apply-status font-size 14px, min-height 1.2em.
npcSettingsSection.ts: import './settings.css'; on focus of a name field call scrollIntoView({ block: 'nearest' }) so the on-screen keyboard does not hide it.
pageHardening.ts: do not preventDefault contextmenu or dblclick when the event target is inside an input or textarea (long-press paste and word selection in the name fields); gesturestart and touchmove rules stay as they are.
Keep every DOM write via createElement/textContent/value.</action>
  <verify>
    <automated>npm run typecheck && echo TYPECHECK_OK && npx vitest run && echo UNIT_OK && npx vite build && echo BUILD_OK && node scripts/size-report.mjs --gate && npx playwright test && echo E2E_OK</automated>
  </verify>
  <acceptance_criteria>
    - Output contains TYPECHECK_OK, UNIT_OK, BUILD_OK, SIZE_GATE_OK and E2E_OK, and the Playwright output has no line containing " failed"
    - `grep -c "font-size: 16px" src/ui/settings.css` prints at least 1 and `grep -c "user-select: text" src/ui/settings.css` prints at least 1
    - `grep -rnE "innerHTML|outerHTML|insertAdjacentHTML" src | grep -vE ":[0-9]+:\s*(//|\*)" | wc -l` prints 0
    - `grep -vE "^\s*(//|\*)" src/ui/npcSettingsSection.ts | grep -cE "fetch\(|XMLHttpRequest|sendBeacon|WebSocket"` prints 0
  </acceptance_criteria>
  <done>The settings screen works end to end on desktop and both phone orientations, with names that can be typed, pasted and scrolled to.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| text inputs → NPC names → DOM labels + localStorage | Free text typed by the player |
| settings Apply → physics world | Player-triggered spawn/despawn while the simulation holds live ragdolls |
| key events inside inputs → game controls | Typing must not become gameplay input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-27-01 | Elevation of privilege (XSS) | name fields and labels | mitigate | normalizeNpcSettings / sanitizeNpcName on Apply; labels and fields written with textContent / value only; repo-wide innerHTML grep gate = 0 |
| T-01-27-02 | Denial of service | repeated Apply growing the physics world | mitigate | grow-only pool capped at MAX_NPCS 10; despawn disables bodies instead of creating/removing; e2e asserts ragdolls.bodies stays 60 after repeated Apply and hud.bodies ≤ 200 |
| T-01-27-03 | Tampering | despawning an NPC mid-ragdoll leaving detached parts or active bodies | mitigate | despawn syncs, deactivates the ragdoll and re-attaches parts in depth order before hiding; e2e checks ragdolls.active 0 and finite positions after regrowth |
| T-01-27-04 | Tampering | typed keys moving/rotating/pausing the game | mitigate | 01-22 isTypingTarget guards; e2e types Z, C, backquote, WASD, arrows, Space and Ctrl into a field and asserts no game change |
| T-01-27-05 | Denial of service | storage throwing on save | mitigate | writeNpcSettings returns false inside try/catch; Apply still applies; status tells the player it was not saved |
| T-01-27-06 | Information disclosure | names transmitted | mitigate | no network API in the section module (grep gate); names only go to localStorage on this device (D-31 local-only rule) |
| T-01-27-07 | Repudiation / content risk | real people's names typed | accept | D-31 accepted for the play-test build; Phase 8 review item carried from 01-26 |
</threat_model>

<verification>
- `npm run typecheck && npx vitest run && npx vite build && node scripts/size-report.mjs --gate && npx playwright test` exits 0 with all markers
</verification>

<success_criteria>
- CTRL-07 is complete in the build: settings pick 0–10 NPCs and name them, applied safely in place and kept on the device; CTRL-04's Ctrl/Esc/⏸ menu is where it lives
</success_criteria>

## Artifacts this phase produces

- Symbols: `Npc.active`, `Npc.despawn`, `Npc.respawn`, `Game.applyNpcSettings`, `createNpcSettingsSection`
- DOM: `section.npc-section`, `#npc-count-dec`, `#npc-count-value`, `#npc-count-inc`, `input.npc-name[data-index]` ×10, `#npc-apply`, `#npc-apply-status`
- CSS: `src/ui/settings.css`
- `window.__bt.npcSettings.source` value `manual`; `__bt.npcs` lists active NPCs only
- Tests: `tests/e2e/npcSettings.spec.ts`

<output>
Create `.planning/phases/01-spike-k-thu-t-ng-deploy/01-27-SUMMARY.md` when done
</output>
