---
phase: 01-spike-k-thu-t-ng-deploy
plan: 21
type: execute
wave: 13
depends_on: ["01-20"]
files_modified:
  - .planning/phases/01-spike-k-thu-t-ng-deploy/01-GATE.md
  - .planning/phases/01-spike-k-thu-t-ng-deploy/01-DEVICE-LOG.md
  - .planning/phases/01-spike-k-thu-t-ng-deploy/01-PLAYCANVAS-EVAL.md
  - .planning/STATE.md
autonomous: true
requirements: [TECH-03, TECH-04]
must_haves:
  truths:
    - "This plan is conditional: Task 1 is an entry guard, and unless 01-OPTIMIZATION.md says OPTIMIZATION=APPLIED with re-measure evidence the executor ends the plan after Task 1 (D-07)"
    - "After an applied pass, the post-optimisation measurements from the same two devices produce VERDICT_AFTER_OPT with the same rules as the gate, including Android ≥ 30 fps and ≥ 20 objects knocked or broken (D-06, D-07, D-08)"
    - "If the re-measurement still fails, work STOPS: 01-PLAYCANVAS-EVAL.md is written and STATE.md carries a blocker that forbids starting Phase 2 on this stack (D-07, ROADMAP gate)"
  artifacts:
    - path: ".planning/phases/01-spike-k-thu-t-ng-deploy/01-PLAYCANVAS-EVAL.md"
      provides: "Stack re-evaluation, written only on a second failure"
    - path: ".planning/phases/01-spike-k-thu-t-ng-deploy/01-GATE.md"
      provides: "Section recording the outcome after the D-07 pass"
      contains: "VERDICT_AFTER_OPT="
  key_links:
    - from: "01-OPTIMIZATION.md"
      to: "Task 1 entry guard"
      via: "only OPTIMIZATION=APPLIED prints GUARD_CONTINUE"
      pattern: "GUARD_CONTINUE"
---

## Phase Goal

**As a** solo operator play-testing on my two reference phones, **I want to** open https://breaktime.doibung.com, walk a low-poly office, slap NPCs into ragdolls and smash 20+ objects in a build that any commit can deploy with one command, **so that** I have measured proof that Three.js + Rapier meets the size, fps and iOS-stability budget before any gameplay phase builds on it.

## Execution Rule (conditional plan)

1. Run Task 1 (entry guard) first. It prints `PLAN_EXIT_NOT_NEEDED`, `PLAN_EXIT_BLOCKED` or `GUARD_CONTINUE`.
2. On either `PLAN_EXIT_*`: **stop executing this plan immediately**, write the SUMMARY with `status: skipped`, and end. Task 2 does not run.
3. Only after `GUARD_CONTINUE` does Task 2 run.

<objective>
Conditional verdict after the single D-07 pass: transcribe the re-measurement, decide PASS or FAIL, and on FAIL stop the project on this stack with a written PlayCanvas evaluation. When no pass was needed, record that and exit.

Purpose: the ROADMAP gate must end with an evidence-backed decision, never with open-ended tuning.
Output: updated 01-GATE.md and 01-DEVICE-LOG.md; on a second failure also 01-PLAYCANVAS-EVAL.md and a STATE.md STOP blocker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-CONTEXT.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-RESEARCH.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-GATE.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-OPTIMIZATION.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-DEVICE-LOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Entry guard — continue only after an applied and re-measured D-07 pass</name>
  <files>.planning/phases/01-spike-k-thu-t-ng-deploy/01-GATE.md</files>
  <read_first>
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-OPTIMIZATION.md, 01-GATE.md
    - The "Execution Rule" section of this plan
  </read_first>
  <action>Implements the D-07 exit condition. If 01-OPTIMIZATION.md contains OPTIMIZATION=NOT_NEEDED: append "## Optimisation pass: not needed (gate PASS)" to 01-GATE.md, print `PLAN_EXIT_NOT_NEEDED`, end the plan. If it contains OPTIMIZATION=BLOCKED_REMEASURE, IN_PROGRESS, or is missing, or if evidence/ lacks both android-bench-opt- and iphone-bench-opt- files: append "## After D-07 pass: blocked (<reason>)" to 01-GATE.md, print `PLAN_EXIT_BLOCKED`, end the plan. Only with OPTIMIZATION=APPLIED and both -opt- screenshots present print `GUARD_CONTINUE`.</action>
  <verify>
    <automated>node -e "const g=require('fs').readFileSync('.planning/phases/01-spike-k-thu-t-ng-deploy/01-GATE.md','utf8');const nn=g.includes('Optimisation pass: not needed');const bl=g.includes('After D-07 pass: blocked');const o=require('fs').readFileSync('.planning/phases/01-spike-k-thu-t-ng-deploy/01-OPTIMIZATION.md','utf8');const cont=o.includes('OPTIMIZATION=APPLIED')&&!nn&&!bl;console.log(nn?'PLAN_EXIT_NOT_NEEDED':bl?'PLAN_EXIT_BLOCKED':cont?'GUARD_CONTINUE':'GUARD_INCONSISTENT');process.exit(nn||bl||cont?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints exactly one of PLAN_EXIT_NOT_NEEDED, PLAN_EXIT_BLOCKED or GUARD_CONTINUE and exits 0
    - On a PLAN_EXIT path the SUMMARY is written with status skipped and Task 2 is not started
  </acceptance_criteria>
  <done>The plan either exited with a recorded reason or confirmed a verdict is needed.</done>
</task>

<task type="auto">
  <name>Task 2: Verdict after the D-07 pass; on failure STOP and write the PlayCanvas evaluation (runs only after GUARD_CONTINUE)</name>
  <files>.planning/phases/01-spike-k-thu-t-ng-deploy/01-GATE.md, .planning/phases/01-spike-k-thu-t-ng-deploy/01-DEVICE-LOG.md, .planning/phases/01-spike-k-thu-t-ng-deploy/01-PLAYCANVAS-EVAL.md, .planning/STATE.md</files>
  <read_first>
    - every -opt- image in .planning/phases/01-spike-k-thu-t-ng-deploy/evidence/ (open each with the Read tool)
    - 01-OPTIMIZATION.md, 01-GATE.md, 01-DEVICE-LOG.md, .planning/phases/01-spike-k-thu-t-ng-deploy/01-19-PLAN.md Task 3 (gate rules)
    - .planning/ROADMAP.md gate paragraph; .planning/PROJECT.md Key Decisions (engine choice)
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-RESEARCH.md "Standard Stack", "Alternatives Considered", "Performance budget"
  </read_first>
  <action>Implements the D-07 stop rule. Transcribe the -opt- screenshots into 01-DEVICE-LOG.md "## Measurement sessions" with the 01-19 rules (sha must equal the optimised sha, throttled or Low Power rows are INVALID → VERDICT_AFTER_OPT=REMEASURE and stop). Append to 01-GATE.md "## After D-07 pass" with GATE_ANDROID_FPS, GATE_ANDROID_OBJECTS (≥ 20 knocked or broken in the same Android row), GATE_IOS_SOAK (reuse the earlier result when it passed and was not re-run), GATE_FIRST_LOAD, and `VERDICT_AFTER_OPT=PASS|FAIL` (PASS only when all four pass).
On PASS: remove the "Phase 1 gate FAIL" blocker from STATE.md and add the decision line "Phase 1 gate passed after one D-07 pass (<sha>)".
On FAIL: do not attempt further optimisation. Write 01-PLAYCANVAS-EVAL.md with sections "Kết quả đo" (before/after table per device with screenshot links), "Nút thắt quan sát được" (draw calls, bodies, frame time, throttling, crash/reload evidence), "Đã thử" (the five levers and their effect), "PlayCanvas là ứng viên 2" (engine runtime and editor workflow, physics options and whether Rapier can still be used, reuse of the GLB/MP3 pipeline, iOS stability considerations, size budget impact, which modules would be rewritten versus kept — src/logic/* and scripts/deploy tooling are engine-independent), "Rủi ro và việc cần đo trước khi chuyển" (a minimal PlayCanvas spike measured with the same bench scenario on the same two devices) and "Khuyến nghị" (one recommendation). Every factual claim about PlayCanvas cites a source URL checked during this task; unverified claims are marked [ASSUMED]. Add the STATE.md blocker "STOP: Three.js + Rapier failed the Phase 1 gate after one D-07 pass — see 01-PLAYCANVAS-EVAL.md; Phase 2 must not start until the operator decides the stack". Do not modify ROADMAP.md or PROJECT.md decisions.</action>
  <verify>
    <automated>node -e "const fs=require('fs');const g=fs.readFileSync('.planning/phases/01-spike-k-thu-t-ng-deploy/01-GATE.md','utf8');const v=(g.match(/VERDICT_AFTER_OPT=(PASS|FAIL|REMEASURE)/)||[])[1];let ok=!!v&&g.includes('GATE_ANDROID_OBJECTS');if(v==='FAIL'){ok=ok&&fs.existsSync('.planning/phases/01-spike-k-thu-t-ng-deploy/01-PLAYCANVAS-EVAL.md')&&fs.readFileSync('.planning/STATE.md','utf8').includes('STOP: Three.js + Rapier failed')}console.log('FINAL',v||'MISSING',ok?'CONSISTENT':'INCONSISTENT');process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `FINAL PASS CONSISTENT`, `FINAL FAIL CONSISTENT` or `FINAL REMEASURE CONSISTENT` and exits 0
    - On FAIL: 01-PLAYCANVAS-EVAL.md contains the six section headings listed in the action and STATE.md contains the STOP blocker
    - On PASS: STATE.md no longer contains "Phase 1 gate FAIL"
  </acceptance_criteria>
  <done>The phase ends with a definitive, evidence-backed stack decision point.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evidence screenshots → project decision | Measurements decide whether the stack continues |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-21-01 | Repudiation | stack decision without evidence | mitigate | Same devices, same seed, sha-checked screenshots; INVALID rows force REMEASURE; PlayCanvas claims cite sources or are marked [ASSUMED] |
| T-01-21-02 | Tampering | silently continuing on a failing stack | mitigate | STATE.md STOP blocker on FAIL; ROADMAP/PROJECT decisions left to the operator |
</threat_model>

<verification>
- 01-GATE.md records either the not-needed/blocked exit or VERDICT_AFTER_OPT; STATE.md consistent with the outcome
</verification>

<success_criteria>
- D-07 honoured exactly: pass, or documented STOP with an evaluation
</success_criteria>

## Artifacts this phase produces

- Files: `01-GATE.md` sections `## Optimisation pass: not needed (gate PASS)`, `## After D-07 pass: blocked (...)` or `## After D-07 pass` (keys `GATE_ANDROID_FPS`, `GATE_ANDROID_OBJECTS`, `GATE_IOS_SOAK`, `GATE_FIRST_LOAD`, `VERDICT_AFTER_OPT`), `01-PLAYCANVAS-EVAL.md` (only on a second failure)
- Guard markers: `PLAN_EXIT_NOT_NEEDED`, `PLAN_EXIT_BLOCKED`, `GUARD_CONTINUE`
- STATE.md lines: "Phase 1 gate passed after one D-07 pass" or "STOP: Three.js + Rapier failed the Phase 1 gate after one D-07 pass"

<output>
Create `.planning/phases/01-spike-k-thu-t-ng-deploy/01-21-SUMMARY.md` when done (status skipped on either PLAN_EXIT path)
</output>
