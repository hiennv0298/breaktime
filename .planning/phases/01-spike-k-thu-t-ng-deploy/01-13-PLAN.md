---
phase: 01-spike-k-thu-t-ng-deploy
plan: 13
type: execute
wave: 5
depends_on: ["01-09", "01-05"]
files_modified:
  - src/logic/sfxNames.ts
  - src/audio/sfx.ts
  - src/main.ts
  - tests/unit/sfxNames.test.ts
  - tests/e2e/audio.spec.ts
autonomous: true
requirements: [TECH-01, TECH-06]
must_haves:
  truths:
    - "Audio unlocks synchronously inside the Chơi tap (AudioContext.resume + a 1-sample silent buffer) and a short start cue plays, so the operator can confirm sound on iPhone Safari right after tapping (D-15, D-23, TECH-01)"
    - "All SFX are the MP3 files produced by the ffmpeg-static pipeline, fetched same-origin during loading and decoded after unlock; no OGG, no HTMLAudioElement, no library (D-15, D-25)"
    - "The audio context suspends when the tab hides and resumes on return or on the next pointerdown after an 'interrupted' state; the silent-switch preference is never overridden (D-15)"
    - "With ?autoplay=1 (no gesture) audio stays locked, play requests are counted but nothing throws (D-23)"
    - "SFX naming and variant selection are pure and unit-tested (TECH-06)"
  artifacts:
    - path: "src/audio/sfx.ts"
      provides: "Web Audio unlock, preload, play, debug state"
      exports: ["unlockFromGesture", "sfxLoadTask", "playSfx", "audioState"]
    - path: "src/logic/sfxNames.ts"
      provides: "Pure SFX name and variant helpers"
      exports: ["sfxNameFromPath", "variantsOf", "pickVariant"]
    - path: "tests/e2e/audio.spec.ts"
      provides: "Unlock on Chơi, locked under autoplay, suspend on hide"
  key_links:
    - from: "src/main.ts"
      to: "src/audio/sfx.ts"
      via: "onPlayGesture(unlockFromGesture) runs inside the click handler"
      pattern: "onPlayGesture(unlockFromGesture"
    - from: "src/main.ts"
      to: "src/audio/sfx.ts"
      via: "sfxLoadTask() appended to the loading tasks"
      pattern: "sfxLoadTask"
---

## Phase Goal

**As a** solo operator play-testing on my two reference phones, **I want to** open https://breaktime.doibung.com, walk a low-poly office, slap NPCs into ragdolls and smash 20+ objects in a build that any commit can deploy with one command, **so that** I have measured proof that Three.js + Rapier meets the size, fps and iOS-stability budget before any gameplay phase builds on it.

<objective>
Sound slice: tapping Chơi unlocks Web Audio on every platform and plays a short start cue; all MP3 SFX are preloaded and ready for the slap and smash slices.

Purpose: D-15 asks to check early that iOS plays sound only after the first tap; separating audio from the slap plan keeps each plan small and lets this run in parallel with the office and HUD slices.
Output: pure SFX name helpers with unit tests, raw Web Audio module, boot wiring, audio e2e.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-CONTEXT.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-RESEARCH.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-05-SUMMARY.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-09-SUMMARY.md
@src/main.ts
@src/boot/playGate.ts
@src/boot/loading.ts

<interfaces>
From earlier plans: `onPlayGesture`, `waitForPlay`, `LoadTask`, `fetchWithProgress`, `gameLoadTasks`, `registerDebug`; SFX files `src/assets/sfx/{slap-0,slap-1,slap-2,break-glass-0,break-glass-1,break-ceramic-0,break-ceramic-1,drop-wood-0,drop-wood-1,drop-soft-0,drop-metal-0}.mp3` (01-05).

Contracts this plan creates (plans 01-15 and 01-16 call them):

```typescript
// src/logic/sfxNames.ts
export function sfxNameFromPath(p: string): string;                  // '../assets/sfx/slap-1.mp3' → 'slap-1'
export function variantsOf(names: string[], prefix: string): string[]; // 'slap' → ['slap-0','slap-1','slap-2'] sorted
export function pickVariant(names: string[], prefix: string, rng: () => number): string | null;
// src/audio/sfx.ts
export function unlockFromGesture(): void;                            // synchronous resume + silent buffer, then decode all, then start cue
export function sfxLoadTask(): LoadTask;                              // id 'sfx', weight 5: fetch every MP3 as ArrayBuffer
export function playSfx(name: string, opts?: { gain?: number; rate?: number }): void;
export function sfxNames(): string[];
export function audioState(): 'locked' | 'suspended' | 'running' | 'interrupted' | 'closed';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SFX name helpers via TDD plus failing audio e2e</name>
  <files>src/logic/sfxNames.ts, tests/unit/sfxNames.test.ts, tests/e2e/audio.spec.ts</files>
  <read_first>
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-RESEARCH.md "Pattern 6", "Pitfall 5"
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-CONTEXT.md decisions D-15, D-23, D-25
    - tests/e2e/helpers.ts
  </read_first>
  <behavior>
    - sfxNameFromPath('../assets/sfx/slap-1.mp3') → 'slap-1'; sfxNameFromPath('/x/drop-soft-0.mp3') → 'drop-soft-0'
    - variantsOf(['drop-wood-1','slap-2','slap-0','slap-1'], 'slap') → ['slap-0','slap-1','slap-2']; variantsOf(names, 'break-glass') excludes 'break-ceramic-0'
    - pickVariant with rng returning 0 → first variant, 0.999 → last variant; unknown prefix → null
    - audio e2e (desktop, no autoplay, './?npcs=0'): click Chơi → within 1000 ms __bt.audio.state 'running', __bt.audio.decoded ≥ 10, __bt.audio.played includes 'drop-soft-0'
    - audio e2e (desktop, './?autoplay=1'): __bt.audio.state 'locked'; no pageerror
    - audio e2e: after Chơi, override document.visibilityState to 'hidden' and dispatch visibilitychange → __bt.audio.state 'suspended'; restore 'visible' and dispatch → 'running'
    - audio e2e: every MP3 request is same-origin and zero off-origin requests occur
  </behavior>
  <action>Implements the pure part of D-15 SFX handling. Write tests/unit/sfxNames.test.ts first, confirm failure, implement src/logic/sfxNames.ts per the interfaces block (no three, no DOM) until green. Then write tests/e2e/audio.spec.ts per the behavior list.</action>
  <verify>
    <automated>npx vitest run tests/unit/sfxNames.test.ts && npx vite build && echo UNIT_GREEN && { npx playwright test tests/e2e/audio.spec.ts --project=desktop; test $? -ne 0 && echo E2E_RED; }</automated>
  </verify>
  <acceptance_criteria>
    - Output contains UNIT_GREEN and E2E_RED
    - `cat src/logic/sfxNames.ts | grep -c "from 'three'\|document\."` prints 0
  </acceptance_criteria>
  <done>SFX naming is proven; audio unlock is a failing e2e.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Raw Web Audio module, preload task, unlock on Chơi with start cue (GREEN)</name>
  <files>src/audio/sfx.ts, src/main.ts</files>
  <read_first>
    - tests/e2e/audio.spec.ts, src/logic/sfxNames.ts
    - src/main.ts, src/boot/playGate.ts, src/boot/loading.ts
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-RESEARCH.md "Pattern 6", "Pitfall 5", "Alternatives Considered" (howler row)
  </read_first>
  <behavior>
    - All audio.spec tests pass; smoke, csp, camera and controls specs still pass
  </behavior>
  <action>Implements D-15 and D-23 audio unlock. sfx.ts per RESEARCH Pattern 6: module-level AudioContext created lazily; unlockFromGesture calls resume() and starts a 1-sample silent buffer synchronously (no await before those calls), then decodes all preloaded buffers and plays 'drop-soft-0' at gain 0.6 as the start cue; sfxLoadTask collects import.meta.glob('../assets/sfx/*.mp3', { query: '?url', import: 'default', eager: true }), maps names with sfxNameFromPath on the glob keys, and fetches each file as ArrayBuffer with fetchWithProgress (same-origin, CSP connect-src 'self'); playSfx creates a BufferSource + GainNode per call with gain and playbackRate, records the name in a rolling list of the last 20 requests, and silently returns while the context is not running; document visibilitychange suspends when hidden and resumes when visible; a document pointerdown listener resumes when state is 'interrupted' or 'suspended' after unlock; never set navigator.audioSession.type; registerDebug('audio', () => ({ state, decoded, requests, played })). No HTMLAudioElement, no howler, no OGG.
main.ts: append sfxLoadTask() to the loading task list next to gameLoadTasks(caps), and register onPlayGesture(unlockFromGesture) before waitForPlay (next to the fullscreen registration from 01-09). Do not edit src/game/assets.ts or src/game/game.ts (plans 01-10 and 01-11 run in this wave).</action>
  <verify>
    <automated>npm run typecheck && npx vitest run && npx vite build && npx playwright test tests/e2e/audio.spec.ts tests/e2e/smoke.spec.ts tests/e2e/csp.spec.ts tests/e2e/camera.spec.ts tests/e2e/controls.spec.ts</automated>
    <human-check>
      <test>On the iPhone (Safari, silent switch OFF) and the Android phone open the deployed URL and tap Chơi; switch apps and come back</test>
      <expected>A soft thud right after Chơi; nothing plays before the tap; sound still works after returning to the tab</expected>
      <why_human>iOS audio unlock and the silent switch only behave truly on a real iPhone</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0 with no " failed"
    - `cat src/audio/sfx.ts | grep -c "audioSession"` prints 0; `grep -c "new Audio(" -r src` prints 0; `grep -c "\.ogg" -r src` prints 0
    - `cat src/main.ts | grep -n "onPlayGesture(unlockFromGesture"` returns a line
    - After the task commit, `git show --name-only --format= HEAD | grep -cE 'game/assets.ts|game/game.ts|game/loop.ts'` prints 0
  </acceptance_criteria>
  <done>Sound unlocks on the Chơi tap on every platform and all SFX are ready for gameplay slices.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page → audio output | Browser autoplay/gesture policy |
| bundled MP3 files → decoder | Committed assets decoded by the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13-01 | Information disclosure | audio/media capture | mitigate | Output only; no microphone APIs used; Caddy Permissions-Policy denies microphone (01-06) |
| T-01-13-02 | Denial of service | audio node leaks | mitigate | Source/gain nodes are per play and garbage-collected after ended; buffers decoded once |
| T-01-13-03 | Tampering | asset loading | accept | Same-origin committed MP3s from the 01-05 pipeline; CSP blocks other origins |
</threat_model>

<verification>
- `npm run typecheck && npx vitest run && npx vite build && npx playwright test tests/e2e/audio.spec.ts tests/e2e/smoke.spec.ts tests/e2e/csp.spec.ts tests/e2e/camera.spec.ts tests/e2e/controls.spec.ts` exits 0
</verification>

<success_criteria>
- Web Audio unlock on Chơi proven headless and ready for the iPhone check; SFX preloaded as MP3
</success_criteria>

## Artifacts this phase produces

- Symbols: `sfxNameFromPath`, `variantsOf`, `pickVariant`, `unlockFromGesture`, `sfxLoadTask`, `playSfx`, `sfxNames`, `audioState`
- Load task: `sfx` (weight 5)
- `window.__bt` key: `audio` (`state`, `decoded`, `requests`, `played`)
- Start cue: `drop-soft-0` at gain 0.6 after unlock
- Tests: `tests/unit/sfxNames.test.ts`, `tests/e2e/audio.spec.ts`

<output>
Create `.planning/phases/01-spike-k-thu-t-ng-deploy/01-13-SUMMARY.md` when done
</output>
