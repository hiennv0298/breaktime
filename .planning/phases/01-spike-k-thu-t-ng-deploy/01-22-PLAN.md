---
phase: 01-spike-k-thu-t-ng-deploy
plan: 22
type: execute
wave: 9
depends_on: ["01-16", "01-08", "01-09"]
files_modified:
  - src/logic/keyMap.ts
  - tests/unit/keyMap.test.ts
  - src/input/keyboard.ts
  - src/input/cameraKeys.ts
  - src/input/inputState.ts
  - src/ui/debugHud.ts
  - tests/e2e/controls.spec.ts
  - tests/e2e/camera.spec.ts
autonomous: true
requirements: [CTRL-01, CTRL-04, TECH-06]
must_haves:
  truths:
    - "On desktop the arrow keys and WASD both move the player camera-relatively; holding D and ArrowRight together is not faster than one of them (D-27, CTRL-01)"
    - "Space is the action key and does exactly what E does (push the glowing prop, slap the glowing coworker); Space never pauses and never scrolls the page (D-27, D-18 revised, CTRL-01, CTRL-04)"
    - "Escape, or Ctrl pressed and released with no other key or click in between, opens the pause/settings menu and closes it again; the mobile pause button keeps working (D-27, D-20 revised, CTRL-04)"
    - "Any key pressed while Ctrl, Meta or Alt is held is ignored by the game and never default-prevented, so browser shortcuts (Ctrl+W, Ctrl+R, Ctrl+Z, Cmd+R) keep working and Ctrl+Z does not rotate the camera (D-27)"
    - "Camera rotation is Z / C only; arrow keys never rotate the camera and E never rotates it (D-19 revised, CTRL-05 unchanged)"
    - "Keys typed into a text input (the NPC name fields of plan 01-27) never move, act, rotate, toggle the debug HUD or pause; only Escape still closes the menu (D-29 groundwork)"
    - "The key bindings and the hint texts live in one pure module with Vitest coverage (TECH-06)"
  artifacts:
    - path: "src/logic/keyMap.ts"
      provides: "Pure D-27 key map: classifyKey, axisFromHeld, createCtrlTap, isTypingTarget, KEY_HINTS"
      exports: ["classifyKey", "axisFromHeld", "createCtrlTap", "isTypingTarget", "KEY_HINTS", "KeyIntent"]
    - path: "tests/unit/keyMap.test.ts"
      provides: "Key map, ctrl-tap and typing-guard cases"
    - path: "src/input/keyboard.ts"
      provides: "Arrows/WASD move, Space/E action, Escape + lone Ctrl pause, modifier and typing guards"
      contains: "createCtrlTap"
  key_links:
    - from: "src/input/keyboard.ts"
      to: "src/logic/keyMap.ts"
      via: "classifyKey / axisFromHeld / createCtrlTap"
      pattern: "classifyKey\\("
    - from: "src/input/cameraKeys.ts"
      to: "src/logic/keyMap.ts"
      via: "rotate intents only (rotate-left / rotate-right)"
      pattern: "rotate-left"
    - from: "src/ui/debugHud.ts"
      to: "src/logic/keyMap.ts"
      via: "isTypingTarget guard on Backquote"
      pattern: "isTypingTarget"
---

## Phase Goal

**As a** solo operator play-testing on my two reference phones, **I want to** open https://game.doibung.com, walk a low-poly office, slap NPCs into ragdolls and smash 20+ objects in a build that any commit can deploy with one command, **so that** I have measured proof that Three.js + Rapier meets the size, fps and iOS-stability budget before any gameplay phase builds on it.

<objective>
Key map slice (operator play-test 15/09/2026): after this plan a desktop player walks with the arrow keys or WASD, hits with Space (E still works), opens settings with Ctrl or Esc, and rotates the camera with Z / C only, without the game ever eating a browser shortcut or a character typed into a text field.

Purpose: D-27 replaces D-20; D-18 and D-19 are revised; CTRL-01 and CTRL-04 were reworded on 15/09/2026. Plans 01-24 (swing), 01-25 (key hint panel) and 01-27 (NPC name inputs) build on this map.
Output: pure key map with unit tests, rewired keyboard / camera keys / debug HUD toggle, and the e2e specs that encoded the old bindings updated with a stated reason.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-08-SUMMARY.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-09-SUMMARY.md
@src/input/keyboard.ts
@src/input/cameraKeys.ts
@src/input/inputState.ts
@src/ui/debugHud.ts

<interfaces>
Existing (do not change their contracts): `InputState { moveX, moveY, interactQueued, pauseToggleQueued }`, `consumeInteract`, `consumePauseToggle` (loop.ts consumes it once per frame and toggles `PauseState`), `attachKeyboard(s) -> detach`, `attachCameraKeys() -> detach`, `rotateCamera(dir: -1 | 1)`, `createDebugHud(initialVisible)` with its Backquote listener, `__bt.paused`, `__bt.pauseReason`, `__bt.camera.targetYawDeg`, `__bt.player.pos`, `__bt.box.pos`, `__bt.interactCount`.

Contracts this plan creates (pure, no three.js, no DOM globals):

```typescript
// src/logic/keyMap.ts
export type KeyIntent = 'up' | 'down' | 'left' | 'right' | 'action' | 'pause' | 'ctrl' | 'rotate-left' | 'rotate-right' | null;
export interface KeyMods { ctrlKey: boolean; metaKey: boolean; altKey: boolean }
export function classifyKey(code: string, mods: KeyMods): KeyIntent;
export function axisFromHeld(held: ReadonlySet<string>): { x: -1 | 0 | 1; y: -1 | 0 | 1 };
export interface CtrlTap { down(code: string, repeat: boolean): void; up(code: string): boolean; interrupt(): void; reset(): void }
export function createCtrlTap(): CtrlTap;
export function isTypingTarget(t: unknown): boolean;
export const KEY_HINTS: ReadonlyArray<{ keys: string; label: string }>;   // rendered by plan 01-25
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure D-27 key map via TDD, and the e2e specs rewritten to the new bindings (RED)</name>
  <files>src/logic/keyMap.ts, tests/unit/keyMap.test.ts, tests/e2e/controls.spec.ts, tests/e2e/camera.spec.ts</files>
  <read_first>
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-CONTEXT.md decisions D-18, D-19, D-20 (revised) and D-27
    - .planning/REQUIREMENTS.md CTRL-01, CTRL-04 (reworded 15/09/2026)
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-RESEARCH.md "Pitfall 10" (key collision) and "Anti-Patterns to Avoid"
    - src/input/keyboard.ts, src/input/cameraKeys.ts (the old bindings being replaced)
    - tests/e2e/controls.spec.ts (test 'Escape and Space toggle pause, freeze the sim and never scroll'), tests/e2e/camera.spec.ts (test 'Z / C and arrow aliases rotate the target yaw by exactly 90°, E never rotates'), tests/e2e/helpers.ts
  </read_first>
  <behavior>
    - classifyKey with no modifiers: KeyW and ArrowUp → 'up'; KeyS and ArrowDown → 'down'; KeyA and ArrowLeft → 'left'; KeyD and ArrowRight → 'right'; Space and KeyE → 'action'; Escape → 'pause'; ControlLeft and ControlRight → 'ctrl'; KeyZ → 'rotate-left'; KeyC → 'rotate-right'; KeyQ, Backquote, Tab, Enter → null
    - classifyKey returns null for every code above when ctrlKey, metaKey or altKey is true (Ctrl+KeyW, Ctrl+KeyR, Ctrl+KeyZ, Meta+KeyR, Alt+ArrowLeft, Ctrl+Space, Ctrl+Escape), except ControlLeft/ControlRight which stay 'ctrl' when only ctrlKey is true (a Control keydown reports ctrlKey true) and become null with metaKey or altKey
    - axisFromHeld: {KeyD} → x 1; {KeyD, ArrowRight} → x 1 (no double speed); {KeyA, KeyD} → x 0; {ArrowUp} → y 1; {KeyS, ArrowUp} → y 0; empty → {0, 0}; unrelated codes ignored
    - createCtrlTap: down ControlLeft then up ControlLeft → up returns true; down ControlRight / up ControlRight → true; down Control, down KeyR, up Control → false; down Control, down ShiftLeft, up Control → false; down Control, five repeat downs of Control, up → true; down Control, interrupt(), up → false; down KeyW (held movement), down Control, up Control → true; up Control without a down → false; down Control, up KeyW, up Control → true; after reset() an up Control returns false; a second tap after a returned true works again
    - isTypingTarget: { tagName: 'INPUT' } and { tagName: 'INPUT', type: 'text' } → true; { tagName: 'INPUT', type: 'button' | 'checkbox' | 'range' } → false; { tagName: 'TEXTAREA' } and { tagName: 'SELECT' } → true; { isContentEditable: true } → true; { tagName: 'BUTTON' }, { tagName: 'DIV' }, null, undefined, 42 → false
    - KEY_HINTS is exactly four rows in this order: { keys '←↑→↓', label 'đi' }, { keys 'Space', label 'đánh' }, { keys 'Ctrl/Esc', label 'settings' }, { keys 'Z/C', label 'xoay' }
    - controls.spec desktop, new test 'arrow keys move like WASD and never rotate the camera': ArrowUp held 1000 ms moves the player ≥ 1.0 m toward world -Z; ArrowRight held 800 ms moves it ≥ 0.6 m toward +X; __bt.camera.targetYawDeg stays 0
    - controls.spec desktop, new test 'Space near box pushes it like E': walk to the test box as in 'E near box pushes it', press Space → interactCount 1 and the box moves ≥ 0.3 m; __bt.paused stays false
    - controls.spec pause, the old test is replaced by 'Escape and a lone Ctrl toggle the menu; Space never pauses; Ctrl combos are not swallowed' (reason in a comment: D-27 / CTRL-04 reworded 15/09/2026, Space no longer pauses): Escape → paused with reason 'user', menu 'Tạm dừng' visible and the sim frozen → Escape → resumed; press Control → paused → press Control → resumed; ControlRight does the same; Space → after 300 ms still not paused, its keydown was default-prevented and scrollY is 0; keyboard.down Control + press KeyZ + up Control → not paused, camera targetYawDeg still 0, and the KeyZ keydown carrying ctrlKey was NOT default-prevented; keyboard.down Control + mouse click at (640, 200) + up Control → not paused; keyboard.down Control + ArrowUp held 500 ms + up both → player moved < 0.05 m and not paused; the '#pause-resume' button still resumes
    - camera.spec desktop, the old arrow-alias test becomes 'Z / C rotate by exactly 90°; arrow keys and E never rotate' (reason in a comment: D-19 revised 15/09/2026): KeyC → 90, KeyZ → 0, ArrowLeft press → targetYawDeg stays 0 after 500 ms, ArrowRight press → stays 0, KeyE → stays 0
    - every changed test still asserts zero console errors and zero off-origin requests
  </behavior>
  <action>Implements D-27 as data, and records D-18 / D-19 / D-20 revisions in the tests. Write tests/unit/keyMap.test.ts first and confirm it fails (module missing), then implement src/logic/keyMap.ts until green: classifyKey uses KeyboardEvent.code values only (layout independent, as keyboard.ts already does), a fixed Record for movement (KeyW/ArrowUp, KeyS/ArrowDown, KeyA/ArrowLeft, KeyD/ArrowRight), ACTION codes Space and KeyE, PAUSE code Escape, CTRL codes ControlLeft and ControlRight, ROTATE codes KeyZ and KeyC; modifier rule exactly as in the behavior list. createCtrlTap keeps two booleans (controlHeld, armed): a non-repeat Control down sets both; any other code down while controlHeld clears armed; Control up returns armed and clears both; interrupt clears armed; reset clears both. isTypingTarget inspects tagName, type and isContentEditable on an unknown value without touching DOM globals (so it runs in Node). KEY_HINTS holds the four D-28 rows used by plan 01-25. No three.js import, no Math.random. Then edit the two e2e specs exactly as the behavior list says; keep every other test in both files byte-identical and extend the local Bt types (camera in controls.spec) only as needed. Confirm the rewritten e2e tests fail against the current build (Space still pauses, arrows still rotate, Ctrl does nothing).</action>
  <verify>
    <automated>npx vitest run tests/unit/keyMap.test.ts && echo UNIT_GREEN && npx vite build && echo BUILD_OK && { npx playwright test tests/e2e/controls.spec.ts tests/e2e/camera.spec.ts --project=desktop; test $? -ne 0 && echo E2E_RED; }</automated>
  </verify>
  <acceptance_criteria>
    - Output contains UNIT_GREEN, BUILD_OK and E2E_RED; the Playwright failures name the new or rewritten tests (not "No tests found")
    - `grep -vE "^\s*(//|\*)" src/logic/keyMap.ts | grep -cE "from 'three'|Math\.random|document\.|window\."` prints 0
    - tests/e2e/controls.spec.ts contains "Space never pauses" and "D-27"; tests/e2e/camera.spec.ts contains "arrow keys and E never rotate" and "D-19"
    - `grep -c "Escape and Space toggle pause" tests/e2e/controls.spec.ts` prints 0
  </acceptance_criteria>
  <done>The new key map is proven in Node, and the browser tests describe the new bindings and currently fail.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire keyboard, camera keys and the debug HUD toggle to the key map (GREEN)</name>
  <files>src/input/keyboard.ts, src/input/cameraKeys.ts, src/input/inputState.ts, src/ui/debugHud.ts</files>
  <read_first>
    - src/logic/keyMap.ts, tests/unit/keyMap.test.ts, tests/e2e/controls.spec.ts, tests/e2e/camera.spec.ts
    - src/input/keyboard.ts, src/input/cameraKeys.ts, src/input/inputState.ts, src/ui/debugHud.ts
    - src/game/loop.ts (consumePauseToggle and the interact drop while paused, unchanged), src/input/touchButtons.ts (pause button unchanged)
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-08-SUMMARY.md decisions (Space preventDefault on keydown and keyup, tabIndex -1 buttons)
  </read_first>
  <behavior>
    - All keyMap unit tests, controls.spec and camera.spec pass; the full `npx playwright test` stays green (hud.spec still opens the menu with Escape; slap, room and breakables specs still act with KeyE)
  </behavior>
  <action>Implements D-27 in the browser (D-18 / D-19 / D-20 revised).
keyboard.ts: one CtrlTap per attach. keydown handler order: (1) if isTypingTarget(e.target) and e.code is not 'Escape', return without touching anything; (2) ctrlTap.down(e.code, e.repeat); (3) intent = classifyKey(e.code, e); movement intents add the code to the held set and recompute moveX/moveY from axisFromHeld (preventDefault only for the four Arrow codes, so the page never scrolls); 'action' sets interactQueued when not repeat and calls preventDefault when the code is Space; 'pause' sets pauseToggleQueued when not repeat; 'ctrl' and null do nothing and never preventDefault; rotate intents are ignored here. keyup handler: always delete the code from held and recompute (so no key sticks even if focus moved into an input); when the target is not a typing target, preventDefault a Space keyup (a focused menu button would otherwise activate) and set pauseToggleQueued when ctrlTap.up(e.code) returns true. A window pointerdown listener (capture) and a wheel listener (passive) call ctrlTap.interrupt(); blur and a hidden tab clear held keys and call ctrlTap.reset(). The detach function removes every listener it added. Update the file comment: arrows/WASD move, Space/E action, Escape or a lone Ctrl pause, modifiers and typing ignored.
cameraKeys.ts: rotate only on classifyKey intents 'rotate-left' (-1) and 'rotate-right' (+1), skip repeats and typing targets, no preventDefault; remove ArrowLeft/ArrowRight entirely (D-19 revised).
debugHud.ts: the Backquote toggle ignores repeats, events with ctrlKey/metaKey/altKey and typing targets (import isTypingTarget).
inputState.ts: comments only — interactQueued is set by Space / E / context button / click, pauseToggleQueued by Escape / lone Ctrl / pause button.
Do not change loop.ts, touchButtons.ts or joystick.ts.</action>
  <verify>
    <automated>npm run typecheck && echo TYPECHECK_OK && npx vitest run && echo UNIT_OK && npx vite build && echo BUILD_OK && npx playwright test && echo E2E_OK</automated>
  </verify>
  <acceptance_criteria>
    - Output contains TYPECHECK_OK, UNIT_OK, BUILD_OK and E2E_OK, and the Playwright output has no line containing " failed"
    - `grep -vE "^\s*(//|\*)" src/input/cameraKeys.ts | grep -c "Arrow"` prints 0
    - `grep -vE "^\s*(//|\*)" src/input/keyboard.ts | grep -c "'Space'"` prints at most 1 and `grep -c "classifyKey(" src/input/keyboard.ts` prints at least 1
    - `grep -c "createCtrlTap" src/input/keyboard.ts` prints at least 1 and `grep -c "isTypingTarget" src/ui/debugHud.ts` prints at least 1
    - `grep -rnE "innerHTML|outerHTML|insertAdjacentHTML" src | grep -vE ":[0-9]+:\s*(//|\*)" | wc -l` prints 0
  </acceptance_criteria>
  <done>Desktop players move with arrows or WASD, hit with Space or E, open settings with Esc or a lone Ctrl, rotate with Z / C only, and browser shortcuts plus typed text are never hijacked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard events → game input | Every key press on the page, including browser shortcuts and text typed into inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-22-01 | Denial of service | browser shortcuts (Ctrl+W/R/T, Cmd+R) swallowed by preventDefault | mitigate | classifyKey returns null for any code with ctrlKey/metaKey/altKey; keyboard.ts only preventDefaults Space and Arrow codes after classification; e2e asserts a Ctrl+KeyZ keydown is not default-prevented |
| T-01-22-02 | Tampering | accidental pause from Ctrl used in a combo | mitigate | createCtrlTap fires only on a lone press+release; any other key, pointerdown, wheel or blur disarms it; unit + e2e cases |
| T-01-22-03 | Tampering | text typed into an input driving game actions (moves, rotation, HUD toggle, pause) | mitigate | isTypingTarget guard in keyboard.ts, cameraKeys.ts and debugHud.ts (only Escape passes); plan 01-27 adds the in-browser typing e2e |
| T-01-22-04 | Denial of service | stuck held key after focus moves into an input | mitigate | keyup always removes the code from the held set; blur and hidden tab clear all keys |
</threat_model>

<verification>
- `npm run typecheck && npx vitest run && npx vite build && npx playwright test` exits 0 with the four markers
</verification>

<success_criteria>
- CTRL-01 / CTRL-04 desktop wording (15/09/2026) is true in the build: arrows + WASD move, Space acts, Esc or lone Ctrl opens settings, Space never pauses, Z / C rotate, no browser shortcut is swallowed
</success_criteria>

## Artifacts this phase produces

- Symbols: `KeyIntent`, `KeyMods`, `classifyKey`, `axisFromHeld`, `CtrlTap`, `createCtrlTap`, `isTypingTarget`, `KEY_HINTS`
- Key bindings (KeyboardEvent.code): move `KeyW/KeyA/KeyS/KeyD` + `ArrowUp/ArrowDown/ArrowLeft/ArrowRight`; action `Space`, `KeyE`; pause `Escape`, lone `ControlLeft`/`ControlRight`; rotate `KeyZ`, `KeyC`; debug HUD `Backquote`
- Tests: `tests/unit/keyMap.test.ts`; changed e2e `tests/e2e/controls.spec.ts` (new 'arrow keys move like WASD…', 'Space near box pushes it like E', replaced pause test), `tests/e2e/camera.spec.ts` (arrow alias test replaced)

<output>
Create `.planning/phases/01-spike-k-thu-t-ng-deploy/01-22-SUMMARY.md` when done
</output>
