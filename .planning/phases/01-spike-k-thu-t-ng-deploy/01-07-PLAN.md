---
phase: 01-spike-k-thu-t-ng-deploy
plan: 07
type: execute
wave: 4
depends_on: ["01-06", "01-04"]
files_modified:
  - scripts/deploy.mjs
  - scripts/lib/dnsCheck.mjs
  - scripts/lib/smoke.mjs
  - scripts/lib/tools.mjs
  - tests/unit/smoke.test.mjs
  - tests/unit/dnsCheck.test.mjs
autonomous: true
requirements: [PLAT-01]
must_haves:
  truths:
    - "`npm run deploy` runs in order: clean-tree + sha guard → ssh preflight → drift check → typecheck/build → precompress → size gate → vitest → playwright → DNS preflight → doibung.com poller → tar-stream upload → atomic activate → validate+reload site file when changed → cleanup keeping 10 → smoke of breaktime, doibung.com and www; any failing step stops with a non-zero exit (D-03, D-24)"
    - "The drift check runs before any build or upload; on failure it prints the Vietnamese re-apply instruction and exits 2 (D-02)"
    - "Upload is a tar stream over Windows OpenSSH; the uploaded file count must equal the local dist file count, and every step checks rc + marker + non-empty output (D-03, RESEARCH C4, Pitfall 7)"
    - "doibung.com is polled every second from upload to smoke and the deploy fails if any response was not 200 (PLAT-01)"
    - "Smoke checks use default TLS verification: breaktime / and /b/<sha>/ 200 with the new sha, 308 on missing slash, 404 on a bad sha, immutable hashed asset, doibung.com 200, www 301/308 to doibung.com (PLAT-02, D-04)"
  artifacts:
    - path: "scripts/deploy.mjs"
      provides: "One-command deploy orchestration"
      contains: "DEPLOY_OK"
    - path: "scripts/lib/smoke.mjs"
      provides: "Pure response evaluation + live smoke runner"
      exports: ["evaluateResponse", "runSmoke"]
    - path: "scripts/lib/dnsCheck.mjs"
      provides: "Multi-resolver DNS preflight"
      exports: ["checkDns", "evaluateDnsResults"]
  key_links:
    - from: "scripts/deploy.mjs"
      to: "scripts/lib/stateCheck.mjs"
      via: "drift check before build"
      pattern: "evaluateState"
    - from: "scripts/deploy.mjs"
      to: "deploy/remote/release.sh"
      via: "sshScript activate and cleanup"
      pattern: "release.sh"
    - from: "scripts/deploy.mjs"
      to: "scripts/lib/poller.mjs"
      via: "doibung.com poller spans upload → smoke"
      pattern: "startPoller"
---

## Phase Goal

**As a** solo operator play-testing on my two reference phones, **I want to** open https://game.doibung.com, walk a low-poly office, slap NPCs into ragdolls and smash 20+ objects in a build that any commit can deploy with one command, **so that** I have measured proof that Three.js + Rapier meets the size, fps and iOS-stability budget before any gameplay phase builds on it.

<objective>
Walking-skeleton slice 4 (one command): `npm run deploy` orchestrates every D-03 gate in order using the libraries from 01-04 and 01-06. It is verified locally in this plan (unit tests, a dirty-tree negative control, and `--dry-run`, which stops at the drift check or right before upload); the first real deploy happens in plan 01-12.

Purpose: PLAT-01 one-command deploy that cannot interrupt doibung.com and refuses to run on a drifted server or a dirty tree.
Output: `scripts/deploy.mjs`, DNS/smoke/tool helpers with unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-CONTEXT.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-RESEARCH.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-04-SUMMARY.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-06-SUMMARY.md
@scripts/lib/run.mjs
@scripts/lib/stateCheck.mjs
@scripts/lib/poller.mjs
@scripts/lib/releases.mjs
@scripts/lib/sizeGate.mjs

<interfaces>
From plan 01-04: `run`, `ssh`, `sshScript`, `SSH`, `TAR`; `parseStateCheck`, `evaluateState`, `DRIFT_MESSAGE`; `startPoller`. From plan 01-06: `validateSha`, `parseReleaseListing`, `selectReleasesToDelete`; scripts `precompress.mjs`, `size-report.mjs --gate`; remote scripts `deploy/remote/release.sh`, `deploy/remote/activate-site.sh`; site file `deploy/caddy/breaktime.caddy`.

Contracts this plan creates:

```javascript
// scripts/lib/dnsCheck.mjs
export function evaluateDnsResults(results: Record<string, string[] | string>, expectedIp: string): boolean;   // pure: every server returned expectedIp
export function checkDns(host, expectedIp, servers = ['1.1.1.1', '8.8.8.8']): Promise<{ ok: boolean, results: Record<string, string[]|string> }>;
// scripts/lib/smoke.mjs
export function evaluateResponse(check: { name, expectStatus: number[], headers?: Record<string, RegExp>, bodyIncludes?: string[], location?: RegExp }, res: { status, headers, body, location }): { name, ok: boolean, detail: string };
export function runSmoke({ sha, firstActivation }): Promise<{ name, ok, detail }[]>;
// scripts/lib/tools.mjs
export function nodeBin(pkg, binName): string;  // absolute JS entry from node_modules/<pkg>/package.json bin
export const GIT: string;                       // 'git' or 'C:/Program Files/Git/cmd/git.exe' fallback
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tools, DNS preflight and smoke helpers (TDD for the pure parts)</name>
  <files>scripts/lib/tools.mjs, scripts/lib/dnsCheck.mjs, scripts/lib/smoke.mjs, tests/unit/smoke.test.mjs, tests/unit/dnsCheck.test.mjs</files>
  <read_first>
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-RESEARCH.md sections "Pattern 12" (first site activation, Let's Encrypt limit), "Pattern 13" (smoke list, Git Bash notes), "Pitfall 7"
    - scripts/lib/run.mjs (spawn conventions to reuse)
  </read_first>
  <behavior>
    - evaluateResponse: status mismatch → ok false with detail naming expected statuses; header regex mismatch → ok false naming the header; location mismatch → ok false; body missing a required substring → ok false; all rules satisfied → ok true
    - evaluateDnsResults: {'1.1.1.1':['187.53.128.67'],'8.8.8.8':['187.53.128.67']} → true; one server returning 'ENOTFOUND' string → false; one server returning a different IP only → false; empty object → false
  </behavior>
  <action>Implements D-03 helper pieces. tools.mjs: nodeBin(pkg, binName) reads node_modules/<pkg>/package.json and returns the absolute bin entry (string or map form); GIT constant ('C:/Program Files/Git/cmd/git.exe' when 'git' cannot be spawned on win32).
dnsCheck.mjs: evaluateDnsResults pure; checkDns uses dns.promises.Resolver({ timeout: 5000, tries: 2 }) per server with setServers([server]) and resolve4(host), storing the address list or the error code string.
smoke.mjs: evaluateResponse pure; runSmoke uses fetch with redirect 'manual' and default TLS verification (never rejectUnauthorized or NODE_TLS_REJECT_UNAUTHORIZED) and returns results for: GET https://game.doibung.com/version.json → 200 and JSON sha equal to the deployed sha (retry every 3 s up to 120 s when firstActivation, else 20 s); GET / → 200, cache-control /no-cache/, x-content-type-options nosniff, body includes 'wasm-unsafe-eval'; GET /b/<sha>/ → 200; GET /b/<sha> → 308 with location ending /b/<sha>/; the first `assets/index-*.js` referenced in the / body → 200 with cache-control /immutable/; GET /b/zzz/ → 404; GET https://doibung.com/ → 200; GET https://www.doibung.com/ → 301 or 308 with location starting https://doibung.com. Write both test files first per the behavior list, confirm they fail, then implement.</action>
  <verify>
    <automated>npx vitest run tests/unit/smoke.test.mjs tests/unit/dnsCheck.test.mjs && node -e "import('./scripts/lib/dnsCheck.mjs').then(async m=>{const r=await m.checkDns('doibung.com','187.53.128.67');console.log('DNS_DOIBUNG',r.ok,JSON.stringify(r.results));process.exit(r.ok?0:1)})"</automated>
  </verify>
  <acceptance_criteria>
    - Vitest reports smoke.test.mjs and dnsCheck.test.mjs passing
    - The live lookup prints `DNS_DOIBUNG true` with 187.53.128.67 from both resolvers
    - `cat scripts/lib/smoke.mjs scripts/lib/dnsCheck.mjs scripts/lib/tools.mjs | grep -c "rejectUnauthorized\|NODE_TLS_REJECT_UNAUTHORIZED"` prints 0
  </acceptance_criteria>
  <done>DNS and smoke evaluation are tested and the live resolver path works.</done>
</task>

<task type="auto">
  <name>Task 2: deploy.mjs orchestration with poller, upload, activation, cleanup, smoke, and local negative controls</name>
  <files>scripts/deploy.mjs</files>
  <read_first>
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-RESEARCH.md sections "System Architecture Diagram", "Pattern 12", "Pattern 13" (all bullets)
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-CONTEXT.md decisions D-02, D-03, D-04, D-24
    - scripts/lib/run.mjs, scripts/lib/stateCheck.mjs, scripts/lib/poller.mjs, scripts/lib/releases.mjs, scripts/lib/sizeGate.mjs, scripts/lib/dnsCheck.mjs, scripts/lib/smoke.mjs, scripts/lib/tools.mjs, scripts/infra.mjs (reuse, do not duplicate)
  </read_first>
  <action>Implements D-03 (order of steps), D-24 (any failing check stops deploy), D-02 (drift check first) and D-04.
Flags: `--dry-run` stops right before the poller/upload and prints DRY_RUN_OK. No flag may skip tests or the drift check. Print `== STEP <n>/<N> <name>` before each step; stop at the first failure with exit 1 (drift uses 2).
(1) clean tree: `git status --porcelain -- src index.html package.json package-lock.json vite.config.ts tsconfig.json tsconfig.node.json vitest.config.ts playwright.config.ts deploy scripts assets-src/build-assets.mjs assets-src/asset-map.json CREDITS.md` must be empty (run with allowEmpty true, then assert length 0), else print "Cây làm việc chưa sạch — commit trước khi deploy" and exit 1; SHA = validateSha(`git rev-parse --short=12 HEAD`).
(2) `ssh('echo __SSH_OK__', { marker: '__SSH_OK__' })`.
(3) drift: sshScript state-check.sh, parseStateCheck, evaluateState(values, { requireSite: values.SITE_FILE_SHA !== '' }); on failure print failures + DRIFT_MESSAGE, exit 2. Never call infra:apply from deploy.
(4) typecheck: tsc `--noEmit -p tsconfig.json` and `-p tsconfig.node.json` via nodeBin('typescript','tsc') with allowEmpty true, then `tsc --noEmit -p tsconfig.json --listFilesOnly` output must include src/main.ts; vite build via nodeBin('vite','vite') with marker "built in".
(5) precompress, marker PRECOMPRESS_DONE. (6) size-report --gate, marker SIZE_GATE_OK.
(7) vitest run via nodeBin('vitest','vitest'): output must match /Test Files\s+\d+ passed/ and must not match / failed/.
(8) playwright test via nodeBin('@playwright/test','playwright'), timeoutMs 900000: output must contain " passed" and not " failed"; then size-report (no gate) printing first-load; require test-results/first-load.json mtime newer than the build start.
(9) checkDns('game.doibung.com', '187.53.128.67') must be ok. `--dry-run` stops here with DRY_RUN_OK.
(10) startPoller('https://doibung.com/', { intervalMs: 1000 }).
(11) upload: spawn TAR ['-czf','-','-C','dist','.'] piped into SSH with remote command `set -euo pipefail; B=/srv/sites/breaktime/releases; I="$B/.incoming-<SHA>"; rm -rf -- "$I"; mkdir -p -- "$I"; tar -xzf - --no-same-owner -C "$I"; test -s "$I/index.html"; chmod -R u=rwX,go=rX -- "$I"; echo "__UPLOADED__ $(find "$I" -type f | wc -l)"` (SHA already validated); both processes exit 0 and the uploaded count equals the local count of regular files in dist; on failure run release.sh abort.
(12) sshScript release.sh ['activate', SHA], marker `__ACTIVATED__ <SHA>`; parse the listing.
(13) site file: sha256 of deploy/caddy/breaktime.caddy vs SITE_FILE_SHA; when different or empty, `ssh('cat > /srv/sites/.breaktime.caddy.new && sha256sum /srv/sites/.breaktime.caddy.new', { input, marker: <local sha256> })`, then sshScript activate-site.sh, marker `__SITE_RELOADED__`.
(14) names = selectReleasesToDelete(listing, 10, SHA); when non-empty sshScript release.sh ['cleanup', SHA, ...names], marker `__CLEANUP__`.
(15) runSmoke({ sha: SHA, firstActivation: SITE_FILE_SHA was empty }); every result must be ok.
(16) stop the poller; non200 must be 0, else print samples and exit 1.
(17) re-run the state check with requireSite true. Print the summary (sizes, first-load total, poller count) and finally `DEPLOY_OK <SHA> https://game.doibung.com/ https://game.doibung.com/b/<SHA>/`.
Local negative controls (no upload in this plan): first commit this task's file (the clean-tree guard covers scripts/). Then create untracked src/__dirty_probe.ts, run `node scripts/deploy.mjs --dry-run` and confirm exit 1 with the clean-tree message before any ssh step, delete the probe; then run `node scripts/deploy.mjs --dry-run` on the clean tree — before plan 01-12 it must stop at step 3 with DRIFT_MESSAGE and exit 2 (after 01-12 it must reach DRY_RUN_OK).</action>
  <verify>
    <automated>npx vitest run && echo "export {}" > src/__dirty_probe.ts && { node scripts/deploy.mjs --dry-run; echo "DIRTY_RC=$?"; } ; rm -f src/__dirty_probe.ts ; { node scripts/deploy.mjs --dry-run; echo "DRY_RC=$?"; }</automated>
  </verify>
  <acceptance_criteria>
    - Output contains `DIRTY_RC=1` and the clean-tree message, with no `__SSH_OK__` during that run
    - The clean-tree run prints either (DRIFT_MESSAGE text and `DRY_RC=2`) or (`DRY_RUN_OK` and `DRY_RC=0`); neither run prints `__UPLOADED__`
    - `git status --porcelain src/__dirty_probe.ts` prints nothing
    - scripts/deploy.mjs contains "--dry-run", "DEPLOY_OK", "evaluateState", "selectReleasesToDelete", "checkDns", "startPoller", "runSmoke" and does not contain "infra:apply" as an executed command
    - `cat scripts/deploy.mjs | grep -c "rejectUnauthorized\|NODE_TLS_REJECT_UNAUTHORIZED"` prints 0
  </acceptance_criteria>
  <done>`npm run deploy` exists with every D-03 gate in order and refuses dirty trees and drifted servers; nothing was uploaded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local build → VPS (root over SSH) | Archive and scripts executed as root |
| deploy → shared doibung Caddy | Site reload affects doibung.com |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07-01 | Denial of service | doibung.com during deploy | mitigate | Drift check first; poller non200 must be 0; smoke asserts doibung.com 200 and www redirect; deploy never runs infra:apply |
| T-01-07-02 | Tampering | command injection via sha | mitigate | validateSha before any interpolation; only fixed script bodies over stdin |
| T-01-07-03 | Denial of service | Let's Encrypt failed-authorization lockout | mitigate | checkDns on 1.1.1.1 and 8.8.8.8 must return 187.53.128.67 before upload and site activation |
| T-01-07-04 | Spoofing | smoke against a MITM or wrong host | mitigate | Default TLS verification; acceptance greps for TLS overrides = 0 |
| T-01-07-05 | Repudiation | unknown deployed code | mitigate | Clean-tree guard maps every deploy to a commit sha; version.json sha verified by smoke |
| T-01-07-06 | Tampering | false success (rc=0 empty output) | mitigate | Every step via run() with rc + marker + output length; uploaded file count compared |
</threat_model>

<verification>
- Unit tests pass; dirty-tree and drift/dry-run controls behave as specified; no upload occurs
</verification>

<success_criteria>
- One command implements the full D-03 deploy order with fail-fast gates
</success_criteria>

## Artifacts this phase produces

- npm script wired: `deploy` (flag `--dry-run`)
- Files: `scripts/deploy.mjs`, `scripts/lib/dnsCheck.mjs` (`evaluateDnsResults`, `checkDns`), `scripts/lib/smoke.mjs` (`evaluateResponse`, `runSmoke`), `scripts/lib/tools.mjs` (`nodeBin`, `GIT`)
- Markers: `== STEP`, `DRY_RUN_OK`, `__SSH_OK__`, `__UPLOADED__`, `DEPLOY_OK`
- Tests: `tests/unit/smoke.test.mjs`, `tests/unit/dnsCheck.test.mjs`

<output>
Create `.planning/phases/01-spike-k-thu-t-ng-deploy/01-07-SUMMARY.md` when done
</output>
