---
phase: 01-spike-k-thu-t-ng-deploy
plan: 24
type: execute
wave: 10
depends_on: ["01-22", "01-23", "01-15"]
files_modified:
  - src/logic/swing.ts
  - tests/unit/swing.test.ts
  - tests/e2e/swing.spec.ts
  - src/render/characters.ts
  - src/game/player.ts
  - src/game/game.ts
  - src/input/pointerPick.ts
  - tests/e2e/controls.spec.ts
  - tests/e2e/camera.spec.ts
  - tests/e2e/room.spec.ts
  - tests/e2e/slap.spec.ts
  - tests/e2e/breakables.spec.ts
  - tests/e2e/hud.spec.ts
  - tests/e2e/characters.spec.ts
autonomous: true
requirements: [CTRL-01, CTRL-02, TECH-06]
must_haves:
  truths:
    - "Pressing Space, E, the mobile context button or left-clicking the game view always plays the player's arm-swing ('attack-melee-right') right away, even with nothing in range (D-30, CTRL-01)"
    - "The hit only happens when a target is in range: the glowing coworker is slapped into a ragdoll or the glowing prop is pushed; a click hits only when it lands on the glowing object, otherwise it just swings (D-30, D-18 revised, D-20 revised)"
    - "A short cooldown (350 ms) drops presses that arrive during it, so mashing cannot spam swings or hits (D-30)"
    - "On mobile the context button swings with or without a target while the other thumb holds the joystick (D-30, CTRL-02)"
    - "Clicks on HUD buttons, HUD panels ([data-hud-panel]) or the pause menu never swing (D-28 panel contract shared with plan 01-25)"
    - "The cooldown gate is a pure module with Vitest coverage (TECH-06)"
  artifacts:
    - path: "src/logic/swing.ts"
      provides: "Pure swing cooldown gate"
      exports: ["createSwingGate", "SWING_COOLDOWN_MS", "SwingGate"]
    - path: "tests/e2e/swing.spec.ts"
      provides: "Swing without target, cooldown, click swing, mobile context swing"
    - path: "src/game/player.ts"
      provides: "Player.swing() with clip restart and __bt.player.motion"
      contains: "swing("
  key_links:
    - from: "src/game/game.ts"
      to: "src/logic/swing.ts"
      via: "every action press passes the gate before swinging / hitting"
      pattern: "tryStart\\("
    - from: "src/input/pointerPick.ts"
      to: "src/game/game.ts"
      via: "onPick(id | null): null = click that did not hit the glowing object"
      pattern: "onPick\\(null\\)"
---

## Phase Goal

**As a** solo operator play-testing on my two reference phones, **I want to** open https://game.doibung.com, walk a low-poly office, slap NPCs into ragdolls and smash 20+ objects in a build that any commit can deploy with one command, **so that** I have measured proof that Three.js + Rapier meets the size, fps and iOS-stability budget before any gameplay phase builds on it.

<objective>
Swing slice (operator play-test 15/09/2026): after this plan every attack press gives instant feedback — the player swings immediately whether or not anything is in reach, and only a target in range gets slapped or pushed.

Purpose: D-30. Today E / context / click do nothing visible without a target (game.ts interactTarget returns early), which read as "the button is broken" on the live build.
Output: pure cooldown gate with tests, swing-first action flow in game.ts, clip restart in characters/player, click-anywhere swing in pointerPick, swing e2e.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-CONTEXT.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-15-SUMMARY.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-22-SUMMARY.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-23-SUMMARY.md
@src/game/game.ts
@src/game/player.ts
@src/render/characters.ts
@src/input/pointerPick.ts
@src/game/slap.ts

<interfaces>
Existing: game.ts fixedUpdate consumes `consumeInteract(input)` and `pickQueued` then calls `interactTarget()` only when there is a target; `performSlap(game, npc, nowMs)` (calls `game.player.slapAt(x, z)`, which faces the NPC and holds 'attack-melee-right' for 0.45 s); `props.push(id, dx, dz)` + `interactCount`; `attachPointerPick(canvas, camera, getHighlighted, onPick, isBlocked)` (mouse left button only, ignores `[data-hud-button], #pause-menu, button`, raycasts the highlighted object); `CharacterInstance.setMotion(name, fadeSec)` returns early when name is already current; loop.ts drops `interactQueued` while paused; `__bt.player { pos, yaw }`, `__bt.interactCount`, `__bt.slap.count`, `__bt.touchUi.contextIcon` ('none' with nothing in range). Space and E both set `interactQueued` since plan 01-22.

Contracts this plan creates:

```typescript
// src/logic/swing.ts
export const SWING_COOLDOWN_MS = 350;
export interface SwingGate { tryStart(nowMs: number): boolean; count(): number; lastMs(): number }
export function createSwingGate(cooldownMs?: number): SwingGate;
// src/render/characters.ts
// CharacterInstance.setMotion(name, fadeSec?, opts?: { restart?: boolean }); CharacterInstance.motion(): CharacterMotion
// src/game/player.ts
// Player.swing(towardX?: number, towardZ?: number): void   (slapAt(x, z) stays as the NPC-facing variant)
// src/input/pointerPick.ts
// onPick: (id: string | null) => void   — id when the ray hits the highlighted object, null for any other game-area click
// window.__bt.swing: { count: number; hits: number; lastMs: number; cooldownMs: number }
// window.__bt.player gains motion: string
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Swing cooldown gate via TDD, plus a failing swing e2e (RED)</name>
  <files>src/logic/swing.ts, tests/unit/swing.test.ts, tests/e2e/swing.spec.ts</files>
  <read_first>
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-CONTEXT.md decisions D-18 (revised), D-20 (revised), D-30
    - src/game/game.ts functions refreshTarget and interactTarget and the fixedUpdate interact block; src/game/player.ts slapAt
    - tests/e2e/controls.spec.ts (walk-to-box helper pattern), tests/e2e/slap.spec.ts, tests/e2e/room.spec.ts 'click only highlighted', tests/e2e/helpers.ts
  </read_first>
  <behavior>
    - createSwingGate(): tryStart(1000) true; tryStart(1100) false; tryStart(1349) false; tryStart(1350) true; count() 2; lastMs() 1350; tryStart(NaN) false and does not change count; createSwingGate(0) accepts every call; a negative or NaN cooldown falls back to SWING_COOLDOWN_MS
    - swing.spec desktop 'Space with nothing in range still swings': './?autoplay=1&npcs=0' at spawn (highlight id null) → press Space → within 150 ms __bt.swing.count 1 and __bt.player.motion 'attack-melee-right'; __bt.swing.hits 0, interactCount 0, the test box has not moved; 800 ms later __bt.player.motion is 'idle'
    - swing.spec desktop 'mashing E is limited by the cooldown': './?autoplay=1&npcs=0' → press KeyE three times within 150 ms → __bt.swing.count 1; wait 400 ms, press KeyE → count 2
    - swing.spec desktop 'clicking the game view swings; clicking the glowing box hits it': './?autoplay=1&npcs=0' → mouse click at (640, 240) with nothing highlighted → swing.count 1, hits 0; walk to the test box until it glows (as controls.spec), wait 400 ms, click the box's __bt.highlight.screen → swing.count 2, hits 1, interactCount 1, box moved ≥ 0.2 m
    - swing.spec desktop 'Space on a coworker swings and slaps': './?autoplay=1&npcs=1&npcAt=0.9,1.0' → press Space → swing.count 1, hits 1, __bt.slap.count 1, npcs[0].mode 'ragdoll'
    - swing.spec desktop 'no swing while paused': press Escape (paused) → press Space → swing.count unchanged; Escape to resume
    - swing.spec mobile-emu 'context button swings without a target and while the joystick is held': './?autoplay=1&npcs=0' → touchUi.contextIcon 'none' → tap #btn-context → swing.count 1, hits 0, player motion 'attack-melee-right'; hold the joystick with finger 0 (touchDown 150,250 + touchMove 150,200), wait 400 ms, tap #btn-context with finger 1 → swing.count 2 and joystick still active
    - no console errors, pageerrors or off-origin requests
  </behavior>
  <action>Implements the pure part of D-30. Write tests/unit/swing.test.ts first, confirm failure, implement src/logic/swing.ts (two numbers of state, no three.js, no Date/performance calls — callers pass nowMs) until green. Then write tests/e2e/swing.spec.ts per the behavior list, reusing the local helper style of controls.spec.ts (bt(), walk-until-highlight loop with KeyW) and the CDP touch helpers; confirm it fails against the current build.</action>
  <verify>
    <automated>npx vitest run tests/unit/swing.test.ts && echo UNIT_GREEN && npx vite build && echo BUILD_OK && { npx playwright test tests/e2e/swing.spec.ts; test $? -ne 0 && echo E2E_RED; }</automated>
  </verify>
  <acceptance_criteria>
    - Output contains UNIT_GREEN, BUILD_OK and E2E_RED with failures on __bt.swing / motion assertions (not "No tests found")
    - `grep -c "350" src/logic/swing.ts` prints at least 1 and `grep -vE "^\s*(//|\*)" src/logic/swing.ts | grep -cE "from 'three'|performance\.|Date\."` prints 0
  </acceptance_criteria>
  <done>Cooldown rules are proven; the always-swing behaviour is a failing e2e on desktop and mobile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Swing-first action flow, clip restart, click-anywhere swing (GREEN)</name>
  <files>src/render/characters.ts, src/game/player.ts, src/game/game.ts, src/input/pointerPick.ts, tests/e2e/controls.spec.ts, tests/e2e/camera.spec.ts, tests/e2e/room.spec.ts, tests/e2e/slap.spec.ts, tests/e2e/breakables.spec.ts, tests/e2e/hud.spec.ts, tests/e2e/characters.spec.ts (a spec is edited only when the cooldown audit finds two action presses under 350 ms apart)</files>
  <read_first>
    - tests/e2e/swing.spec.ts, src/logic/swing.ts
    - src/render/characters.ts (setMotion early return, the rigid skin from 01-23), src/game/player.ts, src/game/game.ts, src/input/pointerPick.ts, src/game/slap.ts, src/game/loop.ts (interact dropped while paused)
    - tests/e2e/room.spec.ts 'click only highlighted', tests/e2e/slap.spec.ts, tests/e2e/breakables.spec.ts (KeyE pressed 3× with 700 ms gaps), tests/e2e/controls.spec.ts 'E far from box does nothing'
    - Cooldown audit input (D-30): the output of `grep -nE "press\('(KeyE|Space)'\)|mouse\.(click|down)\(|touchTap\(|\.tap\(\)|btn-context" tests/e2e/*.spec.ts`, which lists every existing action key press, mouse click and tap; read each hit together with the waits before it in the same test
  </read_first>
  <behavior>
    - All swing.spec tests pass; the full `npx playwright test` stays green ('click only highlighted' still sees the non-highlighted click move nothing, 'E far from box does nothing' still has interactCount 0)
    - In every existing e2e test, consecutive action presses (KeyE, Space, game-area mouse click, #btn-context tap) are separated by a guaranteed wait of at least 350 ms, or the test was adjusted with a stated D-30 reason
  </behavior>
  <action>Implements D-30 (with D-18 / D-20 revised).
characters.ts: setMotion gains an optional third argument { restart?: boolean }; when restart is true and the name is already current, reset the action to time 0 and play it again (no crossfade needed); add motion() returning the current CharacterMotion. Do not touch buildRigidSkin wiring.
player.ts: add swing(towardX?, towardZ?) — when both are finite and at least 1 mm away, face that point (same maths as slapAt); set swingLeft = 0.45 s and call character.setMotion('attack-melee-right', 0.05, { restart: true }) immediately so the clip starts this frame even during an ongoing swing; slapAt(x, z) now simply calls swing(x, z). frameUpdate keeps holding the swing while swingLeft > 0 without restarting. Add motion to the existing 'player' debug getter.
pointerPick.ts: onPick type becomes (id: string | null) => void. For a mouse left-button pointerdown that is not blocked (paused) and whose target is not inside '[data-hud-button], [data-hud-panel], #pause-menu, button, input, textarea, select': call onPick(h.id) when the ray hits the highlighted object, otherwise onPick(null) (including when nothing is highlighted). Update the file comment (D-30: any game-area click swings; only the glowing object is hit).
game.ts: create one SwingGate. Replace the fixedUpdate interact block: pressed = consumeInteract(input); picked = pickQueued (undefined when no click), then clear it. If neither pressed nor a click, do nothing. Otherwise, if !gate.tryStart(performance.now()) drop the press (cooldown). Resolve hitTarget = pressed ? target : (picked !== null && target && picked === target.id ? target : null). If hitTarget is an NPC → performSlap (it swings toward the NPC via slapAt) and count a hit; if it is a prop → player.swing(target.x, target.z), props.push as before, interactCount++, count a hit; if null → player.swing() only. The click queue holds string | null | undefined (a pointerdown while paused is already blocked in pointerPick). Register __bt.swing = { count: gate.count(), hits, lastMs: gate.lastMs(), cooldownMs: SWING_COOLDOWN_MS }. slap.ts is unchanged (the 01-17 bench still calls performSlap directly, outside the gate).
Cooldown audit (D-30), done before the game.ts change is committed: run the grep from read_first. For each hit, find the previous action press, game-area click or #btn-context tap in the same test and the guaranteed time between them (sum of waitForTimeout values, or a waitForFunction on a state that cannot be reached in under 350 ms). Clicks and taps on buttons, [data-hud-button], [data-hud-panel] or inside #pause-menu never reach the gate and count as ok; hits in tests/e2e/swing.spec.ts are intentional cooldown checks and count as ok. For every pair with less than 350 ms guaranteed, insert page.waitForTimeout(400) before the second press (or restructure the test) and add a comment containing 'D-30: 350 ms swing cooldown'. Never lower SWING_COOLDOWN_MS to make a test pass. Record the audit in the SUMMARY under '## Cooldown audit (D-30)': one row per grep hit with file:line, the previous press in the same test (or 'first'), the guaranteed gap in ms, and 'ok' or 'adjusted'.</action>
  <verify>
    <automated>npm run typecheck && echo TYPECHECK_OK && npx vitest run && echo UNIT_OK && npx vite build && echo BUILD_OK && npx playwright test && echo E2E_OK</automated>
  </verify>
  <acceptance_criteria>
    - Output contains TYPECHECK_OK, UNIT_OK, BUILD_OK and E2E_OK, and the Playwright output has no line containing " failed"
    - `grep -c "tryStart(" src/game/game.ts` prints at least 1; `grep -c "onPick(null)" src/input/pointerPick.ts` prints at least 1; `grep -c "data-hud-panel" src/input/pointerPick.ts` prints at least 1
    - `grep -c "restart" src/render/characters.ts` prints at least 1 and `grep -c "registerDebug('swing'" src/game/game.ts` prints 1
    - `git diff --name-only -- src/game/slap.ts src/game/loop.ts | wc -l` prints 0
    - The SUMMARY section '## Cooldown audit (D-30)' has one row per line printed by the read_first grep (row count equals `grep -nE "press\('(KeyE|Space)'\)|mouse\.(click|down)\(|touchTap\(|\.tap\(\)|btn-context" tests/e2e/*.spec.ts | wc -l`), and every row says ok or adjusted
    - Every spec listed as adjusted in that table contains 'D-30' (`grep -c "D-30" <spec>` prints at least 1); `grep -c "SWING_COOLDOWN_MS = 350" src/logic/swing.ts` prints 1; the full Playwright run shows E2E_OK with no line containing " failed"
  </acceptance_criteria>
  <done>Every attack press swings at once, hits only a target in range, and cannot be spammed; mobile and desktop behave the same.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard / pointer / touch presses → swing and hit actions | Player-driven input rate and click positions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-24-01 | Denial of service | action spam (auto-clickers, key repeat) | mitigate | SwingGate 350 ms cooldown drops extra presses; keyboard ignores key repeats (01-22); each hit keeps the fixed impulse × mass rules of 01-15 |
| T-01-24-02 | Tampering | clicks on HUD / menu / panels triggering actions | mitigate | pointerPick ignores targets inside `[data-hud-button], [data-hud-panel], #pause-menu, button, input, textarea, select`; paused state blocks picks |
| T-01-24-03 | Tampering | stale click acting on a new target | mitigate | a queued click hits only if its id still equals the current target at the next fixed step (kept from 01-10) |
</threat_model>

<verification>
- `npm run typecheck && npx vitest run && npx vite build && npx playwright test` exits 0 with the four markers
</verification>

<success_criteria>
- D-30 holds on desktop and mobile: immediate swing on every press, hit only in range, cooldown against spam
</success_criteria>

## Artifacts this phase produces

- Symbols: `SWING_COOLDOWN_MS`, `SwingGate`, `createSwingGate`, `Player.swing`, `CharacterInstance.motion`, `setMotion(..., { restart })`, `onPick(id | null)`
- `window.__bt` key: `swing { count, hits, lastMs, cooldownMs }`; `__bt.player.motion`
- DOM contract: elements carrying `data-hud-panel` never trigger swings (used by plan 01-25)
- Tests: `tests/unit/swing.test.ts`, `tests/e2e/swing.spec.ts`; existing specs adjusted only where the cooldown audit found presses under 350 ms apart (comment 'D-30: 350 ms swing cooldown')
- SUMMARY section: `## Cooldown audit (D-30)`

<output>
Create `.planning/phases/01-spike-k-thu-t-ng-deploy/01-24-SUMMARY.md` when done
</output>
