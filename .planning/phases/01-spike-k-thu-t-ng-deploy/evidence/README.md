# evidence/ — ảnh chụp bằng chứng đo máy thật (plan 01-19)

Tên file mong đợi cho sha đo `04c06b21c39a`:

| Tên file | Nội dung | Trạng thái |
|----------|----------|-----------|
| `android-bench-04c06b21c39a.png` | Bảng "Kết quả benchmark" sau 60 s trên Chrome Android | **chưa lưu** — ảnh operator gửi trong hội thoại 16/09, số đã chép vào 01-DEVICE-LOG.md |
| `iphone-bench-04c06b21c39a.png` | Bảng "Kết quả benchmark" sau 60 s trên Safari iOS | **chưa lưu** — như trên |
| `desktop-bench-04c06b21c39a.png` | Bảng "Kết quả benchmark" sau 60 s trên Chrome desktop | **chưa lưu** — như trên |
| `iphone-soak-04c06b21c39a.png` | Bảng soak sau 15 phút trên iPhone | **chưa lưu** — như trên |
| `iphone-after-reload-04c06b21c39a.png` | (tuỳ chọn) màn hình sau khi tải lại, để xem có banner "Lần chơi trước bị dừng đột ngột" không | chưa chụp |

Ảnh phải thấy rõ: dòng `Commit` đúng sha đo, dòng `NPC` bằng 10, dòng `Bị giới hạn 30 fps?` bằng "Không".

Nợ tài liệu (không chặn cổng): Claude không ghi được file ảnh từ hội thoại xuống đĩa. Operator chép 4 ảnh đã gửi ngày 16/09/2026 vào thư mục này rồi commit; số liệu trong 01-DEVICE-LOG.md và 01-GATE.md đã chép sẵn từ chính các ảnh đó.

Không lưu ảnh có thông báo cá nhân, tài khoản hay số serial (T-01-19-02) — cắt bớt trước khi lưu nếu cần.
