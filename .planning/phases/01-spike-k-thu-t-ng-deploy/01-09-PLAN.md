---
phase: 01-spike-k-thu-t-ng-deploy
plan: 09
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - src/logic/cameraRig.ts
  - src/render/cameraView.ts
  - src/input/cameraKeys.ts
  - src/input/cameraButtons.ts
  - src/render/renderer.ts
  - src/boot/pageHardening.ts
  - src/boot/fullscreen.ts
  - src/main.ts
  - src/ui/layout.css
  - tests/unit/cameraRig.test.ts
  - tests/e2e/camera.spec.ts
  - tests/e2e/orientation.spec.ts
autonomous: true
requirements: [CTRL-03, CTRL-05]
must_haves:
  truths:
    - "Z and C (aliases ArrowLeft and ArrowRight) on desktop and ⟲ ⟳ buttons at the top-right on mobile rotate the camera target yaw by exactly 90°, the view damps smoothly to it, and WASD/joystick movement stays camera-relative; E never rotates (D-19, CTRL-05)"
    - "The camera keeps its fixed tilt and follows the player, framing about half of the room (D-19)"
    - "Portrait and landscape both fill the viewport; rotating mid-play keeps the canvas equal to the viewport and every HUD button inside it; portrait pulls the camera farther back and stacks buttons along the bottom; the game never asks the player to rotate (D-16, CTRL-03)"
    - "The Chơi tap requests fullscreen only when document.fullscreenEnabled is true; on iPhone Safari the game simply fills the viewport with no error (D-23, D-26)"
    - "Pinch zoom, double-tap zoom, pull-to-refresh, long-press menus and text selection are suppressed; canvas resizes are debounced and only happen when the backbuffer size changes"
  artifacts:
    - path: "src/logic/cameraRig.ts"
      provides: "Pure 90° yaw rig with damping and portrait/landscape view params"
      exports: ["createCameraRig", "viewParams"]
    - path: "src/input/cameraButtons.ts"
      provides: "⟲ ⟳ touch buttons"
      exports: ["attachCameraButtons"]
    - path: "src/boot/fullscreen.ts"
      provides: "Feature-detected fullscreen request"
      exports: ["requestFullscreenIfSupported"]
    - path: "tests/e2e/orientation.spec.ts"
      provides: "Portrait/landscape layout assertions"
  key_links:
    - from: "src/render/cameraView.ts"
      to: "src/logic/cameraRig.ts"
      via: "getCameraYaw returns the rig yaw used by movement"
      pattern: "createCameraRig"
    - from: "src/main.ts"
      to: "src/boot/fullscreen.ts"
      via: "onPlayGesture(requestFullscreenIfSupported)"
      pattern: "requestFullscreenIfSupported"
---

## Phase Goal

**As a** solo operator play-testing on my two reference phones, **I want to** open https://game.doibung.com, walk a low-poly office, slap NPCs into ragdolls and smash 20+ objects in a build that any commit can deploy with one command, **so that** I have measured proof that Three.js + Rapier meets the size, fps and iOS-stability budget before any gameplay phase builds on it.

<objective>
Camera + screen slice: the player rotates the view in 90° steps (Z/C or ⟲ ⟳), and the game fills the screen correctly in portrait and landscape with a guarded resize, page hardening and fullscreen where the browser supports it.

Purpose: CTRL-03 and CTRL-05 with D-16, D-19, D-23 (fullscreen on the Chơi tap) and D-26. Runs in the same wave as plan 01-08 with no shared files.
Output: pure camera rig with unit tests, rig-driven camera view, camera keys and buttons, resize guard, layout CSS, page hardening, fullscreen, camera and orientation e2e specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-CONTEXT.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-RESEARCH.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-03-SUMMARY.md
@src/render/cameraView.ts
@src/render/renderer.ts
@src/main.ts

<interfaces>
From plans 01-01..01-03: `registerDebug`, `onPlayGesture`, `createRenderer` / `RenderCtx`, `createCameraView` / `CameraView`, `getCameraYaw` (currently constant 0), `cameraRelativeMove`, `collectPageProblems`.

Contracts this plan creates (movement code already calls getCameraYaw, so it becomes camera-relative without edits):

```typescript
// src/logic/cameraRig.ts
export interface CameraRig { rotate(dir: -1 | 1): void; update(dt: number): void; yawDeg(): number; targetYawDeg(): number; yawDegNormalized(): number; snapped(): boolean }
export function createCameraRig(opts?: { stepDeg?: number; damping?: number }): CameraRig;
export function viewParams(aspect: number): { distance: number; pitchDeg: number };   // landscape 11 / 55, portrait 15 / 60
// src/render/cameraView.ts (extended)
export function rotateCamera(dir: -1 | 1): void;
export function getCameraYaw(): number;          // radians from the rig
// src/input/cameraKeys.ts
export function attachCameraKeys(): () => void;  // KeyZ/ArrowLeft → -1, KeyC/ArrowRight → +1
// src/input/cameraButtons.ts
export function attachCameraButtons(root: HTMLElement): () => void;   // #btn-rot-left, #btn-rot-right
// src/boot/fullscreen.ts
export function requestFullscreenIfSupported(): void;
// src/boot/pageHardening.ts
export function installPageHardening(): void;
```

Button ids styled in portrait by src/ui/layout.css: this plan's `#btn-rot-left`, `#btn-rot-right`, and plan 01-08's `#btn-context`, `#btn-pause`, `#joystick-base` (ids are a shared contract; no file is shared).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing camera-rig unit test and camera/orientation e2e (RED)</name>
  <files>tests/unit/cameraRig.test.ts, tests/e2e/camera.spec.ts, tests/e2e/orientation.spec.ts</files>
  <read_first>
    - tests/e2e/helpers.ts, tests/e2e/controls.spec.ts (patterns only; do not edit either file — plan 01-08 owns them)
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-RESEARCH.md "Pattern 5", "Pattern 6" (fullscreen paragraph), "Pitfall 4", "Pitfall 10", "Conflicts with CONTEXT" C1 and C2
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-CONTEXT.md decisions D-16, D-19, D-23, D-26
  </read_first>
  <behavior>
    - createCameraRig(): rotate(1) → targetYawDeg 90; from 0, rotate(-1) twice → -180; update(2.0) → |yawDeg - targetYawDeg| < 0.5 and snapped() true; four rotate(1) → targetYawDeg 360 and yawDegNormalized() 0 after update(2.0)
    - viewParams(844/390) → {distance 11, pitchDeg 55}; viewParams(390/844) → {distance 15, pitchDeg 60}
    - camera desktop ('./?autoplay=1'): KeyC → after 1000 ms __bt.camera.targetYawDeg 90 and |yawDeg - 90| < 1; KeyZ → 0; ArrowLeft → -90; KeyE leaves targetYawDeg unchanged
    - camera desktop movement: KeyC, wait until snapped, hold KeyW 800 ms → player x decreases ≥ 0.5 m and |Δz| < 0.2 m (camera-relative)
    - camera mobile-emu: tap #btn-rot-right → targetYawDeg 90 and |yawDeg - 90| < 1 after 1000 ms; tap #btn-rot-left twice → -90
    - camera fullscreen (desktop, no autoplay): click Chơi → __bt.fullscreen.requested equals document.fullscreenEnabled and there is no pageerror
    - orientation (mobile-emu): at 844x390 the canvas rect equals the viewport (±1 px) and every [data-hud-button] rect lies inside it; setViewportSize(390, 844) then 300 ms → canvas 390x844, buttons inside, body has class portrait, __bt.camera.distance 15; back to 844x390 → distance 11; ten viewport flips within 100 ms → __bt.renderer.resizeCount grows by at most 2
    - every camera/orientation test ends with zero console errors, pageerrors and off-origin requests
  </behavior>
  <action>Write tests/unit/cameraRig.test.ts and the two e2e files exactly per the behavior list (project guards via test.skip on testInfo.project.name). They must not import or modify tests/e2e/controls.spec.ts; importing collectPageProblems from helpers.ts is fine.</action>
  <verify>
    <automated>npx vite build && echo BUILD_OK && { npx vitest run tests/unit/cameraRig.test.ts; test $? -ne 0 && echo UNIT_RED; } ; { npx playwright test tests/e2e/camera.spec.ts tests/e2e/orientation.spec.ts; test $? -ne 0 && echo E2E_RED; }</automated>
  </verify>
  <acceptance_criteria>
    - Output contains BUILD_OK, UNIT_RED and E2E_RED
    - Playwright lists camera and orientation tests as failed, not "No tests found"
  </acceptance_criteria>
  <done>Rotation, orientation and fullscreen contracts are failing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Camera rig, rig-driven camera view, Z/C/arrow keys and ⟲ ⟳ buttons (GREEN for rotation)</name>
  <files>src/logic/cameraRig.ts, src/render/cameraView.ts, src/input/cameraKeys.ts, src/input/cameraButtons.ts, src/main.ts</files>
  <read_first>
    - tests/unit/cameraRig.test.ts, tests/e2e/camera.spec.ts
    - src/render/cameraView.ts, src/main.ts, src/logic/moveMath.ts
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-CONTEXT.md decision D-19 (Z/C, not Q/E)
  </read_first>
  <behavior>
    - cameraRig unit tests pass; camera.spec rotation and movement tests pass in desktop and mobile-emu; controls.spec and smoke still pass
  </behavior>
  <action>Implements D-19 (Z/C rotation, fixed tilt, smooth follow) and CTRL-05. cameraRig.ts: unwrapped target yaw in degrees, yaw damped with factor 1 - Math.exp(-damping * dt) (damping default 10), snapped() within 0.5°, yawDegNormalized() in [0, 360); viewParams returns landscape {11, 55} for aspect ≥ 1 and portrait {15, 60} otherwise (D-16 farther camera).
cameraView.ts: hold one CameraRig; rotateCamera(dir) calls rig.rotate; getCameraYaw() returns the rig's current yaw in radians; update(dt, target) runs rig.update(dt), takes distance and pitch from viewParams(camera.aspect) (damped), positions the camera around the damped target at that yaw; registerDebug('camera', () => ({ yawDeg, targetYawDeg, distance, pitchDeg })).
cameraKeys.ts: its own window keydown listener (no edits to src/input/keyboard.ts): KeyZ or ArrowLeft → rotateCamera(-1), KeyC or ArrowRight → rotateCamera(1), ignoring e.repeat; never react to KeyE.
cameraButtons.ts: button#btn-rot-left (⟲) and button#btn-rot-right (⟳) at the top-right, each with data-hud-button and its own pointerdown listener (preventDefault) calling rotateCamera; shown when matchMedia('(pointer: coarse)').matches or after a first touch pointerdown, hidden otherwise; styles live in src/ui/layout.css (Task 3) — for this task give them inline position so tests can tap them.
main.ts: after the renderer is created call attachCameraKeys() and attachCameraButtons(app root).</action>
  <verify>
    <automated>npm run typecheck && npx vitest run && npx vite build && npx playwright test tests/e2e/camera.spec.ts tests/e2e/controls.spec.ts tests/e2e/smoke.spec.ts --grep-invert fullscreen</automated>
  </verify>
  <acceptance_criteria>
    - Playwright reports camera rotation/movement tests, controls and smoke passing
    - `cat src/input/cameraKeys.ts | grep -c "KeyE"` prints 0 and `cat src/input/cameraKeys.ts | grep -c "KeyZ"` prints at least 1
    - After the task commit, `git show --name-only --format= HEAD | grep -cE 'input/keyboard.ts|input/touchButtons.ts|ui/hud.css|game/game.ts|game/loop.ts'` prints 0 (plan 01-08 files in the same wave)
  </acceptance_criteria>
  <done>Camera rotates in exact 90° steps from keys and buttons, and movement follows the view.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Resize guard, portrait/landscape layout CSS, page hardening, fullscreen when supported (GREEN)</name>
  <files>src/render/renderer.ts, src/ui/layout.css, src/boot/pageHardening.ts, src/boot/fullscreen.ts, src/main.ts</files>
  <read_first>
    - tests/e2e/orientation.spec.ts, tests/e2e/camera.spec.ts (fullscreen test)
    - src/render/renderer.ts, src/main.ts, src/boot/playGate.ts (onPlayGesture)
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-RESEARCH.md "Pattern 5", "Pattern 6" (fullscreen), "Pitfall 4", "Conflicts with CONTEXT" C2
  </read_first>
  <behavior>
    - All camera.spec and orientation.spec tests pass in their projects; smoke, csp and controls still pass
  </behavior>
  <action>Implements D-16, D-23 (fullscreen on the Chơi tap), D-26 and CTRL-03. renderer.ts: replace the plain resize listener with a guard on window resize, visualViewport resize and orientationchange; debounce 100 ms; width/height from visualViewport (fallback innerWidth/innerHeight) and the integer backbuffer size; renderer.setSize and camera.aspect update only when the backbuffer size changed; toggle body.portrait when height > width; registerDebug('renderer', () => ({ resizeCount, width, height, dpr, shadowMapEnabled: renderer.shadowMap.enabled })).
layout.css (imported from main.ts): top-right rotate buttons in landscape; body.portrait overrides for #btn-context (bottom-centre-right), #btn-rot-left and #btn-rot-right (bottom-left above the joystick area), #btn-pause (top-right) and #joystick-base limits, all offset by env(safe-area-inset-*), hit areas ≥ 56 px; no rotate-your-device message anywhere (D-16).
pageHardening.ts: RESEARCH Pattern 5 listeners (gesturestart preventDefault, touchmove preventDefault with passive false, contextmenu and dblclick preventDefault).
fullscreen.ts: requestFullscreenIfSupported(): only when document.fullscreenEnabled call document.documentElement.requestFullscreen({ navigationUI: 'hide' }) and store any rejection as an error string; never throw, never alert (D-26); registerDebug('fullscreen', () => ({ enabled, requested, error })).
main.ts: installPageHardening() at boot start, import './ui/layout.css', and onPlayGesture(requestFullscreenIfSupported) before waitForPlay.</action>
  <verify>
    <automated>npm run typecheck && npx vitest run && npx vite build && npx playwright test tests/e2e/camera.spec.ts tests/e2e/orientation.spec.ts tests/e2e/controls.spec.ts tests/e2e/smoke.spec.ts tests/e2e/csp.spec.ts</automated>
    <human-check>
      <test>On both reference phones (deployed URL after plan 01-12) rotate the phone mid-play, use ⟲ ⟳, tap Chơi on the iPhone</test>
      <expected>Canvas always fills the screen, buttons stay reachable inside safe areas in both orientations, no zoom/scroll; iPhone shows no fullscreen error and just fills the viewport</expected>
      <why_human>Notch/safe-area layout and iOS fullscreen behaviour exist only on real devices</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0 with no " failed"
    - `cat src/boot/fullscreen.ts | grep -c "fullscreenEnabled"` prints at least 1 and `grep -c "alert(" -r src` prints 0
    - `cat src/ui/layout.css | grep -c "body.portrait"` prints at least 1 and `grep -ri "xoay máy\|rotate your" src | wc -l` prints 0
    - `grep -c "innerHTML" -r src` prints 0
  </acceptance_criteria>
  <done>Both orientations fill the screen, resizes are guarded, and fullscreen is requested only where supported.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard/touch → camera state | Local input only |
| page → browser fullscreen API | Permission-gated browser feature |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09-01 | Denial of service | resize storms on iOS URL-bar animation | mitigate | 100 ms debounce and backbuffer-size equality; e2e bounds resizeCount |
| T-01-09-02 | Denial of service | fullscreen rejection on iPhone | mitigate | Feature-detect fullscreenEnabled, swallow rejection, never block play (D-26) |
| T-01-09-03 | Tampering | DOM construction of buttons | mitigate | createElement/textContent only; acceptance greps innerHTML = 0 |
| T-01-09-04 | Spoofing | accidental zoom/scroll interfering with controls | accept | touch-action none + gesture listeners; residual iOS accessibility zoom accepted (RESEARCH A1) |
</threat_model>

<verification>
- `npm run typecheck && npx vitest run && npx vite build && npx playwright test tests/e2e/camera.spec.ts tests/e2e/orientation.spec.ts tests/e2e/controls.spec.ts tests/e2e/smoke.spec.ts tests/e2e/csp.spec.ts` exits 0
</verification>

<success_criteria>
- CTRL-03 and CTRL-05 proven by e2e plus rig unit tests; no file shared with plan 01-08
</success_criteria>

## Artifacts this phase produces

- Symbols: `createCameraRig`, `CameraRig`, `viewParams`, `rotateCamera`, `getCameraYaw` (rig-driven), `attachCameraKeys`, `attachCameraButtons`, `installPageHardening`, `requestFullscreenIfSupported`
- Key bindings: KeyZ / ArrowLeft (rotate -90°), KeyC / ArrowRight (rotate +90°)
- DOM ids: `#btn-rot-left`, `#btn-rot-right`; body class `portrait`; stylesheet `src/ui/layout.css`
- `window.__bt` keys: `camera`, `renderer`, `fullscreen`
- Constants: view params landscape 11 m / 55°, portrait 15 m / 60°, rig damping 10, resize debounce 100 ms
- Tests: `tests/unit/cameraRig.test.ts`, `tests/e2e/camera.spec.ts`, `tests/e2e/orientation.spec.ts`

<output>
Create `.planning/phases/01-spike-k-thu-t-ng-deploy/01-09-SUMMARY.md` when done
</output>
