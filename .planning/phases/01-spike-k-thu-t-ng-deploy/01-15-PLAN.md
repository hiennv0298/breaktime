---
phase: 01-spike-k-thu-t-ng-deploy
plan: 15
type: execute
wave: 7
depends_on: ["01-14", "01-11", "01-13"]
files_modified:
  - src/logic/rng.ts
  - src/logic/getUpFsm.ts
  - src/logic/hitStop.ts
  - src/physics/ragdoll.ts
  - src/game/slap.ts
  - src/game/npc.ts
  - src/game/game.ts
  - src/game/loop.ts
  - src/render/cameraView.ts
  - tests/unit/rng.test.ts
  - tests/unit/getUpFsm.test.ts
  - tests/unit/hitStop.test.ts
  - tests/e2e/slap.spec.ts
autonomous: true
requirements: [CTRL-01, CTRL-02, TECH-06]
must_haves:
  truths:
    - "An NPC within 1.6 m in front of the player becomes the highlighted target with the 'slap' icon; E, a left-click on it, or the context button slaps it (D-18, D-20)"
    - "A slap freezes the simulation for 60 ms while rendering continues, shakes the camera lightly, plays a 'bốp' punch SFX variant and launches the NPC as a 6-part Rapier ragdoll that flies far and spins (D-12, D-15)"
    - "When the torso settles (speed < 0.35 m/s and angular speed < 1 rad/s for 0.6 s, or after 4 s) the NPC blends upright over 0.45 s and resumes its waypoint loop from the nearest point (D-12)"
    - "There is no blood, gore or injury effect anywhere (D-12, PEGI 12)"
    - "Slap spin and pitch variation use a seeded mulberry32 RNG so benchmark runs replay the same way on a device (D-08)"
    - "getUpFsm, hitStop and rng are pure modules with Vitest coverage (TECH-06)"
  artifacts:
    - path: "src/physics/ragdoll.ts"
      provides: "Pooled 6-body ragdoll with 5 spherical joints per character"
      exports: ["createRagdoll"]
    - path: "src/logic/getUpFsm.ts"
      provides: "Pure animated → ragdoll → recover state machine"
      exports: ["createGetUp", "updateGetUp", "slapGetUp"]
    - path: "src/logic/rng.ts"
      provides: "Seeded PRNG"
      exports: ["mulberry32", "BENCH_SEED"]
    - path: "src/game/slap.ts"
      provides: "Slap impulse, hit-stop, shake, SFX"
      exports: ["performSlap"]
  key_links:
    - from: "src/game/loop.ts"
      to: "src/logic/hitStop.ts"
      via: "game.timeScale(nowMs) returns 0 during hit-stop"
      pattern: "timeScale"
    - from: "src/game/slap.ts"
      to: "src/physics/ragdoll.ts"
      via: "activate with exaggerated impulse"
      pattern: "activate("
    - from: "src/game/slap.ts"
      to: "src/audio/sfx.ts"
      via: "playSfx(pickVariant(sfxNames(), 'slap', rng))"
      pattern: "playSfx"
---

## Phase Goal

**As a** solo operator play-testing on my two reference phones, **I want to** open https://breaktime.doibung.com, walk a low-poly office, slap NPCs into ragdolls and smash 20+ objects in a build that any commit can deploy with one command, **so that** I have measured proof that Three.js + Rapier meets the size, fps and iOS-stability budget before any gameplay phase builds on it.

<objective>
Slapstick slice: walking up to a coworker and pressing E (or tapping the context button, or clicking them) sends them flying as a ragdoll with hit-stop, shake and a punch sound; they get back up and carry on.

Purpose: ROADMAP success criterion 2 (NPC ragdoll) and D-12 feel, plus the ragdoll load the benchmark measures. Audio unlock itself is already done in plan 01-13.
Output: pure rng/getUp/hitStop modules with tests, ragdoll pool, slap system, NPC recover blend, camera shake, slap e2e.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-CONTEXT.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-RESEARCH.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-14-SUMMARY.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-11-SUMMARY.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-13-SUMMARY.md
@src/game/npc.ts
@src/game/game.ts
@src/game/loop.ts
@src/render/cameraView.ts
@src/audio/sfx.ts

<interfaces>
From earlier plans: `registerDebug`, `Game.timeScale(nowMs)`, `getPauseState`, `pickNearest`/`iconFor` (kind 'npc' → 'slap'), `setHighlighted`, `attachPointerPick`, `setContextIcon`, `createNpc`/`Npc`, `CHARACTER_PARTS`, `spawnCharacter`, `stepWalker`, `nearestIndex`, `Props.byColliderHandle`, `playSfx`, `sfxNames`, `pickVariant`, `getCameraYaw`.

Contracts this plan creates:

```typescript
// src/logic/rng.ts
export const BENCH_SEED = 20260914;
export function mulberry32(seed: number): () => number;          // [0, 1)
// src/logic/hitStop.ts
export interface HitStop { trigger(nowMs: number, durationMs: number): void; active(nowMs: number): boolean; count(): number }
export function createHitStop(): HitStop;
// src/logic/getUpFsm.ts
export type GetUpMode = 'animated' | 'ragdoll' | 'recover';
export interface GetUpState { mode: GetUpMode; settleSec: number; ragdollSec: number; recoverSec: number }
export const SETTLE_SPEED = 0.35, SETTLE_ANG = 1.0, SETTLE_HOLD_SEC = 0.6, RAGDOLL_TIMEOUT_SEC = 4.0, RECOVER_SEC = 0.45;
export function createGetUp(): GetUpState;
export function slapGetUp(s: GetUpState): GetUpState;              // animated|recover → ragdoll; ragdoll unchanged
export function updateGetUp(s: GetUpState, input: { torsoSpeed: number; torsoAngSpeed: number; dt: number }): { state: GetUpState; event: 'none' | 'start-recover' | 'resumed'; recoverT: number };
// src/physics/ragdoll.ts
export interface Ragdoll { activate(impulse: { x: number; y: number; z: number }, torque: { x: number; y: number; z: number }): void; deactivate(): void; sync(): void; torso(): RigidBody; active(): boolean; bodyCount(): number }
export function createRagdoll(ctx: GameCtx, parts: Object3D[], groupIndex: number): Ragdoll;
// src/game/slap.ts
export function performSlap(game: Game, npc: Npc, nowMs: number): void;
// src/render/cameraView.ts (added)
export function shakeCamera(amplitude: number, durationMs: number): void;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: rng, hitStop and getUpFsm via TDD plus failing slap e2e</name>
  <files>src/logic/rng.ts, src/logic/hitStop.ts, src/logic/getUpFsm.ts, tests/unit/rng.test.ts, tests/unit/hitStop.test.ts, tests/unit/getUpFsm.test.ts, tests/e2e/slap.spec.ts</files>
  <read_first>
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-RESEARCH.md "Pattern 3" (hit-stop, determinism), "Pattern 14" (settle thresholds and recover), "Don't Hand-Roll" (mulberry32 row)
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-CONTEXT.md decisions D-08, D-12
    - tests/e2e/helpers.ts, tests/e2e/room.spec.ts (highlight/screen patterns)
  </read_first>
  <behavior>
    - rng: two mulberry32(BENCH_SEED) generators produce identical first 5 values; all values in [0, 1); mulberry32(1) differs from mulberry32(2) in the first value
    - hitStop: trigger(1000, 60) → active(1030) true, active(1061) false; trigger(1000,60) then trigger(1040,60) → active(1090) true, active(1101) false; count() counts triggers
    - getUpFsm: slapGetUp(animated) → ragdoll; updates with speed 0.2 and ang 0.5 for 0.6 s total → event 'start-recover' exactly once and mode recover; with speed 2.0 for 4.0 s → start-recover by timeout; recover for 0.45 s → event 'resumed' and mode animated; recoverT goes 0→1 during recover; slapGetUp during recover → ragdoll; slapGetUp during ragdoll leaves ragdollSec unchanged
    - slap e2e (desktop, './?autoplay=1&npcs=1&npcAt=0.9,1.0'): __bt.highlight.kind 'npc' and icon 'slap'; press KeyE → within 300 ms __bt.slap.count 1, __bt.hitStop.count 1, __bt.npcs[0].mode 'ragdoll', __bt.camera.shakeActive observed true at least once; within 1500 ms the NPC torso moved ≥ 2.0 m from its pre-slap position; within 7000 ms __bt.npcs[0].mode is 'walk' or 'dwell' and __bt.ragdolls.active 0; __bt.audio.requests includes a name starting 'slap-'
    - slap e2e (desktop, click): same setup, page.mouse.click on __bt.highlight.screen → __bt.slap.count 1
    - slap e2e (mobile-emu): same URL, tap #btn-context → __bt.slap.count 1
    - no console errors, pageerrors or off-origin requests in any slap test
  </behavior>
  <action>Implements the pure parts of D-12 and the D-08 seed. Write the three unit test files first and confirm they fail; implement rng.ts (standard 32-bit mulberry32), hitStop.ts and getUpFsm.ts per the interfaces block until green. Then write tests/e2e/slap.spec.ts per the behavior list. `npcAt=x,z` is a test/bench parameter (implemented in Task 2) that places NPC 0 at the given floor point with its walker frozen until slapped.</action>
  <verify>
    <automated>npx vitest run tests/unit/rng.test.ts tests/unit/hitStop.test.ts tests/unit/getUpFsm.test.ts && npx vite build && echo UNIT_GREEN && { npx playwright test tests/e2e/slap.spec.ts; test $? -ne 0 && echo E2E_RED; }</automated>
  </verify>
  <acceptance_criteria>
    - Output contains UNIT_GREEN and E2E_RED
    - src/logic/getUpFsm.ts contains "0.35", "0.6", "4" and "0.45"
    - `cat src/logic/rng.ts src/logic/getUpFsm.ts src/logic/hitStop.ts | grep -c "Math.random"` prints 0
  </acceptance_criteria>
  <done>Timing rules and determinism are proven by unit tests; slap behaviour is a failing e2e.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ragdoll pool, slap targeting and impulse, hit-stop, camera shake, punch SFX, get-up blend (GREEN)</name>
  <files>src/physics/ragdoll.ts, src/game/slap.ts, src/game/npc.ts, src/game/game.ts, src/game/loop.ts, src/render/cameraView.ts</files>
  <read_first>
    - tests/e2e/slap.spec.ts
    - src/game/npc.ts, src/game/game.ts, src/game/loop.ts, src/render/cameraView.ts, src/render/characters.ts, src/logic/nearest.ts, src/audio/sfx.ts, src/logic/sfxNames.ts
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-RESEARCH.md "Pattern 14" (all bullets), "Pattern 3", "Conflicts with CONTEXT" C5
    - node_modules/@dimforge/rapier3d-compat/rapier.d.ts (JointData.spherical, ImpulseJoint.setContactsEnabled, RigidBody.setEnabled, setCollisionGroups)
  </read_first>
  <behavior>
    - All slap.spec tests pass; full `npx playwright test` stays green
  </behavior>
  <action>Implements D-12 (slapstick physics), D-15 (punch SFX through 01-13's playSfx) and D-18/D-20 (slap via E, click, context button).
ragdoll.ts: at NPC spawn create 6 dynamic bodies, one per CHARACTER_PARTS entry, with cuboid colliders sized from each part's Box3 in the rest pose (never hardcoded), density 1, linearDamping 0.2, angularDamping 0.5, enabled false; 5 spherical impulse joints (torso↔leg-left and torso↔leg-right at the hip pivots, torso↔arm-left and torso↔arm-right at the shoulders, torso↔head at the neck) with setContactsEnabled(false); collision groups: membership bit (1 + groupIndex) and a filter excluding that bit, so a ragdoll's parts ignore each other but hit the world, props and other ragdolls. activate(): Object3D.attach each part to a flat ragdollRoot group (world transform preserved), set each body translation/rotation from its part's world matrix, setEnabled(true), applyImpulse on the torso and applyTorqueImpulse with the torque. sync() copies body transforms to parts. deactivate() disables bodies.
slap.ts: performSlap(game, npc, nowMs) with a module-level mulberry32(BENCH_SEED): direction = normalised XZ from player to NPC; impulse = torso-group mass * (dir * 9 + up * 5) (exaggerated fly-away, D-12); torque = mass * ((rng()*2-1)*6, (rng()*2-1)*4 + 3, (rng()*2-1)*6); hitStop.trigger(nowMs, 60); shakeCamera(0.12, 180); player setMotion('attack-melee-right') then back to idle/walk; NPC capsule body setEnabled(false); npc.slap(); increment slapCount; playSfx(pickVariant(sfxNames(), 'slap', rng) ?? 'slap-0', { rate: 0.95 + rng() * 0.13 }).
npc.ts: hold GetUpState; slap() → slapGetUp + ragdoll activation via slap.ts; while ragdoll feed torso linear/angular speed into updateGetUp; on 'start-recover' read torso position and yaw (torso forward projected onto XZ), deactivate the ragdoll, re-attach parts to the character hierarchy with Object3D.attach, snapshot each part's local transform; during recover slerp/lerp each part toward the idle pose sampled once at spawn (mixer at idle time 0) using recoverT, root position = torso XZ at y 0, root yaw = torso yaw; on 'resumed' re-enable the capsule at the root, walker.index = nearestIndex(route, x, z), walker.frozen false, setMotion('walk'). Support `npcAt=x,z` for NPC 0 (two finite numbers clamped inside ROOM minus 0.5 m; walker frozen until the first slap). Expose mode as 'ragdoll' | 'recover' | walker mode.
game.ts: add NPC candidates (kind 'npc', radius 0.4, only while animated) to pickNearest; highlight applies to the NPC root; on interact (E, context button, or pointer pick of the highlighted NPC) call performSlap, otherwise push props as before; timeScale(nowMs) returns 0 while hitStop.active(nowMs); registerDebug('slap', () => ({ count })), registerDebug('hitStop', () => ({ count, active })), registerDebug('ragdolls', () => ({ active })).
loop.ts: pass performance.now() to game.timeScale and keep rendering during hit-stop (0 steps, render continues).
cameraView.ts: shakeCamera adds a decaying offset (amplitude * (1 - t)) with direction from a fixed-seed generator, applied after positioning; extend the 'camera' debug getter with shakeActive.</action>
  <verify>
    <automated>npm run typecheck && npx vitest run && npx vite build && npx playwright test</automated>
    <human-check>
      <test>`npm run dev`, walk up to a coworker and press E several times; on a phone use the context button</test>
      <expected>A punchy "bốp" with a brief freeze and shake, the coworker flies and tumbles, then stands up and walks on; nothing gory</expected>
      <why_human>Slapstick feel is a judgement call</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - Full `npx playwright test` exits 0 with no " failed"
    - `cat src/physics/ragdoll.ts | grep -c "setContactsEnabled(false)"` prints at least 1 and `cat src/physics/ragdoll.ts | grep -c "setEnabled(false)"` prints at least 1
    - `grep -riE "blood|gore|máu" src | wc -l` prints 0
    - src/game/slap.ts contains "shakeCamera(0.12, 180)", "playSfx" and a hit-stop trigger with 60
  </acceptance_criteria>
  <done>Slapped NPCs fly as ragdolls with hit-stop, shake and sound, then get up and resume their routes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query ?npcAt → NPC placement | Visitor-controlled coordinates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-15-01 | Tampering | ?npcAt parameter | mitigate | Parse two finite numbers, clamp inside ROOM bounds minus 0.5 m, ignore otherwise |
| T-01-15-02 | Denial of service | ragdoll body/joint creation | mitigate | Bodies and joints created once per NPC at spawn and toggled with setEnabled; no allocation per slap; NPC count capped at 8 |
| T-01-15-03 | Denial of service | runaway physics from repeated slaps | mitigate | Slap ignored while already ragdoll (getUpFsm); bounded impulse constants; 4 s ragdoll timeout |
| T-01-15-04 | Information disclosure | content rating | mitigate | No blood or injury effects (acceptance grep); PEGI 12 |
</threat_model>

<verification>
- `npm run typecheck && npx vitest run && npx vite build && npx playwright test` exits 0
</verification>

<success_criteria>
- NPC ragdoll slap with hit-stop, shake, SFX and get-up proven by e2e; pure timing logic unit-tested
</success_criteria>

## Artifacts this phase produces

- Symbols: `BENCH_SEED`, `mulberry32`, `createHitStop`, `HitStop`, `GetUpMode`, `GetUpState`, `SETTLE_SPEED`, `SETTLE_ANG`, `SETTLE_HOLD_SEC`, `RAGDOLL_TIMEOUT_SEC`, `RECOVER_SEC`, `createGetUp`, `slapGetUp`, `updateGetUp`, `createRagdoll`, `Ragdoll`, `performSlap`, `shakeCamera`
- URL param: `npcAt=x,z`
- `window.__bt` keys: `slap`, `hitStop`, `ragdolls` (and `camera.shakeActive`)
- Constants: hit-stop 60 ms, shake 0.12 m / 180 ms, slap impulse mass × (9 horizontal, 5 up), NPC target radius 0.4 m
- Tests: `tests/unit/rng.test.ts`, `tests/unit/hitStop.test.ts`, `tests/unit/getUpFsm.test.ts`, `tests/e2e/slap.spec.ts`

<output>
Create `.planning/phases/01-spike-k-thu-t-ng-deploy/01-15-SUMMARY.md` when done
</output>
