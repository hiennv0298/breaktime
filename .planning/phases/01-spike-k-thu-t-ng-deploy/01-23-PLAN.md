---
phase: 01-spike-k-thu-t-ng-deploy
plan: 23
type: execute
wave: 9
depends_on: ["01-16", "01-14", "01-15"]
files_modified:
  - src/render/rigidSkin.ts
  - tests/unit/rigidSkin.test.ts
  - tests/e2e/characters.spec.ts
  - src/render/characters.ts
  - src/game/waypoints.ts
  - tests/unit/waypoints.test.ts
  - src/game/game.ts
  - tests/e2e/npc.spec.ts
autonomous: true
requirements: [TECH-03, TECH-06, CTRL-07]
must_haves:
  truths:
    - "Every Blocky character (player and each NPC) renders as ONE rigid-skinned mesh: 1 draw call per character instead of 6, with each part still animated by its clip and still driven by its ragdoll body (RESEARCH A6, 'Hệ quả hiệu năng' in CONTEXT, D-11 revised)"
    - "Per-NPC textures from 01-14 still differ per character, and the 01-15 slap → ragdoll → get-up still works on the merged mesh (D-12, D-09)"
    - "?npcs=10 spawns ten coworkers; ?npcs= is clamped to 0..10 (D-29, D-11 revised)"
    - "NPCs 9 and 10 walk their own hand-placed routes (no navmesh) that pass the existing clearance unit test and keep clear of floor-standing props (D-29, D-11)"
    - "With 10 NPCs + player at the peak of ?scenario=smash the headless HUD reports peak draw calls ≤ 120 and peak bodies ≤ 200 (RESEARCH Performance budget), and the measured draw calls / bodies at 3 and at 10 NPCs are written in the SUMMARY — this lands BEFORE the 01-17 benchmark"
  artifacts:
    - path: "src/render/rigidSkin.ts"
      provides: "Merge a Blocky model's 6 part meshes into one SkinnedMesh with 6 rigidly weighted bones"
      exports: ["buildRigidSkin", "RigidSkin"]
    - path: "src/render/characters.ts"
      provides: "spawnCharacter builds the rigid skin; __bt.characters reports skinnedMeshes, drawsPerCharacter and a lazy skin bounds list"
      contains: "buildRigidSkin"
    - path: "src/game/waypoints.ts"
      provides: "5 hand-placed routes and the NPC → route mapping"
      exports: ["NPC_ROUTES", "routeIndexForNpc", "sharedIndexForNpc"]
    - path: "tests/e2e/characters.spec.ts"
      provides: "Draw-call budget at 3 and 10 NPCs, skin follows walk and ragdoll"
  key_links:
    - from: "src/render/characters.ts"
      to: "src/render/rigidSkin.ts"
      via: "spawnCharacter → buildRigidSkin(model, CHARACTER_PARTS, material, asset.scene)"
      pattern: "buildRigidSkin\\("
    - from: "src/game/game.ts"
      to: "src/game/waypoints.ts"
      via: "route and shared offset per NPC index"
      pattern: "routeIndexForNpc\\("
---

## Phase Goal

**As a** solo operator play-testing on my two reference phones, **I want to** open https://game.doibung.com, walk a low-poly office, slap NPCs into ragdolls and smash 20+ objects in a build that any commit can deploy with one command, **so that** I have measured proof that Three.js + Rapier meets the size, fps and iOS-stability budget before any gameplay phase builds on it.

<objective>
Ten-coworker slice: after this plan `?npcs=10` fills the office with ten coworkers on hand-placed routes, and the whole cast costs one draw call per character, so the 10-NPC benchmark of plan 01-17 fits the 120 draw-call budget.

Purpose: D-29 lets the player pick up to 10 NPCs and D-11 (revised) makes the benchmark measure that ceiling. 01-14 measured 8 NPCs at 134 draw calls because each Blocky character is 6 draws; CONTEXT "Hệ quả hiệu năng" moves the character merge (RESEARCH A6) in front of 01-17 instead of leaving it to the D-07 pass.
Output: rigid-skin builder with unit tests, merged characters, two new routes, `?npcs` clamp 0..10, and an e2e that measures the budget.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-CONTEXT.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-RESEARCH.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-14-SUMMARY.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-15-SUMMARY.md
@.planning/phases/01-spike-k-thu-t-ng-deploy/01-16-SUMMARY.md
@src/render/characters.ts
@src/physics/ragdoll.ts
@src/game/npc.ts
@src/game/waypoints.ts
@src/game/game.ts

<interfaces>
Existing: `CHARACTER_PARTS` ('leg-left','leg-right','torso','arm-left','arm-right','head'), `CharacterAsset { scene, clips }`, `CharacterInstance { root, mixer, parts, material, setMotion, dispose }`, `spawnCharacter(asset, letter)` (SkeletonUtils.clone of asset.scene, model scaled to 1.5 m, Lambert material cached per letter, meshes sit on unnamed children of the named part nodes and are meshopt-quantized), `createRagdoll(ctx, parts, groupIndex)` (measures each part's OWN mesh children — skipping nested part nodes — and detaches parts to a flat 'ragdoll-root' group with `Object3D.attach`), `setHighlighted(root)` (swaps materials on every Mesh under root), `attachPointerPick` (raycasts the highlighted root recursively), `NPC_ROUTES` (3 routes), `ROUTE_OBSTACLES`, `NPC_RADIUS 0.3`, `ROUTE_CLEARANCE 0.05`, `DESKS`, `PANTRY`, `PROP_PLACEMENTS`, `CORRIDOR`, `ROOM`, `npcCountFromQuery` (clamp 8), `MAX_NPCS 8`, `__bt.hud { drawCalls, bodies, peakBodies, peakDrawCalls }`, `__bt.npcs[] { id, pos, mode, texture }`, `__bt.debris.broken`, `__bt.ragdolls`.

Verified in node_modules/three (r186): `SkinnedMesh.bind(skeleton)` without a bind matrix calls `updateMatrixWorld(true)` + `skeleton.calculateInverses()` from bone world matrices; `bindMode` defaults to AttachedBindMode (bindMatrixInverse follows matrixWorld); `SkinnedMesh.computeBoundingBox()` applies bone transforms per vertex; `mergeGeometries` lives in `three/addons/utils/BufferGeometryUtils.js` (room.ts already imports it).

Contracts this plan creates:

```typescript
// src/render/rigidSkin.ts
export interface RigidSkin { mesh: SkinnedMesh; bones: Bone[] }
/** partNames in bone order; cacheKey shares the merged geometry between clones of the same asset. */
export function buildRigidSkin(model: Object3D, partNames: readonly string[], material: Material, cacheKey: object): RigidSkin;
// src/render/characters.ts (additions)
// CharacterInstance.skinned: SkinnedMesh
// __bt.characters: { texturesLoaded, skinnedMeshes, drawsPerCharacter: 1, skin: Array<{ letter: string; centre: [number, number, number]; height: number }> }
// src/game/waypoints.ts (additions)
export function routeIndexForNpc(i: number): number;   // 0..7 → i % 3, 8 → 3, 9 → 4
export function sharedIndexForNpc(i: number): number;  // earlier NPCs on the same route: 0..7 → floor(i / 3), 8 and 9 → 0
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rigid-skin builder via TDD, plus a failing character budget e2e (RED)</name>
  <files>src/render/rigidSkin.ts, tests/unit/rigidSkin.test.ts, tests/e2e/characters.spec.ts</files>
  <read_first>
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-RESEARCH.md "Performance budget" (the SkinnedMesh note), "Assumptions Log" A6, "Pattern 14" (part structure, head scale 0.1, attach-based ragdoll), "Pitfall 8", "Anti-Patterns to Avoid"
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-CONTEXT.md decisions D-09, D-11 (revised), D-12, D-29 and "Hệ quả hiệu năng"
    - src/physics/ragdoll.ts functions ownBounds and createRagdoll (bounds rule to mirror, attach detach), src/render/room.ts function toFloatGeometry (quantized attribute decode pattern)
    - node_modules/three/src/objects/SkinnedMesh.js (bind, updateMatrixWorld, computeBoundingBox, applyBoneTransform)
    - tests/e2e/npc.spec.ts, tests/e2e/slap.spec.ts, tests/e2e/helpers.ts
  </read_first>
  <behavior>
    - Synthetic model in Node (no GLB): Group 'model' → part 'torso' (position 0,0.7,0) with a Mesh child (BoxGeometry 0.8×0.9×0.6), torso → nested part 'head' (position 0,1.2,0, scale 0.1) with a Mesh child (BoxGeometry 8×8×8), model → part 'leg-left' with a Mesh child whose position attribute is a normalized Int16 BufferAttribute: buildRigidSkin(model, ['torso','head','leg-left'], material, key) returns one SkinnedMesh whose vertex count equals the sum of the three source meshes; skinIndex component 0 of every vertex is the owning part's index (the torso mesh never gets index 1, the nested head mesh never gets index 0); skinWeight is (1,0,0,0) everywhere
    - Each part node gets exactly one Bone child named 'bone-<part>' with identity local transform; the three source meshes have visible === false and still exist under their part nodes; the SkinnedMesh is a direct child of model with frustumCulled === false
    - After model.updateMatrixWorld(true) the skinned bounding box (computeBoundingBox, then applyMatrix4(mesh.matrixWorld)) equals the union of the three source meshes' world boxes within 1e-4, including the decoded Int16 leg
    - Moving part 'leg-left' by +2 m on x and updating world matrices moves the skinned box max.x by about +2 m while the torso vertices stay put; scaling or moving model moves the whole skinned box
    - Re-parenting 'torso' to a separate identity Group with attach() (the ragdoll detach) leaves the skinned world box unchanged
    - Two calls with the same cacheKey on two clones return meshes sharing the same BufferGeometry instance; a different cacheKey builds a new geometry
    - A missing part name throws 'rigid skin: no part <name>'; a part without own meshes throws 'rigid skin: part <name> has no mesh'
    - characters.spec (desktop) '3 NPCs render one draw per character': './?autoplay=1' → after 1500 ms __bt.characters.skinnedMeshes is 4, drawsPerCharacter is 1 and __bt.hud.drawCalls ≤ 90 (01-14 measured 104 before the merge); prints a line starting 'MEASURE npcs=3' with drawCalls, peakDrawCalls, bodies
    - characters.spec (desktop) 'skin follows walking and ragdoll parts': './?autoplay=1&npcs=1&npcAt=0.9,1.0' → __bt.characters.skin has an entry for letter 'a' within 0.6 m (XZ) of __bt.player.pos and height 1.2..1.8, and one for letter 'b' within 0.6 m of __bt.npcs[0].pos; press KeyE, wait __bt.ragdolls.active 1 plus 400 ms → the 'b' entry centre is within 1.0 m (XZ) of __bt.npcs[0].pos (the torso)
    - characters.spec (desktop) '10 NPCs smash within the bench budget': './?autoplay=1&npcs=10&scenario=smash' → __bt.npcs length 10 with 10 distinct textures, none 'a'; wait __bt.debris.broken ≥ 10 (timeout 10000) then 1500 ms → __bt.hud.peakDrawCalls ≤ 120, __bt.hud.peakBodies ≤ 200, __bt.characters.skinnedMeshes 11; prints 'MEASURE npcs=10' with drawCalls, peakDrawCalls, bodies, peakBodies
    - no console errors, pageerrors or off-origin requests in any characters.spec test
  </behavior>
  <action>Implements RESEARCH A6 (the "Hệ quả hiệu năng" consequence of D-29 / D-11 revised) as a tested builder. Write tests/unit/rigidSkin.test.ts first and confirm failure, then implement src/render/rigidSkin.ts until green:
1) model.updateMatrixWorld(true); modelInverse = inverse of model.matrixWorld.
2) For part index i, find the node by exact name (throw if missing) and collect its own meshes with the ragdoll ownBounds rule: walk children recursively, skip any child whose name is in partNames (a nested part); throw if none.
3) Geometry (build only when the module WeakMap has no entry for cacheKey): for each own mesh copy POSITION, NORMAL and UV into Float32 attributes through getComponent (quantized normalized attributes decode correctly; compute normals when NORMAL is missing; drop every other attribute), bake modelInverse × mesh.matrixWorld into positions and its normal matrix into normals, add skinIndex (Uint16, itemSize 4, value i,0,0,0) and skinWeight (Float32, itemSize 4, value 1,0,0,0), make all pieces indexed (or all non-indexed) and merge with mergeGeometries(list, false); computeBoundingBox and computeBoundingSphere; store under cacheKey.
4) For each part node add a Bone named 'bone-' + name with identity position/quaternion/scale.
5) mesh = new SkinnedMesh(geometry, material), name 'character-skin', frustumCulled false (a ragdoll part can fly 9 m from the root, so the cached sphere would cull wrongly), model.add(mesh), model.updateMatrixWorld(true), mesh.bind(new Skeleton(bones)).
6) Set visible = false on every collected own mesh. Keep them: ragdoll collider sizing reads their bounding boxes and the pointer-pick ray still hits them.
Then write tests/e2e/characters.spec.ts per the behavior list (the MEASURE lines go to stdout via console.log inside the spec) and confirm it fails against the current build.</action>
  <verify>
    <automated>npx vitest run tests/unit/rigidSkin.test.ts && echo UNIT_GREEN && npx vite build && echo BUILD_OK && { npx playwright test tests/e2e/characters.spec.ts --project=desktop; test $? -ne 0 && echo E2E_RED; }</automated>
  </verify>
  <acceptance_criteria>
    - Output contains UNIT_GREEN, BUILD_OK and E2E_RED, and the e2e failures are on assertions or missing __bt keys (not "No tests found")
    - `grep -c "frustumCulled = false" src/render/rigidSkin.ts` prints at least 1 and `grep -c "mergeGeometries" src/render/rigidSkin.ts` prints at least 1
    - tests/unit/rigidSkin.test.ts contains "Int16Array", "attach(" and "computeBoundingBox"
  </acceptance_criteria>
  <done>The rigid-skin approach is proven in Node (bind, animate, re-parent, quantized input, shared geometry); the budget e2e is failing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Characters render through the rigid skin (GREEN for 3 NPCs and ragdoll)</name>
  <files>src/render/characters.ts</files>
  <read_first>
    - src/render/rigidSkin.ts, tests/unit/rigidSkin.test.ts, tests/e2e/characters.spec.ts
    - src/render/characters.ts, src/game/player.ts, src/game/npc.ts (idle pose sampling and get-up re-attach), src/physics/ragdoll.ts, src/render/highlight.ts
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-14-SUMMARY.md decisions (texture sampler, model faces +Z, 1.5 m fit)
  </read_first>
  <behavior>
    - characters.spec '3 NPCs render one draw per character' and 'skin follows walking and ragdoll parts' pass; npc.spec, slap.spec, room.spec, controls.spec and smoke.spec stay green
  </behavior>
  <action>Implements the D-11 (revised) / D-29 performance prerequisite without changing any gameplay behaviour. In spawnCharacter, after the clone is scaled and every mesh has the per-letter Lambert material (01-14 texture per NPC stays), call buildRigidSkin(model, CHARACTER_PARTS, material, asset.scene) BEFORE the AnimationMixer is created or updated (the builder needs the bind pose) and expose the result as CharacterInstance.skinned. Keep `parts` as the named part nodes (the ragdoll and the get-up blend keep driving them; their Bone children follow). Keep a module Set of live instances: add on spawn, remove in dispose (dispose also calls skinned.skeleton.dispose()). Extend the existing 'characters' debug getter (do not register a new key) with skinnedMeshes (live instance count), drawsPerCharacter 1, and skin: for each live instance, lazily inside the getter, skinned.computeBoundingBox(), copy the box, applyMatrix4(skinned.matrixWorld), report letter, centre [x,y,z] and height. Do not change player.ts, npc.ts, ragdoll.ts or highlight.ts: highlight glow clones the shared material for the SkinnedMesh like any mesh, and the hidden part meshes keep the ray target working. Run the probe numbers: note drawCalls at 3 NPCs for the SUMMARY.</action>
  <verify>
    <automated>npm run typecheck && echo TYPECHECK_OK && npx vitest run && echo UNIT_OK && npx vite build && echo BUILD_OK && npx playwright test tests/e2e/characters.spec.ts -g "3 NPCs|skin follows" --project=desktop && npx playwright test tests/e2e/npc.spec.ts tests/e2e/slap.spec.ts tests/e2e/room.spec.ts tests/e2e/controls.spec.ts tests/e2e/smoke.spec.ts && echo E2E_SLICE_OK</automated>
  </verify>
  <acceptance_criteria>
    - Output contains TYPECHECK_OK, UNIT_OK, BUILD_OK and E2E_SLICE_OK with no line containing " failed"
    - `grep -c "buildRigidSkin(" src/render/characters.ts` prints 1 and `grep -c "registerDebug('characters'" src/render/characters.ts` prints 1
    - `git diff --name-only -- src/game/player.ts src/game/npc.ts src/physics/ragdoll.ts src/render/highlight.ts | wc -l` prints 0
  </acceptance_criteria>
  <done>Every character is one draw call, textures per NPC and slap/ragdoll/get-up are unchanged, and the skin provably follows parts in both modes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Hand-placed routes for NPCs 9–10, ?npcs clamp 0..10, budget measured at 3 and 10 NPCs (GREEN)</name>
  <files>src/game/waypoints.ts, tests/unit/waypoints.test.ts, src/game/game.ts, tests/e2e/npc.spec.ts</files>
  <read_first>
    - src/game/waypoints.ts, tests/unit/waypoints.test.ts, src/game/layout.ts (DESKS, PANTRY, PROP_PLACEMENTS, CORRIDOR, ROOM)
    - src/game/game.ts (DEFAULT_NPCS, MAX_NPCS, npcCountFromQuery, the NPC spawn loop), src/physics/ragdoll.ts (MAX_GROUP_INDEX 14 covers index 9)
    - tests/e2e/npc.spec.ts tests 'npcs=8 spawns eight from the shared character' and 'npcs clamps to 0..8', tests/e2e/characters.spec.ts
    - .planning/phases/01-spike-k-thu-t-ng-deploy/01-14-SUMMARY.md deviation 2 (why the lanes exist)
  </read_first>
  <behavior>
    - waypoints unit: NPC_ROUTES.length is 5 (the old '3 looping routes' assertion becomes 5 with a comment: D-29 needs hand-placed routes for NPCs 9–10); every route keeps ≥ 2 dwell stops, finite points, no zero-length segment, ≥ 0.5 m wall clearance, the ROUTE_OBSTACLES clearance and the CORRIDOR check (the existing loops now cover routes 3 and 4)
    - waypoints unit: routeIndexForNpc(0..9) = [0,1,2,0,1,2,0,1,3,4]; sharedIndexForNpc(0..9) = [0,0,0,1,1,1,2,2,0,0]; values outside 0..9 or NaN are clamped/truncated into 0..9 first; every returned route index < NPC_ROUTES.length
    - waypoints unit: routes 3 and 4 keep every sampled point ≥ 0.6 m (centre distance) from the home position of every floor-standing dynamic prop with role trashcan, boxClosed, pottedPlant or plantSmall (PROP_PLACEMENTS entries without `on`), so the new NPCs do not shove a prop on every loop
    - waypoints unit: the existing stop-count test for routes 0–2 is unchanged
    - npc.spec: 'npcs=8 spawns eight…' becomes 'npcs=10 spawns ten from the shared character' (10 ids, 10 distinct textures, none 'a', inside the room after 1 s, shadows ≥ dynamicCount + 11); 'npcs clamps to 0..8' becomes 'npcs clamps to 0..10' (npcs=99 → 10, npcs=0 → 0); each with a comment citing D-29 / D-11 revised
    - characters.spec '10 NPCs smash within the bench budget' passes; the full Playwright suite is green
  </behavior>
  <action>Implements D-29's "NPC thứ 9–10 cần tuyến đường đặt tay thêm (vẫn không navmesh)" and D-11 revised (ceiling 10). Update the unit tests first and confirm failure. In waypoints.ts append two routes built from layout constants and the existing lane helpers (WEST_X, NORTH_Z, westMid, westNorth), starting from these coordinates: route 3 (NPC 9, desk d2 ↔ pantry counter east end) = d2 stop (d2.x, d2.z + BEHIND_CHAIR_Z, dwell 3) → westMid → westNorth → (3.6, NORTH_Z) → (PANTRY.counter.x + 0.6, PANTRY.counter.z + 1.1, dwell 3) → (3.6, NORTH_Z) → westNorth → westMid; route 4 (NPC 10, east window ↔ storage boxes) = (6.2, 1.0, dwell 3) → (3.0, 1.0) → (3.0, 3.9) → (4.85, 3.9, dwell 3) → (3.0, 3.9) → (3.0, 1.0). If a clearance test fails, move only the offending point by the smallest amount that passes and record the final coordinates in the SUMMARY; never loosen a test threshold. Add routeIndexForNpc and sharedIndexForNpc (so NPCs 1–8 keep exactly the routes and offsets they had in 01-14..01-16 and the earlier measurements stay comparable). In game.ts set MAX_NPCS = 10, clamp npcCountFromQuery to [0, MAX_NPCS] (missing or unparsable still gives 3), and pick route = NPC_ROUTES[routeIndexForNpc(i)], startIndex = i % route.length, spawn offset = sharedIndexForNpc(i) × SHARED_ROUTE_OFFSET; update the doc comments (0..10). Texture letters b..k stay within a..r. Then run the full suite, run characters.spec once more and copy both MEASURE lines (3 and 10 NPCs: drawCalls, peakDrawCalls, bodies, peakBodies) into the SUMMARY next to the 01-14/01-16 numbers (104 / 134 draws before the merge).</action>
  <verify>
    <automated>npm run typecheck && echo TYPECHECK_OK && npx vitest run && echo UNIT_OK && npx vite build && echo BUILD_OK && node scripts/size-report.mjs --gate && npx playwright test && echo E2E_OK</automated>
  </verify>
  <acceptance_criteria>
    - Output contains TYPECHECK_OK, UNIT_OK, BUILD_OK, SIZE_GATE_OK and E2E_OK, and the Playwright output has no line containing " failed"
    - `grep -vE "^\s*(//|\*)" src/game/game.ts | grep -c "Math.min(8"` prints 0 and `grep -c "MAX_NPCS = 10" src/game/game.ts` prints 1
    - `grep -ciE "navmesh|pathfind" src/game/waypoints.ts` prints 0
    - The SUMMARY contains a table with draw calls and bodies at 3 and at 10 NPCs taken from the MEASURE lines, and the 10-NPC peak draw calls value is ≤ 120
  </acceptance_criteria>
  <done>Ten coworkers walk hand-placed routes and the full cast stays inside the 120 draw-call / 200 body budget, measured before the benchmark is built.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query `npcs` → entity count | Visitor-controlled number that allocates Rapier bodies and textures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-23-01 | Denial of service | `?npcs=` | mitigate | Integer parse clamped to [0, 10]; 6 pooled ragdoll bodies + 1 capsule per NPC → at most 70 NPC bodies; e2e asserts npcs=99 → 10 and peak bodies ≤ 200 |
| T-01-23-02 | Denial of service | GPU memory on iOS (RESEARCH Pitfall 8) | accept | 11 textures × 512² RGBA without mipmaps ≈ 11 MB; merged geometry is shared per asset; one tiny bone texture per character; the 01-18 soak watches renderer memory |
| T-01-23-03 | Tampering | wrong culling / invisible ragdoll after the merge | mitigate | frustumCulled false on the skinned mesh; e2e 'skin follows walking and ragdoll parts' checks the skinned bounds against the capsule and torso |
</threat_model>

<verification>
- `npm run typecheck && npx vitest run && npx vite build && node scripts/size-report.mjs --gate && npx playwright test` exits 0 with all markers
- SUMMARY records measured draw calls and bodies at 3 and 10 NPCs
</verification>

<success_criteria>
- ?npcs=10 works on hand-placed routes, one draw per character, peak draw calls ≤ 120 at 10 NPCs in the smash scenario — ready for the 01-17 benchmark
</success_criteria>

## Artifacts this phase produces

- Symbols: `RigidSkin`, `buildRigidSkin`, `CharacterInstance.skinned`, `routeIndexForNpc`, `sharedIndexForNpc`, `MAX_NPCS = 10`
- Scene objects: one `SkinnedMesh` named `character-skin` per character with bones `bone-<part>`
- URL param: `npcs=0..10`
- `window.__bt.characters` extra fields: `skinnedMeshes`, `drawsPerCharacter`, `skin[] { letter, centre, height }`
- Routes: `NPC_ROUTES[3]` (desk d2 ↔ pantry counter), `NPC_ROUTES[4]` (east window ↔ storage boxes)
- Tests: `tests/unit/rigidSkin.test.ts`, `tests/e2e/characters.spec.ts`; changed `tests/unit/waypoints.test.ts`, `tests/e2e/npc.spec.ts`
- SUMMARY table: draw calls / bodies at 3 and 10 NPCs

<output>
Create `.planning/phases/01-spike-k-thu-t-ng-deploy/01-23-SUMMARY.md` when done
</output>
